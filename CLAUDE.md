# GarudaX — Multi-Tenant AI-Native Trading Platform

## Product Vision

GarudaX is a **multi-tenant, AI-native operating platform** that hosts regulated trading venues. Each tenant is an independent exchange with its own trading rules, participants, and regulatory requirements.

### Tenants
- **ace-commodities** (ACTIVE) — Mongolian commodity exchange: wheat, barley, cattle, cashmere, wool — physical delivery via eWR
- **mse-equities** (ONBOARDING, flagship) — Mongolian Stock Exchange: equities, bonds, ETFs — T+2 settlement, CSD integration

### Platform Invariant
> GarudaX is the platform. Tenants are the venues. MSE is the flagship. Tenant ID is never optional.

### Current State (refreshed 2026-06-26 by R016 from the R015 fresh full-stack run)
- 14 Go modules under `src/*/go.mod` (auth-service, clearing-engine, compliance-service, corporate-actions, fix-gateway, gateway, margin-engine, market-data-service, matching-engine, platform-service, securities-service, settlement-engine, shared, warehouse-service) + 2 shared zero-dep sub-modules (`shared/pkg/types/decimal`, `shared/pkg/tenant`)
- 3 React SPAs (web-ui trading terminal :3000, admin-ui dashboard :3001, demo-runner :3002)
- Securities module complete (Phase 7): instrument CRUD, order matching, T+2 settlement, 9 MCP tools
- Multi-tenant platform pivot in progress — gateway tenant enforcement live (R011); cross-tenant authZ + backend gRPC enforcement still open
- ~2769 Go test funcs (+515 `t.Run` subtests); R015 ran all Go tests green; e2e suite 32 top-level / 141 subtests / 8 graceful skips / 0 fail on a freshly-built stack
- Latest integration verdict: `run-20260626-051039` (R015) **PASS** — supersedes the stale 2026-03-29 FAIL narrative. See the dated R016 pattern in Learned Patterns for the full metric reconciliation.

### Roadmap
- **Phase 0.5** (complete): Multi-tenant platform specs — architecture, migrations V29-V30, tenant context design
- **Phase 0.6**: ace-commodities retrofit — schema renames, tenant context middleware, code updates, Kafka topic migration
- **Phase 0.7**: Platform control plane — tenant registry service, lifecycle workflows, platform-admin API
- **Phase 0.8**: mse-equities flagship build — equities domain, corporate actions, FRC reporting, MCSD integration
- **Phase 9**: FIX protocol gateway (tenant-aware broker connectivity)
- **Phase 10**: AI bot expansion (tenant-scoped operations)

---

# Self-Learning Softhouse — Architecture Reference

This file is the persistent memory for an AI-driven software development pipeline.
Read this at the start of every session before planning or spawning agents.

---

## Architecture Overview

```
User requirement
  → Planner Agent        (task graph + dependency tree + estimates)
  → Orchestrator         (TaskCreate · spawn workers · monitor · merge branches)
      → Coder            (isolated worktree per task)
      → Test Writer      (isolated worktree per task)
      → Docs             (isolated worktree per task)
      → Security         (isolated worktree per task)
      → Arch Review      (isolated worktree per task)
  → Reviewer Agent       (code quality · security · approve/reject)
  → Integration Test     (full build · CI pass · coverage gate)
  → PostMortem Agent     (writes outcomes back to this file)
```

---

## Agent Roles

### Planner Agent
- Input: raw user requirement (natural language)
- Output: structured `tasks.json` with fields: `id`, `title`, `description`, `dependencies: []`, `estimate_minutes`, `worktree_branch`
- Always reads the **Learned Patterns** section below before generating the plan
- Uses `subagent_type: Plan`

### Orchestrator
- Reads `tasks.json` and creates tasks via `TaskCreate`
- Spawns one worker agent per task using `isolation: "worktree"`
- Runs independent tasks in parallel (background: true)
- Monitors completion via task notifications
- Merges approved branches only
- Checkpoints state into git after every agent completes — never hold state only in memory

### Worker Agents (Coder, Test Writer, Docs, Security, Arch Review)
- Each runs in its own isolated git worktree branch
- Writes a `handoff/<task-id>.md` on completion with: summary, decisions made, blockers found, suggested follow-ups
- Reads `handoff/` files from upstream tasks before starting

### Reviewer Agent
- Reads worker output + `handoff/<task-id>.md`
- Evaluates: correctness, security, style, test coverage
- Writes verdict to `handoff/<task-id>-review.md`: APPROVED / REJECTED + reason
- Rejected tasks are re-queued by the Orchestrator with reviewer notes injected into context

### Integration Test Agent
- Triggers after all branches merged
- Runs full build + test suite
- Writes pass/fail + coverage to `handoff/integration-<run-id>.md`
- Failure triggers PostMortem Agent

### PostMortem Agent
- Runs after every completed feature (success or failure)
- Extracts: what worked, what failed, what estimate was wrong, any new project-specific patterns
- **Appends findings to the Learned Patterns section of this file**
- Commits the updated `CLAUDE.md` to git

---

## State Management (Persistence Strategy)

Sessions do not survive terminal close. Compensate by externalizing all state:

| State | Storage location |
|---|---|
| Task graph | `tasks.json` (committed to git) |
| Agent handoffs | `handoff/<task-id>.md` (committed) |
| Review verdicts | `handoff/<task-id>-review.md` (committed) |
| Learned patterns | This file, **Learned Patterns** section |
| Session recovery | Read `tasks.json` + `handoff/` on startup to reconstruct context |

**Session startup checklist:**
1. Read this file (`CLAUDE.md`)
2. Read `tasks.json` — identify incomplete tasks
3. Read all `handoff/` files for completed tasks
4. Resume from last incomplete task

---

## Agent-to-Agent Communication

Agents cannot message each other directly. Use the shared `handoff/` directory:

```
handoff/
  task-3.md              # Coder output summary
  task-3-review.md       # Reviewer verdict
  task-5.md
  integration-run-1.md
```

Convention:
- Worker writes `handoff/<task-id>.md` on completion
- Reviewer writes `handoff/<task-id>-review.md`
- Downstream agents read upstream `handoff/` files before starting
- Orchestrator reads all `handoff/` files to track global state

---

## The Learning Loop

This is what makes it a softhouse rather than a pipeline.

After each feature:
1. PostMortem Agent reads: reviewer notes, integration results, test failures, timing deltas
2. Extracts reusable patterns specific to this codebase and domain
3. Appends to **Learned Patterns** below
4. Planner reads Learned Patterns on every new task → plans improve over time

The learning is not gradient descent — it is structured knowledge accumulation.
It compounds: week-1 plans are generic, week-8 plans are codebase-aware.

---

## Known Limitations

| Limitation | Current workaround |
|---|---|
| Sessions don't persist | Externalize all state to git (see above) |
| No agent-to-agent messaging | `handoff/` directory convention |
| No mid-flight plan restructuring | Orchestrator re-plans from scratch on critical failure; reads `handoff/` for context |
| Finite orchestrator context | Summarize completed task outputs; don't pass full file contents up |
| No external triggers (CI, Slack) | Manual session restart with `tasks.json` resume |

---

## Task JSON Schema

```json
{
  "tasks": [
    {
      "id": "task-1",
      "title": "Short title",
      "description": "Detailed description for the worker agent",
      "agent_role": "coder | test_writer | docs | security | arch_review",
      "dependencies": [],
      "blockedBy": ["task-0"],
      "estimate_minutes": 20,
      "status": "pending | in_progress | done | rejected",
      "worktree_branch": "feature/task-1-short-title"
    }
  ],
  "feature": "Human-readable feature name",
  "created_at": "ISO timestamp",
  "last_updated": "ISO timestamp"
}
```

---

## Learned Patterns

> PostMortem Agent appends new entries here after each completed feature.
> Planner Agent reads this section before every planning pass.

<!-- LEARNED PATTERNS START — do not remove this comment -->

### Pattern: Design-first tasks succeed; implementation tasks need code scaffolding (2026-03-27)
- **Context:** Phase 0 foundation run — 12 tasks planned, 3 completed (T001, T002, T004), 5 rejected (T003, T005, T006, T007, T015), 4 still pending.
- **Finding:** All 3 completed tasks produced documents or declarative artifacts (ADR, Terraform modules, SQL migrations). All 5 rejected tasks required either a running environment (T003 EKS/Istio, T006 CI/CD) or application code implementation (T005 Auth, T007 Exchange spec, T015 KYC spec). Rejected tasks had no review files (`*-review.md`), so rejection reasons are undocumented — a process gap.
- **Action:** Planner should ensure that implementation tasks (agent_role: builder) are not scheduled until the language-level scaffolding exists (go.mod, main.go, basic project structure). Add an explicit "project init" task per service before any builder task. Architect spec tasks (T007, T015) should depend on project init too, so specs can reference real package paths.

### Pattern: Pre-provision downstream dependencies in infrastructure tasks (2026-03-27)
- **Context:** T002 (Terraform) pre-created the IRSA OIDC provider and Karpenter role for T003 to consume.
- **Finding:** This forward-provisioning pattern meant T003 had clear inputs. T002's handoff explicitly listed what T003 should use. This is the right model even though T003 was ultimately rejected for other reasons.
- **Action:** Infrastructure tasks should always pre-provision IAM roles, secrets, and config that downstream tasks need, and list them explicitly in the handoff file under "Suggested Follow-ups."

### Pattern: Handoff cross-references are high-value — enforce them (2026-03-27)
- **Context:** T001, T002, and T004 handoffs all included specific "Suggested Follow-ups" naming downstream task IDs and which sections/outputs to reference.
- **Finding:** These cross-references (e.g., "T005 should use the `auth` schema", "T003 needs OIDC provider ARN from eks module output") create a dependency contract that downstream agents can act on without re-reading all upstream code.
- **Action:** Worker agent prompt should require a "Suggested Follow-ups" section in every handoff file, with at minimum: downstream task IDs, specific artifacts/outputs they should consume, and any constraints they must respect.

### Pattern: Reviewer must always produce a review file — even for rejections (2026-03-27)
- **Context:** 5 tasks were rejected (T003, T005, T006, T007, T015) but zero `handoff/*-review.md` files exist.
- **Finding:** Without review files, the PostMortem agent cannot root-cause rejections, and the Orchestrator cannot inject reviewer notes when re-queuing. The learning loop is broken for rejected tasks.
- **Action:** Orchestrator must enforce that every status transition to "rejected" is accompanied by a `handoff/<task-id>-review.md` file. If a task is rejected without a review file, flag it as a process violation before re-queuing.

### Pattern: Integration tests are vacuous without application code — gate on code existence (2026-03-27)
- **Context:** Integration test run `run-20260327-074658` passed with 0 tests, 0 failures, N/A coverage. All 7 services had only stub Dockerfiles.
- **Finding:** A green integration run with no tests gives false confidence. The integration agent correctly noted this but the result was still "PASS."
- **Action:** Integration test agent should report `SKIP` (not `PASS`) when no buildable source code or test files are found. Planner should not schedule integration runs until at least one service has compilable code and tests.

### Pattern: Estimate data is unusable without start/finish timestamps on completed tasks (2026-03-27)
- **Context:** T001, T002, T004 are marked "done" but have no `started_at` or `finished_at` fields. Only rejected tasks have timestamps, and those are all identical (instant rejection).
- **Finding:** Without timing data on successful tasks, the PostMortem agent cannot evaluate estimate accuracy, which means the Planner cannot calibrate future estimates.
- **Action:** Orchestrator must record `started_at` when a worker agent begins and `finished_at` when it completes (for both done and rejected). These fields should be mandatory in `tasks.json` for any non-pending task.

### Pattern: Learning loop is functional — second integration run self-corrected (2026-03-27)
- **Context:** Phase 0 second integration run (`run-20260327-075008`) followed the first run (`run-20260327-074658`) which incorrectly reported PASS with zero tests.
- **Finding:** The second run correctly reported `SKIP` instead of `PASS`, citing the learned pattern from the first postmortem. This confirms the PostMortem → CLAUDE.md → Agent read loop is working as designed.
- **Action:** Continue the pattern of having agents read Learned Patterns before execution. When a pattern corrects agent behavior successfully, note it so we can distinguish validated patterns from untested ones.

### Pattern: Rejected tasks produce no handoff files — workers need a fail-safe output (2026-03-27)
- **Context:** T003, T005, T006, T007, T015 were all rejected but none produced a `handoff/<task-id>.md` file. Only the completed tasks (T001, T002, T004) have handoff files.
- **Finding:** Workers that fail early (3-7 min vs 180-300 min estimate) exit without writing any handoff. This means neither the Reviewer, Orchestrator, nor PostMortem agent has any diagnostic data. Combined with the missing review files (Pattern #4 above), rejected tasks are completely opaque.
- **Action:** Worker agent prompt should mandate writing a handoff file as the FIRST action (with status "in-progress") and updating it on exit regardless of success/failure. The handoff should include: what was attempted, what blocked progress, and what prerequisites were missing. This is separate from the Reviewer's review file.

### Pattern: Parallel scheduling of tasks with shared missing prerequisites wastes all slots (2026-03-27)
- **Context:** T003 (EKS+Istio), T005 (Auth Service), and T006 (CI/CD) were all started in parallel at `2026-03-27T07:50:08Z`. All three failed within 7 minutes for the same root cause: no application code scaffolding exists.
- **Finding:** Running 3 agents in parallel when they share the same unmet prerequisite (no `go.mod`, no source files) means 3x the compute cost for zero output. A single "canary" task could have detected the blocker before committing the other two slots.
- **Action:** When the Orchestrator launches a batch of parallel tasks that share a common dependency (e.g., "compilable source code exists"), run ONE task first as a canary. If it fails within 20% of its estimate, hold the remaining tasks and surface the blocker to the Planner for re-planning.

### Pattern: Rejected task timing reveals instant-fail vs partial-progress — use this signal (2026-03-27)
- **Context:** All 5 rejected tasks completed in 3-7 minutes against estimates of 180-300 minutes. T003/T005/T006 took ~7 min; T007/T015 took ~3 min.
- **Finding:** A task finishing in <5% of its estimate is a strong signal that prerequisites were missing entirely, not that the task was difficult. This is qualitatively different from a task that runs for 80% of its estimate and then fails (which would indicate a real implementation problem).
- **Action:** Orchestrator should detect "instant rejection" (completion in <10% of estimate) and treat it as a prerequisite failure, not a task failure. Instead of re-queuing the same task, it should flag the missing prerequisite and ask the Planner to insert a new dependency task.

### Pattern: AI agent estimates need 10-20x reduction from human estimates (2026-03-27)
- **Context:** Phase 1-2 pipeline run — 10 tasks with timing data. Estimates ranged 180-480 minutes. Actual completion times ranged 5-21 minutes.
- **Finding:** Every completed task finished in <5% of its estimate. T008 (Matching Engine, est 480m) took 21 min. T027 (Clearing Engine, est 480m) took 8.5 min. T006 (CI/CD, est 180m) took 5 min. Spec tasks (T007, T015) took 8-10 min against 180-240m estimates. The estimates appear calibrated for human developers, not AI agents working in isolated worktrees.
- **Action:** Planner should use estimates of 10-30 minutes for AI agent tasks, not human-scale estimates. Reserve 180+ minute estimates only for tasks requiring external system interaction (cloud provisioning, CI pipeline runs). Use actual timing from this run as calibration: spec tasks ~10 min, service implementation ~15-20 min, infrastructure/CI ~5-10 min.

### Pattern: Zero-dependency Go module per service is the winning architecture pattern (2026-03-27)
- **Context:** Four exchange-core services (matching-engine, clearing-engine, margin-engine, settlement-engine) were each built as independent Go modules with zero external dependencies.
- **Finding:** All four built cleanly, passed all 165 tests, and achieved ~43% average coverage. The zero-dep approach eliminated version conflicts, simplified builds, and made each service independently testable. The identical `Decimal(18,4)` type was copied into each module — duplication but zero coupling.
- **Action:** Continue the zero-dep Go module pattern for new services. Accept Decimal type duplication across services rather than introducing a shared library (the coupling cost outweighs the DRY benefit at this stage). When a shared types library becomes warranted (5+ services duplicating), extract `pkg/types/decimal.go` as an internal module.

### Pattern: Port allocation convention — document to prevent collisions (2026-03-27)
- **Context:** Four services independently chose non-colliding port pairs: matching-engine (50051/8081), clearing-engine (50052/8082), margin-engine (50053/8083), settlement-engine (50054/8084).
- **Finding:** The pattern emerged naturally from handoff cross-references (each service noted the previous service's ports). Format is `gRPC: 5005x / health HTTP: 808x` where x increments per service.
- **Action:** Planner should assign port numbers in task descriptions for new services. Next available: 50055/8085. Maintain this table: matching=50051, clearing=50052, margin=50053, settlement=50054, auth=50055(suggested), compliance=50056(suggested), gateway=8080(HTTP), market-data=50057(suggested), warehouse=50058(suggested).

### Pattern: Spec-first then implement produces clean code with fewer review issues (2026-03-27)
- **Context:** T007 (Exchange Spec) → T008 (Matching Engine) pipeline. T015 (KYC/AML Spec) produced clean artifacts. Both spec tasks were APPROVED with zero required fixes.
- **Finding:** T008 implementation referenced T007's spec directly (protobuf contracts, SQL migration, matching algorithm pseudocode) and was APPROVED with zero required fixes and only non-blocking suggestions. By contrast, T003 (no spec, direct implementation of EKS+Istio) was REJECTED with 3 correctness bugs. Specs create a verifiable contract that downstream workers can implement against.
- **Action:** For any service with business logic (not pure infrastructure), Planner should create a spec task before the implementation task. Spec task produces: architecture doc, protobuf/API contracts, SQL migration. Implementation task consumes these as inputs.

### Pattern: Code review catches real bugs in infrastructure code — keep reviews mandatory (2026-03-27)
- **Context:** T003 (EKS+Istio) was REJECTED by the reviewer. T008 (Matching Engine) was APPROVED but with 4 non-blocking suggestions including an overflow risk.
- **Finding:** T003 review caught: (1) duplicate YAML key silently dropping `holdApplicationUntilProxyStarts: true` — a startup reliability bug, (2) wrong DestinationRule host pattern (`*.local` vs `*.svc.cluster.local`) — an mTLS routing bug, (3) variable type mismatch for node groups. These are subtle bugs that YAML/Terraform linting alone wouldn't catch. The reviewer agent is providing genuine value.
- **Action:** Never skip reviews for cost/speed reasons. For infrastructure code specifically, add a suggestion to include YAML lint and Terraform validate in the worker's pre-handoff checks so the reviewer can focus on semantic correctness rather than syntactic issues.

### Pattern: Rework pipeline works — T005 rejected then approved on second pass (2026-03-27)
- **Context:** T005 (Auth Service) was rejected in the first pass (~8.5 min, instant-fail pattern) then re-run and approved with a thorough review (JWT, PKCE, RBAC, bcrypt, parameterized SQL — all passing).
- **Finding:** The second run produced a complete, production-quality auth service with zero required fixes. The reviewer noted only non-blocking suggestions (PKCE refresh token gap, S256-only restriction, rate limiting). This confirms the rework loop works when the prerequisite issue (missing scaffolding) is resolved.
- **Action:** When re-queuing a previously rejected task, inject both the original rejection reason AND any new context (e.g., scaffolding now exists, upstream handoffs available) into the worker's prompt. The worker should not need to rediscover what went wrong.

### Pattern: Integration test run correctly distinguished SKIP vs PASS vs real PASS (2026-03-27)
- **Context:** Three integration runs: run-074658 (PASS with 0 tests — incorrect), run-075008 (SKIP — correct per learned pattern), run-080509 (PASS with 165 tests — genuinely correct).
- **Finding:** The third integration run validated 4 services (matching, clearing, margin, settlement) with 165 passing tests and ~43% average coverage. Coverage ranged from 23.7% (novation) to 89.3% (orderbook). The core matching logic (orderbook) has the highest coverage, which is appropriate for a financial system.
- **Action:** Set a coverage floor of 60% for business-critical packages (orderbook, position, novation, pnl) and 30% for infrastructure packages (server, config, store). The current ~43% average is acceptable for phase 1-2 but should increase as services mature.

### Pattern: Phase 3 achieved zero rejections — spec-first + calibrated estimates = clean runs (2026-03-28)
- **Context:** Phase 3 pipeline run — 9 tasks (T031-T039), all 9 approved on first pass, zero rejections. This is the first run with a perfect approval rate.
- **Finding:** Three factors contributed: (1) spec-first for all business services (T033→T034, T035→T036, T037→T038), (2) AI-calibrated estimates (10-25 min vs Phase 1-2's 180-480 min), (3) all prerequisites satisfied before task launch. Actual times: specs ~5-7 min, implementations ~7-9 min, integration test ~2.5 min. Estimates were 1.5-3x actual (acceptable), vs Phase 1-2's 10-50x overestimates.
- **Action:** The current estimate calibration (10m specs, 25m implementations, 15m integration) is close. Fine-tune to: specs ~10m, implementations ~15m, rework ~10m, integration ~5m. These are upper bounds, not targets.

### Pattern: PostMortem agent fails on large context — pipeline needs context size guard (2026-03-28)
- **Context:** PostMortem agent failed with `Argument list too long` (shell error E2BIG) after Phase 3. The `build_postmortem_context` function concatenates ALL handoff files into a single CLI argument string. By Phase 3, this exceeded the Linux ARG_MAX limit (~2MB).
- **Finding:** The pipeline's postmortem prompt builder reads every `handoff/*.md` file and passes them as a single `-p` argument to `claude`. With 20+ handoff files totaling ~50KB+ of text, plus the CLAUDE.md content, this exceeds shell limits.
- **Action:** Fix `pipeline/lib/context.sh` `build_postmortem_context` to either: (1) write context to a temp file and pass via `--input-file` or stdin, (2) summarize completed tasks rather than passing full handoff contents, or (3) only pass handoff files from the current run (not all historical files). Option 1 is the quickest fix.

### Pattern: Auth service code exists on a branch but was never merged to main (2026-03-28)
- **Context:** T005 (Auth Service) status was fixed to `done` and the review says APPROVED, but `src/auth-service/` on main still contains only a stub Dockerfile. The integration test confirms: "auth-service — SKIP, Dockerfile only."
- **Finding:** T005 was originally rejected (Phase 1), then the review file was written as APPROVED (likely from a rework run), but the branch was never merged. The planner updated the status to `done` without verifying the code was on main. Similarly, `src/clearing-service/` is a stub — the clearing engine lives at `src/clearing-engine/` (different directory name).
- **Action:** Before marking a rejected task as `done`, the Orchestrator must verify the worktree branch was actually merged. Add a check: `git log --oneline main | grep <branch>` or verify the expected deliverable files exist on main. For T005, the auth service code needs to be rebuilt or the original branch found and merged. The clearing-service/clearing-engine naming mismatch should be reconciled.

### Pattern: Service naming inconsistency — src directory names don't match service names (2026-03-28)
- **Context:** `src/clearing-engine/` vs `src/clearing-service/` (stub). Similarly `src/matching-engine/` vs `src/margin-engine/` vs `src/settlement-engine/` use `-engine` suffix while `src/auth-service/`, `src/compliance-service/`, `src/market-data-service/`, `src/warehouse-service/`, `src/gateway/` use `-service` or no suffix.
- **Finding:** The `-engine` suffix was used for core exchange pipeline services (matching, clearing, margin, settlement) and `-service` for supporting services (auth, compliance, market-data, warehouse). This is actually a meaningful convention — engines are the real-time trading pipeline, services are supporting infrastructure. But the stub `src/clearing-service/` directory creates confusion since the real code is in `src/clearing-engine/`.
- **Action:** Remove the stub `src/clearing-service/` directory (it only has a Dockerfile). Document the naming convention: `*-engine` for real-time trading pipeline, `*-service` for supporting services, bare name for `gateway`. Update Dockerfiles and CI to reference the correct directory names.

### Pattern: Coverage improved from 43% to ~55% average — Phase 3 services set higher bar (2026-03-28)
- **Context:** Phase 1-2 services averaged ~43% coverage. Phase 3 services: compliance 80.1% (onboarding), gateway 93% (router), market-data 68.7% (candle), warehouse 83.8% (store). The 60% floor instruction in the task descriptions worked.
- **Finding:** Explicitly stating coverage targets in task descriptions ("targeting 60%+ coverage on business logic") produced measurably higher coverage than Phase 1-2 where no target was specified. Phase 3 business-critical packages averaged ~75%, vs Phase 1-2's ~45%.
- **Action:** Always include explicit coverage targets in builder task descriptions. Use "60%+ on business logic packages, 30%+ on infrastructure packages" as the standard instruction. For financial-critical packages (orderbook, novation, pnl), raise to "80%+ coverage."

### Pattern: 3-parallel worker limit is optimal for current pipeline (2026-03-28)
- **Context:** MAX_PARALLEL=3 was used throughout. Iteration 1 had 5 ready tasks but only launched 3 (T031, T032, T033). Iterations 2-3 naturally had 2-3 ready tasks.
- **Finding:** All 3 parallel workers completed successfully in every iteration — no resource contention, no merge conflicts, no race conditions on `tasks.json`. The worktree isolation pattern prevents conflicts. The pipeline completed 9 tasks in 4 iterations over ~32 minutes of execution time.
- **Action:** Keep MAX_PARALLEL=3 as default. Consider increasing to 4-5 only when tasks are all independent (no shared file writes). The bottleneck is sequential review-then-merge after each batch, not worker concurrency.

### Pattern: Frontend implementation tasks fail without Node.js tooling — gate on runtime availability (2026-03-28)
- **Context:** Phase 4 — T048 (Trading Web UI) and T050 (Admin Dashboard) were both rejected. Both are React+TypeScript+Vite SPAs requiring `npm`/`node` to scaffold and build.
- **Finding:** The worker environment has Go tooling but no Node.js/npm. Both tasks were rejected after ~10 minutes (vs 25m estimate), the same "instant rejection" pattern seen in Phase 1 when Go scaffolding was missing. The specs (T047, T049) were approved — only the implementations failed.
- **Action:** Planner must verify that the target runtime/toolchain exists before scheduling implementation tasks. For frontend tasks, add an explicit "frontend tooling setup" task (install Node.js, create `package.json`, configure Vite) before any React implementation task. Alternatively, if the pipeline environment cannot run `npm`, mark frontend tasks as "manual" and skip them in automated runs.

### Pattern: External dependency implementations fail without library availability (2026-03-28)
- **Context:** Phase 4 — T052 (Kafka Event Wiring Implementation) was rejected. The task required a Kafka client library (`segmentio/kafka-go` or `confluent-kafka-go`) which isn't available in the zero-dependency Go module pattern.
- **Finding:** The spec (T051) was approved cleanly, but the implementation couldn't proceed without an external Go dependency for Kafka protocol support. Unlike HTTP/gRPC (which can use stdlib `net/http`), Kafka requires a dedicated client library. The zero-dep pattern that worked for all 8 Go services breaks down for integration-layer code.
- **Action:** Planner should distinguish between "business logic" tasks (zero-dep viable) and "integration glue" tasks (external deps required). Integration tasks should either: (1) allow external dependencies in `go.mod`, or (2) define an interface with a stub implementation (like the existing `TradeStore`, `CollateralSource` patterns) and defer real Kafka wiring to a deployment phase where deps are available.

### Pattern: K8s manifest tasks need cross-resource validation — YAML linting is insufficient (2026-03-28)
- **Context:** Phase 4 — T054 (Kubernetes Deployment Manifests) was REJECTED with 3 correctness bugs: cross-namespace ConfigMap reference (pods in `ace-exchange` referencing ConfigMap in `ace-services`), duplicate namespace file (`namespace.yaml` vs existing `namespaces.yaml`), and missing Secret in `ace-exchange` namespace.
- **Finding:** These bugs are cross-resource reference errors — each individual YAML file is valid, but the references between them are broken. Standard YAML linting or `kubectl --dry-run` on individual files wouldn't catch these. The reviewer correctly identified all three. This is similar to T003's rejection (duplicate YAML key, wrong DestinationRule host) — infrastructure manifests have a higher cross-reference bug rate than application code.
- **Action:** For K8s manifest tasks, the worker should include a validation step that checks: (1) every `configMapRef`/`secretRef` has a matching resource in the same namespace, (2) no duplicate resource definitions across files, (3) namespace consistency between resource definitions and references. Consider adding `kustomize build` as a pre-handoff check (catches duplicate resources).

### Pattern: Phase 4 estimate calibration is converging — 1.5-4x overestimate is acceptable (2026-03-28)
- **Context:** Phase 4 timing data across 14 tasks with timestamps. Estimates ranged 5-25 minutes. Actuals ranged 1-11 minutes.
- **Finding:** Estimate-to-actual ratios: T040 (15m est / 11m actual = 1.4x), T041 (10m/3.5m = 2.9x), T042 (5m/1m = 5x), T043 (15m/7m = 2.1x), T044 (15m/4m = 3.8x), T045 (15m/3.5m = 4.3x), T046 (5m/3m = 1.7x), T047 (10m/5m = 2x), T049 (10m/4m = 2.5x), T051 (10m/4m = 2.5x). Average ratio: ~2.6x. Phase 3 was 1.5-3x. Phase 1-2 was 10-50x. The calibration is converging but still consistently overestimates.
- **Action:** Fine-tune estimates down one more notch: cleanup/bug-fix tasks ~3m, spec tasks ~5m, coverage improvement tasks ~5m, Go service implementations ~10m, integration tests ~3m. These are upper bounds. The 5-minute minimum estimate is still too high for trivial tasks like T042 (1 minute actual).

### Pattern: Spec-approved then implementation-rejected is a tooling gap, not a spec gap (2026-03-28)
- **Context:** Three spec→implementation pairs in Phase 4: T047→T048 (UI), T049→T050 (Admin), T051→T052 (Kafka). All specs were APPROVED; all implementations were REJECTED.
- **Finding:** The rejections were not caused by spec quality issues — the specs were comprehensive and the reviewers praised them. The rejections were caused by missing tooling (Node.js for frontend, Kafka client library for event wiring). This is a different failure mode than Phase 1's rejections (missing Go scaffolding), which were fixed by adding project-init tasks. The current failure is at the environment/dependency level, not the project level.
- **Action:** Planner should tag tasks with required toolchain (`go`, `node`, `python`, `external-deps`). Orchestrator should verify toolchain availability before spawning workers. If a toolchain is unavailable, the task should be marked `blocked` (not `rejected`) with a clear reason, preserving the approved spec for when the toolchain becomes available.

### Pattern: Coverage improvement tasks are highly efficient — batch them (2026-03-28)
- **Context:** T043 (clearing-engine coverage), T044 (margin-engine coverage), T045 (settlement-engine coverage) ran in parallel. All three completed in 3.5-7 minutes, all approved first pass with zero required fixes.
- **Finding:** Coverage tasks have the best effort-to-value ratio in the pipeline: they add no new features but significantly improve confidence in existing code. T043 took novation from 23.7%→100%, netting from 39.8%→100%, engine from 32.8%→95%. T044 took engine from 76.7%→96.7%. T045 closed the only real gap (pnl CalculateBatch error path). All three workers independently identified which coverage gaps were worth closing vs which required source changes to test.
- **Action:** After any phase that adds new services, schedule a dedicated "coverage batch" iteration with one coverage task per service. These parallelize perfectly (no shared state), complete fast (~5m each), and dramatically improve the integration test signal. Estimate 5m per service for coverage tasks.

### Pattern: T054 review file exists for rejected task — reviewer process is now working (2026-03-28)
- **Context:** T054 (K8s Manifests) was REJECTED and has a detailed `handoff/T054-review.md` with 3 specific required fixes, security evaluation, and suggestions.
- **Finding:** In Phase 1, rejected tasks (T003, T005, T006, T007, T015) had no review files, breaking the learning loop. The learned pattern "Reviewer must always produce a review file" (2026-03-27) has been successfully adopted — T054's rejection includes a thorough review with actionable fixes. This means the rework pipeline has the information it needs to re-queue T054 with specific fix instructions.
- **Action:** Continue enforcing review files for all verdicts. The T054 review is a model: it identifies exactly which lines/files have bugs, explains why they're bugs, and provides fix instructions. When re-queuing T054, inject the 3 required fixes directly into the worker prompt.

### Pattern: Channel-based Kafka adapters bypass external dependency constraint (2026-03-28)
- **Context:** T052 (Kafka Event Wiring Implementation) was previously rejected in an earlier attempt because it required `segmentio/kafka-go` or `confluent-kafka-go`. The rework used Go channel-based `Producer`/`Consumer` interfaces with zero external dependencies.
- **Finding:** By defining `Producer` and `Consumer` as Go interfaces backed by `ChannelProducer`/`ChannelConsumer` implementations using Go channels, the service achieves full event-driven wiring (envelope, retry, idempotency, DLQ) without any Kafka client library. Real wire-protocol adapters can be swapped in behind the interface at deployment. All 9 services got kafka packages with 82%+ coverage. This extends the zero-dep pattern to integration-layer code.
- **Action:** For any integration task requiring an external protocol client (Kafka, Redis, AMQP), define a Go interface for the protocol operations and provide a channel/in-memory implementation. This keeps services buildable and testable without external dependencies. Wire the real client behind the interface in a deployment-specific adapter package.

### Pattern: Rebuild tasks succeed when prior review feedback is injected into context (2026-03-28)
- **Context:** T040 (Auth Service Rebuild) succeeded on first attempt, producing 43 tests and 88.5% coverage on business logic. The original T005 auth service was rejected in Phase 1, and a review file existed with specific gaps (PKCE refresh token, S256-only restriction, rate limiting).
- **Finding:** T040's task description incorporated all T005 reviewer suggestions: S256-only PKCE (rejecting `plain`), refresh token hash stored on session (fixing the gap), token theft detection triggering session revocation, and error leaking prevention. The worker addressed every prior suggestion without needing to rediscover them. This is the "rework pipeline works" pattern (2026-03-27) at a larger scale — full service rebuild, not just bug fixes.
- **Action:** When rebuilding a previously rejected or incomplete service, compile ALL prior review suggestions (both required fixes and non-blocking suggestions) into the task description. The worker should not need to read old review files — the relevant feedback should be pre-digested into the task spec.

### Pattern: Pipeline infrastructure must be committed to git for worktree isolation to work (2026-03-28)
- **Context:** T041 (PostMortem Context Fix) discovered that the `pipeline/` directory was never committed to git. It existed only as untracked files in the main working directory.
- **Finding:** Workers run in isolated git worktrees, which only contain committed files. T041 had to copy all `pipeline/` files into its worktree as a workaround, adding diff noise and risking stale copies. Any directory that agents need to read or modify must be tracked by git — otherwise worktree-based isolation breaks the agent's ability to access it.
- **Action:** Before the next pipeline run, commit the `pipeline/` directory to git. As a general rule: if a file is referenced by any agent task, it must be committed. The Orchestrator should verify that all paths referenced in task descriptions exist in the git tree, not just the working directory.

### Pattern: Docker Compose has same cross-resource bug class as K8s manifests (2026-03-28)
- **Context:** T053 (Docker Compose) had structural issues similar to T054 (K8s Manifests) — both involved cross-resource reference validation (ConfigMap references across namespaces in K8s, service dependency and port wiring in Compose).
- **Finding:** T053 was approved on its attempt but the reviewer noted a pre-existing V8 migration conflict (duplicate `V8__market_data_timescaledb.sql` and `V8__warehouse_tables.sql`). Infrastructure-as-code tasks consistently have the highest cross-reference bug density because each individual file is valid but references between files can be broken. This pattern now spans Terraform (T003), K8s manifests (T054), Helm values (T003 duplicate YAML key), and Docker Compose (T053).
- **Action:** All infrastructure-as-code tasks should include a cross-reference validation step in the worker's pre-handoff checklist. For Docker Compose: verify all `depends_on` targets exist, all port mappings are unique, all env var service references match actual service names. For K8s: verify configMapRef/secretRef targets exist in the same namespace. For Terraform: verify all module output references are valid.

### Pattern: Final platform metrics — 689 tests, 69% coverage, 9 services, 4 phases (2026-03-28)
- **Context:** End-of-Phase-4 integration run (`run-20260328-150430`) after all tasks completed.
- **Finding:** Platform state: 9 Go services (matching-engine, clearing-engine, margin-engine, settlement-engine, auth-service, compliance-service, gateway, market-data-service, warehouse-service), 689 tests, 0 failures, 69.0% average coverage. Coverage range: 56.7% (gateway) to 79.9% (clearing-engine). All services build as independent zero-dep Go modules (except auth-service which uses golang.org/x/crypto for bcrypt). One stub directory remains (`src/clearing-service/` with Dockerfile only). Two frontend tasks (T048, T050) blocked on Node.js availability.
- **Action:** This serves as the baseline for Phase 5 planning. Priority gaps: gateway coverage (56.7%), frontend implementation (blocked on tooling), Kafka wire-protocol adapter (needed for production), migration numbering conflict (two V8 files). The clearing-service stub should be cleaned up.

### Pattern: Estimate calibration plateau — 2-3x overestimate is the steady state (2026-03-28)
- **Context:** Phase 4 timing data across 14 timed tasks. Phase 1-2 was 10-50x over. Phase 3 was 1.5-3x. Phase 4 is 1.4-5x with median ~2.5x.
- **Finding:** The estimate-to-actual ratio has converged but not improved further between Phase 3 and Phase 4. Specific Phase 4 ratios: T040 1.4x, T041 2.9x, T042 5x, T043 2.1x, T044 3.8x, T045 4.3x, T046 1.7x, T047 2x, T049 2.5x, T051 2.5x, T052 3.1x, T054 3.3x. The outlier is T042 (cleanup task, 5m est / 1m actual). Implementation tasks cluster around 2-3x; spec/cleanup tasks around 2.5-5x.
- **Action:** Accept 2-3x overestimate as the steady state for AI agent estimates. Do not reduce estimates further — the buffer absorbs variance from complex tasks (T040 at 1.4x was tight). Recommended estimate ranges: cleanup/trivial 3m, spec tasks 5m, coverage tasks 5m, service implementation 10m, infrastructure/rework 5m, integration tests 3m.

### Pattern: E2E tests with graceful-skip pattern enable CI without full infrastructure (2026-03-28)
- **Context:** T055 created 20 e2e integration tests that exercise the full trading flow through the gateway HTTP API. All tests skip gracefully when the gateway is not reachable.
- **Finding:** The `skipIfGatewayUnavailable` + per-step 503/502 skip pattern allows the same test suite to run in two modes: (1) unit-test mode where all 20 tests skip cleanly (0 pass, 0 fail, 20 skip), and (2) full-stack mode with Docker Compose where tests exercise real service interactions. This dual-mode approach means e2e tests can live in CI without blocking on infrastructure availability. The test suite also revealed an API contract gap: no `POST /v1/settlement/cycle` endpoint exists in the gateway despite being expected by the trading lifecycle.
- **Action:** All future e2e/integration tests should use the graceful-skip pattern. Tests should check service reachability at the start (`t.Skip` if unavailable) and handle 502/503 per-step (skip that step, not the whole test). This allows partial validation even when some backends are down. E2e test failures in full-stack mode should be treated as API contract bugs, not test bugs.

### Pattern: Template-duplicated Kafka packages achieve consistent coverage across all services (2026-03-28)
- **Context:** T052 created `internal/kafka/` packages for all 9 services by copying a shared template (event envelope, producer, consumer, config) and adding service-specific wiring files. Each service's kafka package achieved 82.4-82.9% coverage.
- **Finding:** The template-duplication approach (same pattern as Decimal type) works well for test coverage too — core test files (`event_test.go`, `producer_test.go`, `consumer_test.go`) are copied alongside the source, so every service gets baseline test coverage "for free." Service-specific `wiring_test.go` files add targeted coverage. The narrow coverage range (82.4-82.9%) across 9 services confirms the template is well-tested. The channel-based Producer/Consumer interfaces allow full testing without Kafka infrastructure.
- **Action:** When adding a new cross-cutting concern to all services (e.g., metrics, tracing), use the same template-duplication pattern: create a well-tested template package with both source and tests, then copy to each service with service-specific extensions. The test template ensures minimum coverage without per-service test effort.

### Pattern: Full pipeline completed 55 tasks across 5 phases — zero to production-ready in one day (2026-03-28)
- **Context:** The ACE Platform pipeline ran 55 tasks (T001-T055) across 5 phases: Phase 0 (foundation/infra), Phase 1-2 (core exchange engines), Phase 3 (supporting services), Phase 4 (integration/debt/specs), Phase 5 (e2e tests). Final state: 9 Go services, 689 unit tests, 20 e2e tests, 69% average coverage, Docker Compose stack, K8s manifests.
- **Finding:** Phase-over-phase improvements were measurable: rejection rate dropped from ~60% (Phase 0-1) to ~30% (Phase 4, frontend/tooling only) to 0% (Phase 5). Estimate accuracy improved from 10-50x overestimate (Phase 0-1) to 2-3x (Phase 3-5). Coverage improved from 43% (Phase 1-2) to 69% (final). The learning loop (PostMortem → CLAUDE.md → Planner) was the primary driver: each phase's patterns directly improved the next phase's planning.
- **Action:** This serves as the baseline for future feature pipelines on this codebase. Key metrics to track: rejection rate per phase (target: <10%), estimate accuracy (target: 2-3x), coverage trend (target: >60% average). If rejection rate spikes, check whether new tooling/runtime prerequisites were introduced without a corresponding setup task.

### Pattern: E2E tests expose API contract mismatches between spec and implementation (2026-03-28)
- **Context:** T055 found that `POST /v1/settlement/cycle` (expected by the trading lifecycle) doesn't exist in gateway routes. Also found that JSON field naming varies (`access_token` vs `AccessToken`) depending on whether Go struct tags are present.
- **Finding:** Spec tasks (T033, T047) define API contracts, implementation tasks (T034) implement routes, and e2e tests (T055) validate the full chain. Contract mismatches only surface at the e2e level — unit tests for the gateway pass because they test what's implemented, not what's specified. The JSON field casing issue (`AccessToken` vs `access_token`) indicates the auth service's `TokenPair` struct is missing `json:` tags, which was noted in the T040 review as a non-blocking suggestion.
- **Action:** After any spec+implementation phase, run e2e tests against the spec's expected endpoints (not just the implementation's actual endpoints) to catch missing routes. Non-blocking review suggestions that affect API contracts (like missing JSON tags) should be promoted to required fixes if they'll cause integration failures.

### Pattern: Coverage measurement methodology must be consistent across integration runs (2026-03-28)
- **Context:** Final integration runs showed wildly different average coverage for the same codebase: run-150430 reported 69.0%, run-151936 reported 45.7%, run-153022 reported 66.5%. All three ran the same 9 services with 0 test failures.
- **Finding:** The variance comes from how "average coverage" is computed. Statement-weighted averages (Go's default `go test -cover`) give higher numbers because high-coverage packages like `orderbook` (89.3%) have more statements. Package-count averages give lower numbers because many packages have 0% (cmd, server, types). Including 0% `cmd/` and `server/` packages that are intentionally untested drags the average down significantly.
- **Action:** Integration test agent should report TWO coverage numbers: (1) statement-weighted average (the number Go produces), and (2) business-logic-only average (excluding `cmd/`, `server/`, `config/`, `types/` packages). Use the business-logic average for trend tracking and floor enforcement. Always specify the methodology in the report header so numbers are comparable across runs.

### Pattern: Kafka template duplication inflates test count but adds real regression value (2026-03-28)
- **Context:** T052 added `internal/kafka/` packages to all 9 services using a shared template. Test count jumped from 689 to 828 (+139 tests, ~20% increase). Each service's kafka package has 82.4-82.9% coverage.
- **Finding:** The 139 new tests are ~85% template copies (event_test.go, producer_test.go, consumer_test.go) and ~15% service-specific wiring tests. While the template tests are redundant across services, they serve as regression guards — if a service's copy of the kafka package is modified independently (e.g., adding a custom retry policy), the template tests catch regressions in the shared patterns. This is the same trade-off as the Decimal type duplication.
- **Action:** When evaluating test count growth, distinguish between "new unique tests" (wiring_test.go per service) and "template regression tests" (copied event/producer/consumer tests). The template tests are valuable but should not be counted toward coverage improvement goals. Track "unique test logic" separately from "total test count."

### Pattern: Two permanent rejections (T048, T050) are tooling-blocked, not logic-blocked (2026-03-28)
- **Context:** Of 55 total tasks, only T048 (Trading Web UI) and T050 (Admin Dashboard) remain permanently rejected. Both specs (T047, T049) were APPROVED with zero required fixes. T056 (Frontend Integration Test) remains pending.
- **Finding:** These rejections are purely environmental — Node.js/npm is not available in the pipeline worker environment. The specs are production-quality and ready for implementation. Unlike Phase 1 rejections (missing Go scaffolding, which was fixed by adding project-init tasks), this tooling gap cannot be resolved by adding pipeline tasks — it requires environment configuration changes.
- **Action:** For future pipeline runs, maintain a "blocked" task status distinct from "rejected." Blocked tasks should not be re-queued (wasting compute) and should not count against the rejection rate metric. The planner should check `which node npm go python` (or equivalent) before scheduling tasks that require specific runtimes. T048/T050 should be attempted once Node.js is available in the environment.

### Pattern: Review quality improved throughout the pipeline — later reviews are more actionable (2026-03-28)
- **Context:** Compared early reviews (T003, T005 from Phase 0-1) with late reviews (T052, T053, T054, T055 from Phase 4-5). Early reviews had 3-5 required fixes per rejection. Late reviews had 0 required fixes on approvals but included 3-5 specific non-blocking suggestions each.
- **Finding:** The improvement has two causes: (1) workers improved (spec-first, calibrated estimates, injected feedback), producing fewer bugs, and (2) reviewers became more specific (citing exact line numbers, providing fix code, distinguishing required vs non-blocking). The T054 rework is the clearest example — the original rejection had 3 precise bugs with fix instructions, and the rework addressed all 3 exactly. The T055 review identified dead code (`readJSONArray` unused) and weak assertions (`TestMethodNotAllowed` accepting too many status codes) — quality feedback that wouldn't have surfaced in Phase 1 reviews.
- **Action:** Continue requiring reviews for all tasks. For approved tasks with non-blocking suggestions, the planner should evaluate which suggestions affect API contracts or cross-service integration and promote those to required fixes in a follow-up task. Suggestions about dead code, naming, or internal-only behavior can remain non-blocking.

### Pattern: Previously-rejected frontend tasks succeed immediately once tooling is available (2026-03-28)
- **Context:** T048 (Trading Web UI) and T050 (Admin Dashboard) were rejected in Phase 4 due to missing Node.js/npm. In Phase 5, Node.js v22.22.0 and npm 10.9.4 were available at `/usr/bin/`, and both tasks completed on first attempt with zero required fixes.
- **Finding:** T048 produced 84 tests with 60-100% business logic coverage; T050 produced 64 tests. Both built as <210KB gzipped bundles. The approved specs (T047, T049) were directly consumable — no rework needed on the spec side. The delay between spec approval and implementation (Phase 4 → Phase 5) caused zero drift. This validates that "blocked" is the correct status for tooling-unavailable tasks (not "rejected"), as the specs remained valid.
- **Action:** When re-attempting previously tooling-blocked tasks, reuse the original approved spec without modification. The implementation task description should note that the spec was already approved and reference it directly. No need to re-run the spec task. If >1 week has passed since spec approval, verify the spec still matches current codebase state before implementing.

### Pattern: Frontend SPA test coverage splits into two tiers — business logic vs components (2026-03-28)
- **Context:** T056 integration test measured web-ui at 30.8% overall / 60-100% business logic, admin-ui at 24.5% overall / 50-100% business logic. Both have 0% on React page/component files.
- **Finding:** Frontend test coverage naturally splits into: (1) business logic (services, reducers, types, validators) which is testable with pure unit tests and achieves 60-100%, and (2) React components/pages which require React Testing Library and achieve 0% without it. The overall coverage number (24-31%) is misleading because it conflates these two tiers. Business logic coverage is the meaningful metric for CI gates.
- **Action:** For frontend integration tests, report TWO coverage numbers: business-logic coverage (services/, contexts/, types/, hooks/) and component coverage (pages/, components/). Use business-logic coverage for floor enforcement (60%+). Component coverage should be tracked separately and improved incrementally as React Testing Library tests are added. Do not fail CI on low component coverage when business logic meets the floor.

### Pattern: Final pipeline totals — 857 tests, 11 buildable artifacts, 56 tasks across 6 phases (2026-03-28)
- **Context:** End-of-pipeline integration run (`run-20260328-154735`) after all 56 tasks (T001-T056) completed.
- **Finding:** Final state: 9 Go services (689 unit tests), 2 React/TypeScript SPAs (148 frontend tests), 20 e2e tests (skip without infrastructure), 66.5% average Go coverage. All 56 tasks completed — 0 permanently rejected. The two previously-blocked frontend tasks (T048, T050) were unblocked by Node.js availability. Total pipeline execution across all phases: ~3 hours of wall-clock agent time (sum of all task actuals). Estimate accuracy for Phase 5: 1.25-4.3x overestimate (median ~2.5x), consistent with Phase 3-4 steady state.
- **Action:** This is the definitive baseline for the ACE Platform. Key metrics: 857 tests (689 Go + 148 TS + 20 e2e), 11 build artifacts (9 Go binaries + 2 SPA bundles), 66.5% Go coverage average, 60-100% frontend business logic coverage. For the next feature pipeline, start from these numbers and track deltas.

### Pattern: Demo/documentation tasks have the best estimate calibration — 1.3-3.4x (2026-03-28)
- **Context:** Phase 5-6 tasks T055-T058: e2e tests, frontend integration test, demo runbook, demo smoke script. All 4 approved first pass, zero rejections.
- **Finding:** Estimate-to-actual ratios: T055 (10m/5m = 2.0x), T056 (5m/4m = 1.3x), T057 (15m/4.5m = 3.4x), T058 (10m/5.5m = 1.8x). Median 2.0x — the tightest calibration of any phase. T057 (runbook) was the biggest overestimate because document generation from existing code is faster than implementation. T056 (frontend integration) was the tightest because it's mostly running commands (npm install, npm test, npm build) rather than writing code.
- **Action:** For documentation tasks (runbooks, ADRs, specs that reference existing code), estimate 5m. For "run existing tests and report" tasks, estimate 3-5m. These task types are faster than implementation because they read code rather than write it.

### Pattern: E2E tests expose API contract gaps that unit tests and specs miss (2026-03-28)
- **Context:** T055 e2e tests found: (1) no `POST /v1/settlement/cycle` endpoint despite being expected by the trading lifecycle, (2) `AccessToken` vs `access_token` JSON field casing inconsistency, (3) warehouse-service and market-data-service have no gateway REST routes.
- **Finding:** These gaps were invisible to unit tests (which test what's implemented), specs (which describe intent), and reviews (which check implementation against spec). Only e2e tests — which exercise the full HTTP path from client to gateway to backend — surface cross-service integration gaps. The settlement endpoint gap means the demo runbook had to document a grpcurl workaround.
- **Action:** After completing a new service or API surface, run e2e tests against the gateway routes before marking the feature complete. Missing routes should be filed as bugs, not documented as workarounds. For the ACE platform specifically: add `POST /api/v1/settlement/cycle`, add warehouse REST routes, and add `json:` struct tags to auth-service `TokenPair`.

### Pattern: Mock-server-based bash test suites enable CI testing of shell scripts (2026-03-28)
- **Context:** T058 demo smoke script includes a 28-test bash test suite (`tests/demo/demo_test.sh`) that uses a Python HTTP mock server to validate all script paths without requiring a running platform.
- **Finding:** The mock server approach (Python `http.server` returning canned JSON) allows testing: CLI flags, unreachable gateway handling, healthy flow with all 6 steps, unhealthy gateway (503), and file properties. This is the same pattern as Go's `httptest.Server` but for bash scripts. The tests run in <5 seconds and need no infrastructure.
- **Action:** For any bash script that makes HTTP calls (deploy scripts, health checks, migration runners), include a mock-server-based test suite. The pattern is: Python mock on a random port → run script against it → assert exit codes and output. This catches regressions without requiring a running platform.

### Pattern: Phase 5-6 achieved 100% first-pass approval — zero rejections across all task types (2026-03-28)
- **Context:** Phase 5-6 ran 4 tasks: T055 (e2e Go tests), T056 (frontend integration test), T057 (demo runbook), T058 (demo smoke script). All 4 approved on first pass with zero required fixes. Reviewers provided only non-blocking suggestions (dead code, tighter assertions, eval→printf-v).
- **Finding:** The zero-rejection streak now extends across Phases 3-6 (excluding Phase 4's tooling-blocked frontend tasks and T054's cross-resource bugs). The contributing factors: (1) mature learned patterns guide workers, (2) specs are consumed directly without drift, (3) tasks are small and focused (5-15m estimates), (4) workers produce self-contained artifacts with tests. The only Phase 4 rejections (T048, T050, T054) were tooling/environment issues, not logic errors.
- **Action:** The pipeline has reached steady-state quality for well-scoped tasks. Future rejections should be treated as signals of either a new task type (unfamiliar tooling/pattern) or a missing prerequisite, not normal variance. If rejection rate rises above 10% in a phase, investigate root cause before continuing.

### Pattern: Final pipeline totals (updated) — 857 tests, 58 tasks, 6 phases, ~3h agent time (2026-03-28)
- **Context:** Complete pipeline run T001-T058 across 6 phases. Final integration run `run-20260328-155812`.
- **Finding:** 58 tasks total (55 original + T057/T058 demo tasks). 857 tests (689 Go + 148 frontend + 20 e2e). 11 buildable artifacts. Business-logic coverage ~65%. Rejection rate by phase: Phase 0-1 ~60%, Phase 2 ~0%, Phase 3 ~0%, Phase 4 ~30% (tooling), Phase 5-6 ~0%. Estimate accuracy converged from 10-50x (Phase 0-1) to 2x median (Phase 5-6). Total agent execution time: ~3h wall-clock (sum of task actuals). The learning loop (PostMortem → CLAUDE.md → Planner/Worker) was the primary driver of improvement.
- **Action:** This is the final baseline. For the next feature pipeline on this codebase, start from: 857 tests, 65% business-logic coverage, 2x estimate multiplier, 3-parallel worker limit, spec-first for business logic, zero-dep Go modules, template-duplication for cross-cutting concerns. Track deltas from these numbers.

### Pattern: T059 rejected with no handoff or review file — process gap persists for late-pipeline tasks (2026-03-28)
- **Context:** T059 (Demo Runner Web App) was rejected after ~8 minutes (15m estimate). No `handoff/T059.md` or `handoff/T059-review.md` exists. T060 (Demo Runner Integration Test) remains pending, blocked by T059.
- **Finding:** Despite the learned pattern "Reviewer must always produce a review file" (2026-03-27) being adopted for Phase 3-5 tasks (T054's rejection had a detailed review file), T059's rejection produced zero diagnostic artifacts. This means the PostMortem agent cannot root-cause the failure — we can only infer from timing (~8 min vs 15m estimate = ~53% of estimate) that it was not an instant-fail prerequisite issue (those complete in <10% of estimate) but likely a partial-progress failure. The rejection broke the T059→T060 dependency chain, leaving T060 permanently pending.
- **Action:** The Orchestrator must enforce the "no status transition to rejected without a review file" rule as a hard gate, not just a convention. If a worker exits without a handoff file AND the reviewer doesn't produce a review file, the Orchestrator should generate a minimal diagnostic file from available signals (exit timing, error output, branch state) before marking the task rejected. For T059 specifically: inspect the `feature/T059-demo-runner-web-app` branch for any partial work before re-queuing.

### Pattern: Dependent tasks should not remain permanently pending when blocker is rejected (2026-03-28)
- **Context:** T060 (Demo Runner Integration Test) has `status: pending` and `blockedBy: ["T059"]`. T059 is rejected. T060 will never run unless T059 is re-queued and completed.
- **Finding:** The Orchestrator does not cascade rejection status to downstream tasks. A rejected blocker leaves dependents in limbo — they're not rejected (so they don't trigger rework), not blocked (no explicit status), just silently pending forever. This is a state management gap: the task graph has an unreachable node.
- **Action:** When a task is rejected, the Orchestrator should mark all tasks that have it in their `blockedBy` list as `blocked` (new status) with a reason referencing the rejected blocker. This makes the dependency failure visible in the task graph. When the blocker is re-queued and completed, the blocked tasks should automatically transition back to `pending`.

### Pattern: Late-pipeline "nice-to-have" tasks have higher rejection risk — scope them conservatively (2026-03-28)
- **Context:** T059 (Demo Runner Web App, Phase 9) was the only rejection in the final pipeline batch. All core tasks (T055-T058) in Phases 5-6 were approved first pass. T059 was a "demo runner" web app — a convenience tool, not a core platform component.
- **Finding:** T059 was scheduled after 58 successful tasks, suggesting the pipeline was mature. Yet it was rejected while simpler tasks (T057 runbook, T058 smoke script) succeeded. Late-pipeline tasks that add new UI surfaces (T059 is a web app) face the same tooling/scaffolding risks as early-pipeline tasks if they introduce new patterns not yet validated in the pipeline. The demo runner was a React app but distinct from the existing web-ui/admin-ui pattern — it may have needed different scaffolding.
- **Action:** For "nice-to-have" tasks added late in the pipeline, prefer extending existing artifacts (add features to web-ui or admin-ui) over creating new standalone apps. If a new app is necessary, ensure the task description includes explicit scaffolding steps and references a successfully-completed sibling task (e.g., "follow the same pattern as T048 web-ui").

### Pattern: Integration run coverage methodology variance is now documented but still causes confusion (2026-03-28)
- **Context:** Six integration runs in the final day reported different average coverage for the same codebase: run-150430 (69.0%), run-151936 (45.7%), run-153022 (66.5%), run-154735 (~65% biz-logic), run-155812 (51.3% stmt / ~65% biz-logic), run-162057 (~66% biz-logic).
- **Finding:** The pattern "Coverage measurement methodology must be consistent" (2026-03-28) was learned earlier today, and the later runs (154735, 155812, 162057) correctly report both statement-weighted and business-logic-only numbers. However, the variance between runs that should report identical numbers (same code, same tests) suggests the integration test agent is not deterministic in which packages it includes. The business-logic average stabilized at ~65-66% across the last 3 runs, confirming that methodology is the variable, not actual coverage changes.
- **Action:** Standardize the integration test agent's coverage script to use a fixed package exclusion list (`cmd/`, `server/`, `config/`) and report both numbers with labels. The business-logic number (~66%) should be the canonical metric used in CLAUDE.md baselines and phase-over-phase comparisons. Stop reporting the all-packages-including-0% number as it's misleading.

### Pattern: Final pipeline totals (definitive) — 60 tasks, 857 tests, 1 permanent rejection (2026-03-28)
- **Context:** Complete pipeline run T001-T060. Final integration run `run-20260328-162057`. T059 rejected, T060 pending (blocked).
- **Finding:** 60 tasks total. 58 completed (done), 1 rejected (T059), 1 pending/blocked (T060). 857 tests (689 Go + 148 frontend + 20 e2e). 11 buildable artifacts (9 Go + 2 SPA). Business-logic coverage ~66%. Rejection rate: 1/60 = 1.7% overall. By phase: Phase 0-1 ~60% → Phase 2-3 0% → Phase 4 ~30% (tooling) → Phase 5-6 0% → Phase 9 50% (1/2). The single Phase 9 rejection (T059) breaks the zero-rejection streak from Phases 5-6 and demonstrates that new task types introduced late still carry risk.
- **Action:** This supersedes the previous "final pipeline totals" pattern. For the next pipeline run: 857 tests baseline, 66% business-logic coverage, 2-3x estimate multiplier, spec-first for business logic, zero-dep Go modules. The T059/T060 gap (demo runner) should be re-attempted with explicit scaffolding instructions referencing T048's successful web-ui pattern.

### Pattern: T059 rebuild succeeded after initial rejection — same pattern as T005/T048/T050 (2026-03-28)
- **Context:** T059 (Demo Runner Web App) was initially rejected (~8 min, no handoff/review files produced). It was then rebuilt and merged successfully, producing 50 tests and 60.2% overall coverage. T060 (integration test) confirmed all checks pass.
- **Finding:** The rebuild followed the same pattern that worked for T005 (auth service), T048 (web-ui), and T050 (admin-ui): identify the blocker, resolve it, and re-run. T059's initial rejection produced no diagnostic artifacts (handoff or review files), so the root cause had to be inferred from timing and branch state. The successful rebuild produced a complete React SPA with 50 tests and 86-100% business logic coverage — comparable quality to T048 (84 tests) and T050 (64 tests).
- **Action:** The "no artifacts on rejection" problem persists despite multiple learned patterns addressing it. For the next pipeline iteration, implement a hard gate in the Orchestrator: if a worker exits and neither `handoff/<task-id>.md` nor `handoff/<task-id>-review.md` exists, automatically generate a diagnostic file from git diff of the worktree branch, stderr output, and exit timing before marking the task as rejected.

### Pattern: Three frontend SPAs now share a validated pattern — use it as a template (2026-03-28)
- **Context:** Three React 18 + TypeScript + Vite SPAs exist: web-ui (84 tests, port 3000), admin-ui (64 tests, port 3001), demo-runner (50 tests, port 3002). All share the same stack: React Context + useReducer, CSS Modules, Vitest, zero heavy dependencies.
- **Finding:** All three SPAs were built using the same architecture pattern (Context + useReducer, CSS Modules, fetch API, Vitest) and all achieved 60%+ business logic coverage on first approved pass. The pattern is now validated across three independent implementations with different feature sets (trading, admin, demo). The consistent test structure (services → contexts → types → components) and coverage split (60-100% business logic, 0-25% components without RTL) is predictable and reliable.
- **Action:** For any future frontend task, reference all three SPAs as templates. New SPAs should scaffold from `src/web-ui/` or `src/demo-runner/` structure. Port allocation: next available is 3003. Include in task description: "Follow the React Context + useReducer + CSS Modules + Vitest pattern from src/web-ui/". Do NOT introduce Redux, Tailwind, or axios — the zero-heavy-dep pattern is validated.

### Pattern: Final pipeline totals (complete) — 60 tasks done, 887 tests, 12 artifacts (2026-03-28)
- **Context:** Complete pipeline T001-T060. Final integration run `run-20260328-171513`. All 60 tasks are `done` — zero rejected, zero pending.
- **Finding:** 60 tasks completed across 7+ phases. 887 tests (689 Go + 198 frontend + 20 e2e skipped). 12 buildable artifacts (9 Go binaries + 3 SPA bundles). Go business-logic coverage ~66%. Frontend business-logic coverage ~73%. Total agent execution time ~3.5h (sum of task actuals). The T059 initial rejection was resolved via rebuild, bringing the final rejection count to 0 permanent rejections. This supersedes the previous "definitive" totals pattern which reported 1 rejection and 857 tests.
- **Action:** This is the true final baseline. Key deltas from previous "definitive" pattern: +30 tests (198 vs 148 frontend, from demo-runner), +1 buildable artifact (demo-runner SPA), 0 permanent rejections (vs 1). For the next feature pipeline: 887 tests baseline, 12 artifacts, ~66% Go coverage, ~73% frontend business-logic coverage, 2-3x estimate multiplier.

### Pattern: Estimate accuracy for T059/T060 batch — 1.9-2.0x, tightest yet for implementation tasks (2026-03-28)
- **Context:** T059 (15m est / 7m54s actual = 1.9x), T060 (5m est / 2m27s actual = 2.0x). Both completed successfully.
- **Finding:** The T059/T060 estimates are the most accurate implementation-task estimates in the pipeline history. Previous phases: Phase 0-1 (10-50x), Phase 3 (1.5-3x), Phase 4 (1.4-5x), Phase 5-6 (1.3-3.4x). The ~2x ratio for T059 (a full React SPA) and T060 (an integration test) suggests the pipeline has reached optimal estimate calibration for tasks that follow established patterns.
- **Action:** Maintain current estimate ranges: React SPA implementation 15m, integration/validation tests 5m, spec tasks 5-10m, Go service implementation 10-15m. The 2x overestimate buffer is appropriate — it absorbs minor variance without significantly impacting scheduling.

### Pattern: Final integration run confirms platform stability — 6 consecutive green runs with identical test counts (2026-03-28)
- **Context:** Integration runs 153022, 154735, 155812, 162057, 171513, and 191810 all report 689 Go tests and 0 failures across the same 9 services. Frontend tests stabilized at 198 (84 web-ui + 64 admin-ui + 50 demo-runner) after demo-runner was added.
- **Finding:** After the demo-runner merge (T059), six consecutive integration runs produced identical pass/fail counts (689 Go, 198 frontend, 20 e2e skipped). No flaky tests were observed across any run. Go coverage methodology variance persists (all-packages avg ranges 44.7-53.3% across runs, but business-logic avg is stable at 65-66%). This confirms the test suite is deterministic and the codebase is stable.
- **Action:** For the next feature pipeline, any test count decrease from the 887 baseline (689+198) should be treated as a regression to investigate, not normal variance. Any new flaky test (passing in one run, failing in another) should be fixed immediately — the current suite has zero flakiness.

### Pattern: Frontend business-logic coverage exceeds Go coverage — different testing economics (2026-03-28)
- **Context:** Final run shows Go business-logic coverage at ~66% vs frontend business-logic coverage at ~84% (web-ui services 98.7%, demo-runner services 100%, admin-ui services 50.2%).
- **Finding:** Frontend TypeScript services (API clients, state reducers, validators, token managers) achieve near-100% coverage with pure unit tests because they have no I/O dependencies — they're pure functions operating on typed data. Go services achieve lower coverage because business logic is interleaved with concurrency primitives (mutexes, channels) and interface-based store interactions that require more test setup. The exception is admin-ui's `api.ts` at 50.2% — it has 29 endpoint functions but only 5 are tested.
- **Action:** For frontend coverage, the bottleneck is React component testing (requires RTL), not business logic. For Go coverage, the bottleneck is store/engine integration paths. Prioritize Go coverage improvements over frontend — the marginal value of going from 84% to 90% on frontend is lower than going from 66% to 75% on Go business logic.

### Pattern: Complete pipeline timing — 42 timed tasks, median 5.4 min, total ~3.5h agent time (2026-03-28)
- **Context:** All 42 tasks with start/finish timestamps analyzed. 18 tasks lack timing (T001, T002, T004 from Phase 0 had no timestamps; T005/T006/T007/T008/T015 from Phase 1 had pre-fix timestamps).
- **Finding:** Across all 42 timed tasks: fastest was T042 (cleanup, 1m02s), slowest was T040 (auth service rebuild, 10m38s). Median actual time: 5.4 min. Total actual agent execution: ~3.5 hours. By task type: spec tasks averaged 5.3 min (T033 6m49s, T035 5m38s, T037 6m33s, T047 5m19s, T049 3m58s, T051 3m46s), implementation tasks averaged 7.8 min (T034 9m03s, T036 6m40s, T038 7m25s, T052 8m21s, T048 5m30s, T050 3m30s, T059 7m54s), coverage tasks averaged 4.4 min (T043 6m48s, T044 3m57s, T045 3m33s), integration tests averaged 2.9 min (T039 2m36s, T046 3m10s, T056 3m56s, T060 2m27s), rework averaged 3.3 min (T031 3m07s, T054 2m56s).
- **Action:** Use these medians for future estimation: spec tasks 6m, implementation tasks 8m, coverage improvement 5m, integration tests 3m, rework/bugfix 4m, cleanup 2m, documentation 5m. These are actuals, not estimates — apply a 1.5x buffer for planning purposes.

### Pattern: First integration FAIL after 15+ green runs — e2e tests are the canary, not unit tests (2026-03-29)
- **Context:** Integration run `run-20260329-082942` reported FAIL — the first FAIL in the pipeline's history. All 828 Go unit tests and 198 frontend tests passed. The 6 failures were all in e2e tests (`tests/e2e/`).
- **Finding:** Unit tests are necessary but insufficient for catching integration bugs. The e2e suite caught 3 distinct bug classes that unit tests missed: (1) gateway auth middleware ordering (401 instead of 404 for unknown paths), (2) compliance API response format mismatch (array vs object), (3) cross-service event propagation failure (channel-based Kafka stubs don't bridge separate processes). These bugs existed through all previous "PASS" integration runs because those runs only executed unit tests — e2e tests were always skipped (no gateway running).
- **Action:** Integration test runs should distinguish between "unit test pass" and "e2e test pass" as separate verdicts. A run with 0 unit test failures but e2e failures should report `PARTIAL` (not `FAIL`), since the unit test signal is still valid. The Planner should schedule e2e test fixes as high-priority tasks since these represent real API contract bugs visible to clients.

### Pattern: Gateway auth middleware runs before routing — unknown paths return 401 instead of 404 (2026-03-29)
- **Context:** `TestGatewayHealth/unknown_endpoint_returns_404` failed because the gateway returned HTTP 401 with `{"error":{"code":"UNAUTHENTICATED","message":"Missing authorization token"}}` for a non-existent path.
- **Finding:** The middleware chain in `src/gateway/internal/middleware/` is: RequestID → Logging → BodyLimit → Auth → RateLimit → Router. Auth middleware runs before the router, so unauthenticated requests to non-existent paths get rejected at the auth layer before the router can return 404. Public paths are whitelisted by exact match or prefix, but unknown paths don't match any whitelist entry. This is a middleware ordering issue, not an auth bug.
- **Action:** Fix the gateway middleware chain so that routing (or at least path existence checking) occurs before auth enforcement. Two options: (1) move auth middleware after the router (requires per-route auth configuration), or (2) add a pre-routing check that returns 404 for paths not matching any registered route before auth runs. Option 1 is cleaner and aligns with how most API gateways work.

### Pattern: Compliance API response format differs from e2e test expectations — array vs object (2026-03-29)
- **Context:** `TestComplianceKYCFlow/submit_KYC_application` expected a JSON object (`map[string]interface{}`) but received a JSON array (`[]`). The error was `json: cannot unmarshal array into Go value of type map[string]interface{}`.
- **Finding:** The compliance service's KYC submission endpoint returns different response shapes than the e2e test expects. This is the same class of bug as the `AccessToken` vs `access_token` JSON field casing issue — API contracts defined in specs (T015, T033) don't perfectly match implementations. Unit tests pass because they test against mocks that match the test's expectations, not the real service's responses.
- **Action:** After fixing the response format, add a contract test layer that validates actual service responses against the OpenAPI spec (T033's `docs/T033_openapi.yaml`). This catches format mismatches without requiring a full running stack. For the compliance endpoint specifically, verify whether the service should return the created application object or a list, and update either the service or the e2e test to match.

### Pattern: Channel-based Kafka stubs work in-process but fail cross-service in e2e (2026-03-29)
- **Context:** 4 of the 6 e2e failures were in `TestFullTradingLifecycle` subtests (clearing positions, netting, margin calls, settlement cycles) that depend on events flowing from matching-engine → clearing-engine → margin-engine → settlement-engine.
- **Finding:** The channel-based `Producer`/`Consumer` from T052 works perfectly within a single Go process (all 82%+ coverage in unit tests). But in e2e mode, each service runs as a separate process, and Go channels don't cross process boundaries. Events published by matching-engine's `ChannelProducer` are never received by clearing-engine's `ChannelConsumer` because they're different channel instances in different processes. This is the fundamental limitation of the zero-dep approach for integration-layer code.
- **Action:** For e2e testing of cross-service flows, either: (1) run a real Kafka broker (Docker Compose) and use wire-protocol adapters behind the `Producer`/`Consumer` interfaces, (2) create a shared-memory or HTTP-based event bridge for testing (a test-only `Producer` that POSTs events to a test broker), or (3) accept that cross-service e2e tests require infrastructure and mark them as `skip` without infrastructure rather than `fail`. Option 1 is the production path; option 3 is the immediate fix.

### Pattern: Admin-ui is actively being developed between pipeline runs — watch for merge conflicts (2026-03-29)
- **Context:** Git status shows modified `src/admin-ui/src/services/api.ts` and new untracked files (`src/admin-ui/src/components/Sparkline.module.css`, `src/admin-ui/src/components/Sparkline.tsx`). These changes were not produced by any task in the pipeline.
- **Finding:** Manual development is happening in parallel with the AI pipeline. The admin-ui API service and new Sparkline charting component are being added outside the task system. This creates a risk of merge conflicts when the next pipeline run touches admin-ui files, and means the integration test's admin-ui test count (64) may change outside of pipeline tracking.
- **Action:** Before starting a new pipeline run, check `git status` for uncommitted changes. If manual development is in progress on files that pipeline tasks will touch, either commit the manual changes first or exclude those files from the pipeline's scope. The Planner should ask about in-progress manual work before generating the task graph.

### Pattern: E2e test count grew from 20 (all skip) to 55 (6 fail, 5 skip, 44 pass) — first real e2e execution (2026-03-29)
- **Context:** Previous integration runs showed 20 e2e tests all skipping (no gateway). Run `run-20260329-082942` shows 55 e2e tests with 44 passing, 6 failing, and 5 skipping. The gateway was running for this test.
- **Finding:** This is the first integration run where e2e tests actually executed against a live gateway. The 44 passing tests validate auth flows, order submission, market data endpoints, admin endpoints, health checks, and concurrent requests. The 6 failures are in cross-service integration and API contract areas. The 5 skips are for endpoints where backend services returned 502/503 (stub backend). The e2e suite is providing genuine value — it caught real bugs that 15+ previous "PASS" runs missed.
- **Action:** Prioritize fixing the 6 e2e failures as they represent real client-visible bugs. The 3 failure categories in priority order: (1) gateway auth middleware ordering (affects all unknown paths), (2) compliance API response format (affects KYC flow), (3) cross-service event propagation (requires Kafka infrastructure). Categories 1 and 2 are code fixes; category 3 is an infrastructure requirement.

### Pattern: T062 marked rejected despite APPROVED review — status/review verdict mismatch (2026-03-29)
- **Context:** T062 (Admin UI WebSocket Real-Time Hook) has `status: rejected` in tasks.json and timing shows it ran for ~4 minutes (10m estimate). However, `handoff/T062-review.md` exists with verdict APPROVED, 17 tests passing, and zero required fixes. No `handoff/T062.md` worker handoff file exists.
- **Finding:** The worker produced code that the reviewer approved, but the task was marked rejected — possibly because the worker failed to write a handoff file (the persistent "no handoff on rejection" pattern), or the merge step failed. The approved review without a worker handoff creates an ambiguous state: the code may exist on the worktree branch but was never merged. This is a new failure mode — previous rejected-with-no-review cases (T059 first attempt) were clearly failures; this one has contradictory signals.
- **Action:** When the Orchestrator sees a task marked `rejected` but with an APPROVED review file, it should investigate: (1) check if the worktree branch has the code, (2) check if the merge failed, (3) re-attempt the merge if the code is valid. The Planner should not re-queue T062 as a full rework — the code exists and was reviewed favorably. Instead, queue a "merge T062 branch" task.

### Pattern: Admin-ui feature tasks complete in 3-4 minutes — fastest task category (2026-03-29)
- **Context:** Four admin-ui feature tasks ran in this batch: T061 (CSV/PDF Export, 3m27s), T062 (WebSocket Hook, 4m04s), T063 (Keyboard Shortcuts, 3m29s), T064 (Market Phase Control, 3m45s). Estimates were 8-10 minutes.
- **Finding:** Admin-ui feature tasks that add a utility/hook + tests are the fastest task type in the pipeline, averaging 3.7 minutes. They're faster than Go service implementations (7.8m avg), specs (5.3m avg), and even coverage tasks (4.4m avg). The speed comes from: (1) established project patterns (CSS Modules, hooks, Context), (2) pure function extraction for testability (no RTL needed), (3) small scope (single feature per task). The estimate-to-actual ratio (2.3-2.9x) is consistent with steady state.
- **Action:** Estimate admin-ui feature tasks at 5 minutes (not 8-10m). These parallelize well — all 4 ran in the same batch with zero conflicts. For future UI feature batches, use 3-parallel with 5m estimates.

### Pattern: Pure function extraction enables 100% test coverage without React Testing Library (2026-03-29)
- **Context:** T061 exported `buildCSVString` as a pure function separate from DOM-dependent `exportToCSV`, achieving 100% coverage. T064 exported `normalizeInstruments` and `getPhaseAction` as pure functions, enabling 16 tests without rendering React components.
- **Finding:** The pattern of extracting business logic into pure, exported functions from React page modules is the primary testing strategy when RTL is unavailable. Every approved admin-ui task in this batch used this approach: T061 (CSV logic), T063 (shortcut config), T064 (instrument normalization, phase actions). The tests cover the logic that matters (data transformation, validation) while accepting 0% on React rendering paths.
- **Action:** For all frontend tasks, require that business logic (data transformation, validation, formatting) be implemented as exported pure functions in the page/component module or a co-located utility file. This enables unit testing without RTL. The task description should specify: "Export business logic as pure functions for testability."

### Pattern: E2e failures persist across integration runs — need dedicated fix tasks, not just documentation (2026-03-29)
- **Context:** The same 6 e2e failures appeared in both `run-20260329-082942` and `run-20260329-101906`. The failures are: (1) gateway auth middleware ordering (401 vs 404), (2) compliance API response format, (3-6) cross-service response format mismatches (array vs object for clearing/netting/margin/settlement endpoints).
- **Finding:** These bugs were first documented in the Phase 5 postmortem (2026-03-28) with patterns recommending priority fixes. Two integration runs later, the bugs remain because no fix tasks were created — only documentation patterns. The postmortem-to-planner loop works for *future* planning but does not fix *current* bugs. The Planner reads learned patterns before planning new features but does not automatically generate bug-fix tasks from e2e failures.
- **Action:** When integration tests report FAIL with specific e2e failures, the Orchestrator should automatically generate bug-fix tasks (not just document patterns). For the current 6 failures, the Planner should create: (1) "Fix gateway middleware ordering" task (move auth after routing), (2) "Fix API response format mismatches" task (6 endpoints returning array where tests expect object). These should be scheduled before any new feature work.

### Pattern: Admin-ui test count nearly doubled in one batch — 64 to 127 tests (2026-03-29)
- **Context:** Before this batch, admin-ui had 64 tests. After T061 (12 tests), T062 (17 tests — on branch, status ambiguous), T063 (35 tests), T064 (16 tests), admin-ui has 127 tests in the latest integration run.
- **Finding:** Four focused feature tasks added 63 new tests (+98% growth). Each task followed the pure-function testing pattern and contributed 12-35 tests. The high test-to-effort ratio (63 tests in ~15 minutes of total agent time) demonstrates that small, well-scoped UI tasks with clear testing patterns produce high test density. The admin-ui now has more tests (127) than any individual Go service except clearing-engine (198) and matching-engine (114).
- **Action:** Continue batching admin-ui features as 3-4 parallel tasks per iteration. The test growth rate is sustainable and provides regression coverage for the growing feature set. Track admin-ui test count as a quality metric alongside Go test counts.

### Pattern: Estimate calibration for admin-ui tasks — 2.5x overestimate, target 5m (2026-03-29)
- **Context:** T061 (8m/3.5m=2.3x), T062 (10m/4m=2.5x), T063 (10m/3.5m=2.9x), T064 (10m/3.7m=2.7x). Average ratio 2.6x.
- **Finding:** Admin-ui tasks are consistently estimated at 2.5x actual. The 8-10m estimates for these tasks were set before we had data on frontend feature task speed. The actual times (3.3-4.1m) cluster tightly, suggesting this task type has low variance — pure functions + CSS Modules + existing patterns = predictable effort.
- **Action:** Updated estimate recommendations: admin-ui feature tasks 5m (was 8-10m), admin-ui with new component pattern 8m, admin-ui requiring RTL or new dependency 10m. The 5m estimate gives a 1.4x buffer over the 3.7m average, which is tighter than the 2x steady-state target but appropriate for low-variance tasks.

### Pattern: Three consecutive FAIL integration runs from same 6 e2e bugs — postmortem loop documents but doesn't fix (2026-03-29)
- **Context:** Integration runs `run-20260329-082942`, `run-20260329-101906`, and `run-20260329-105018` all report FAIL with identical 6 e2e failures: (1) gateway auth middleware ordering (401 vs 404), (2) compliance API response format (array vs object), (3-6) clearing/netting/margin/settlement endpoints return array where tests expect object.
- **Finding:** These bugs were first documented in the 2026-03-28 postmortem with patterns recommending fixes. Three integration runs and two postmortem cycles later, the bugs persist because the PostMortem→CLAUDE.md→Planner loop only generates documentation patterns, not fix tasks. The Planner reads learned patterns before planning *new features* but does not scan for unfixed bugs from prior runs. This is the most significant process gap in the pipeline: the learning loop improves future planning but does not remediate current defects.
- **Action:** Add a "Bug Backlog" section to `tasks.json` (or a separate `bugs.json`) that the PostMortem agent populates with concrete fix tasks when integration tests FAIL. The Planner must check this backlog before scheduling new feature work — bug fixes take priority. For the current 6 failures: (1) move auth middleware after routing in gateway, (2) fix compliance KYC endpoint to return object not array, (3) fix e2e tests to handle array responses from clearing/netting/margin/settlement endpoints (these endpoints correctly return arrays — the tests are wrong).

### Pattern: Data-only tasks (adding steps to existing data files) are sub-3-minute — fastest task type (2026-03-29)
- **Context:** T065 (Demo Runner Admin Sections) added 19 API steps to existing `sections.ts` data file. Completed in 2m08s against 8m estimate (3.8x overestimate). T066 (integration test) completed in 4m50s against 5m estimate (1.0x — essentially perfect).
- **Finding:** Tasks that add data to existing well-structured files (step definitions, test cases, configuration entries) are faster than any other task type because: (1) the file structure is established, (2) no new patterns to design, (3) the data format is copy-paste-and-modify from existing entries. T065's 2m08s is the fastest implementation task in the entire 66-task pipeline history. T066's near-perfect estimate accuracy (1.0x) suggests integration test tasks that follow established patterns are highly predictable.
- **Action:** Estimate data-addition tasks at 3m. Estimate integration tests that follow the T066 pattern (run existing tests + add structural validation) at 5m. These are the two most predictable task types in the pipeline.

### Pattern: Frontend test count nearly tripled in one day — 198→313 across two feature batches (2026-03-29)
- **Context:** Before admin-ui features: 198 frontend tests (84 web-ui + 64 admin-ui + 50 demo-runner). After T061-T066: 313 frontend tests (84 web-ui + 144 admin-ui + 85 demo-runner). Admin-ui grew from 64→144 (+125%), demo-runner from 50→85 (+70%).
- **Finding:** Focused feature batches (3-4 tasks per batch) with mandatory tests per task produce rapid test growth. Each admin-ui feature task added 12-35 tests; each demo-runner task added 5-30 tests. The growth is sustainable because each task follows established test patterns (pure function extraction, Vitest, same test structure). The 144 admin-ui tests now exceed most individual Go service test counts (only clearing-engine at 136 and matching-engine at 114 are comparable).
- **Action:** Track frontend test count as a primary quality metric alongside Go test count. Current baseline: 313 frontend tests (84 + 144 + 85). With Go at 689 and e2e at 55, total platform test count is 1057. For the next feature batch, any decrease from 313 should be investigated as a regression.

### Pattern: T062 is a second instance of rejected-with-approved-review — Orchestrator merge gate is broken (2026-03-29)
- **Context:** T062 (WebSocket Hook) has `status: rejected` in tasks.json but `handoff/T062-review.md` says APPROVED with 17 tests and zero required fixes. No `handoff/T062.md` worker handoff exists. This is the same failure mode as documented in the 2026-03-28 postmortem for T059.
- **Finding:** Two tasks now share this failure mode: T059 (initially rejected, rebuilt successfully) and T062 (rejected, not yet rebuilt). The pattern is: worker produces code → reviewer approves → merge step fails or is skipped → task marked rejected despite approved code. The Orchestrator does not distinguish between "reviewer rejected" and "merge failed after approval." Without a worker handoff file, the failure point cannot be diagnosed. T062's code likely exists on its worktree branch but was never merged.
- **Action:** The Orchestrator must implement a three-state rejection: `rejected_by_review` (reviewer said no), `rejected_merge_failed` (reviewer approved but merge failed), and `rejected_no_output` (worker produced nothing). For T062 specifically: check the `feature/T062-*` branch for the approved code and merge it rather than re-running the task from scratch.

### Pattern: Playwright E2E is a distinct task type — slower but tightest estimate accuracy (2026-03-29)
- **Context:** T067 (Playwright Admin Dashboard Verification) took 13m11s against a 15m estimate (1.14x ratio). It produced 13 serial E2E tests verifying admin dashboard pages after demo runner setup. This is the first Playwright task in the pipeline.
- **Finding:** Playwright tasks are slower than unit-test-only tasks (13m vs 3-5m for admin-ui features) because they: (1) require serial execution (data setup in Test 1, verification in Tests 2-13), (2) need real browser automation with page navigation and API interception, (3) must re-login per test due to SPA in-memory auth tokens. However, the estimate accuracy (1.14x) is the tightest of any task type — Playwright execution time is predictable because it's dominated by browser startup, navigation, and network latency rather than code generation variance. The SPA navigation constraint (sidebar clicks instead of `page.goto()` to preserve auth state) is a real architectural limitation that affects test design.
- **Action:** Estimate Playwright E2E tasks at 15m (the current estimate was accurate). These tasks should NOT be parallelized with other tasks that modify the same frontend code. For admin-ui Playwright tests, always use sidebar navigation (not direct URL navigation) to preserve in-memory auth state. Consider adding `storageState` support to admin-ui to allow login state reuse across tests.

### Pattern: Four consecutive FAIL integration runs from same 6 e2e bugs — need automated bug-to-task pipeline (2026-03-29)
- **Context:** Integration runs `run-20260329-082942`, `run-20260329-101906`, `run-20260329-105018`, and `run-20260329-110902` all report FAIL with identical 6 e2e failures. The bugs were first documented on 2026-03-28. Four postmortem cycles have documented them. Zero fix tasks have been created.
- **Finding:** The PostMortem → CLAUDE.md → Planner learning loop is effective for preventing future mistakes but completely ineffective for remediating current defects. The gap is structural: the PostMortem agent writes patterns, but patterns are read by the Planner only when planning *new features*. No agent reads patterns to generate *bug fix tasks*. The 6 e2e bugs (gateway middleware ordering, compliance API format, 4 cross-service response format mismatches) have been documented in 3 separate patterns across 2 postmortem cycles, yet the code remains unchanged.
- **Action:** Add a mandatory step between PostMortem and the next pipeline run: the Orchestrator must scan integration test results for FAIL verdicts and create bug-fix tasks in `tasks.json` before the Planner generates new feature tasks. Bug-fix tasks should take priority over feature work. For the current 6 bugs: (1) fix gateway middleware chain to route before auth, (2) fix e2e tests to accept array responses from list endpoints (clearing/netting/margin/settlement correctly return arrays), (3) investigate compliance KYC endpoint response format.

### Pattern: Playwright tests are the third testing tier — they catch UI integration bugs that unit and e2e API tests miss (2026-03-29)
- **Context:** T067 Playwright tests verify that demo runner actions produce visible results in the admin dashboard (13 tests: participants listed, order book populated, system health cards showing 9+ services, etc.). These tests caught that SPA navigation requires sidebar clicks (not `page.goto()`) to preserve React in-memory auth tokens.
- **Finding:** The platform now has three testing tiers: (1) unit tests (689 Go + 313 frontend = 1002, test business logic), (2) e2e API tests (58 subtests, test HTTP contracts through the gateway), (3) Playwright browser tests (13, test that UI renders expected data after API actions). Each tier catches different bug classes: unit tests catch logic errors, e2e catches API contract mismatches, Playwright catches UI rendering and navigation issues. The SPA in-memory auth constraint was only discoverable at the Playwright tier.
- **Action:** For future UI features, include a Playwright verification test task alongside the implementation task. The Playwright test should verify the feature is visible in the browser after the API action, not just that the API returns the right data. Use `test.describe.serial` for flows that depend on data setup. Use `expect.soft()` for content checks on pages that may show empty states.

### Pattern: Platform test count milestone — 1199 total tests across 3 tiers (2026-03-29)
- **Context:** After T061-T067 batch: 828 Go unit tests (689 service + 139 kafka template), 313 frontend tests (84 web-ui + 144 admin-ui + 85 demo-runner), 58 e2e subtests (47 pass, 6 fail, 5 skip), 13 Playwright tests. Total: 1199+ tests across 12 buildable services.
- **Finding:** Test count grew from 887 (end of T060) to 1199+ (end of T067), a 35% increase driven primarily by admin-ui features (+80 tests) and demo-runner admin sections (+35 tests). The Playwright tier added 13 tests. The Go unit test count stabilized at 828 (no new Go code in this batch). Frontend tests now exceed Go unit tests for the first time if kafka template tests are excluded (313 frontend vs 689 Go non-kafka).
- **Action:** Updated test baselines: 828 Go, 313 frontend, 58 e2e API, 13 Playwright. Any decrease from these numbers should be investigated. For the next batch, prioritize fixing the 6 e2e failures over adding new tests — the e2e FAIL status undermines confidence in the integration signal.

### Pattern: T062 WebSocket hook code exists on branch but remains unmerged — third instance of merge-gate failure (2026-03-29)
- **Context:** T062 has `status: rejected` with an APPROVED review (17 tests, zero required fixes). T059 had the same failure mode and was resolved by rebuilding. T062 has not been resolved. The admin-ui integration run (T066) shows 144 tests — the T062 tests (17) are NOT included, confirming the code was never merged.
- **Finding:** This is the third instance of the "rejected-with-approved-review" pattern (T059 initial, T062, and implicitly T005 from Phase 1). The Orchestrator's merge step is a silent failure point: when it fails, no diagnostic artifact is produced, the task is marked `rejected` indistinguishably from a reviewer rejection, and the approved code is stranded on the worktree branch. The 17 WebSocket hook tests from T062 would bring admin-ui to 161 tests if merged.
- **Action:** Before the next pipeline run: (1) check if `feature/T062-*` branch exists with the approved code, (2) if so, merge it manually rather than re-running the task. The Orchestrator must log merge failures separately from review rejections. A new status `merge_failed` should be added to `tasks.json` to distinguish the two failure modes. This is now a P1 pipeline infrastructure bug — it has caused wasted compute across 3 tasks.

### Pattern: Multi-tenancy directive (2026-04-23)
- **Context:** GarudaX_Strategy_Directive.md established multi-tenant pivot. Product is GarudaX (platform), tenants are venues (ace-commodities, mse-equities).
- **Finding:** Every task from directive-2026-04 forward assumes multi-tenancy. Tenant ID is a first-class argument to every domain operation. No service accepts traffic with an unresolved tenant.
- **Action:** Reviewer Agent rejects any task without tenant context in its API surface. ace-commodities retrofit must complete before mse-equities build. Auth is platform-level; all other schemas are per-tenant.

### Pattern: Shared decimal module reverses the "accept duplication" pattern once copies diverge into bugs (2026-06-26)
- **Context:** R0 financial-correctness remediation (R001–R006). The six per-engine `internal/types/decimal.go` copies (matching, clearing, margin, settlement, market-data, warehouse) — and securities-service's `float64` money math — had diverged into three P0 correctness bugs: silent int64 overflow on large notionals, truncation bias in `MulDecimal`/`DivInt`, and zero-on-divide-by-zero. The earlier learned pattern "accept Decimal type duplication" (2026-03-27) was correct *until* the copies diverged.
- **Finding:** R001/R002 specified and built one shared zero-dep module `github.com/garudax-platform/decimal` (at `src/shared/pkg/types/decimal/`, 92%+ coverage) whose API is a strict **superset** of all six copies, so migration was an import swap, not a rewrite. The fix surface was tiny: switching truncation→banker's (half-even) rounding broke exactly ONE test across all six engines (`clearing-engine` novation truncation assertion), confirming the buggy behavior was rarely depended on. The shared type keeps the `int64`-scaled-by-10000 representation and `Raw()`/`DecimalFromRaw()`, so no DB/proto migration was needed.
- **Action:** The duplication-vs-shared threshold is **correctness divergence**, not count. When duplicated copies of a primitive start carrying *different* bug profiles (especially in money/financial code), consolidate immediately. The migration recipe that worked: (1) spec the superset API, (2) build the shared module, (3) replace each local copy with a 23-line `type Decimal = decimal.Decimal` alias shim + re-exported constructors so all call sites compile unchanged, (4) fix only the tests that asserted the old buggy behavior.

### Pattern: Filesystem `replace` directive is the validated way to share a Go module in this repo (2026-06-26)
- **Context:** R002/R003/R004/R005 all wired the shared decimal module via `require github.com/garudax-platform/decimal v0.0.0` + `replace github.com/garudax-platform/decimal => ../shared/pkg/types/decimal` (relative path) in each consumer `go.mod`.
- **Finding:** The filesystem `replace` keeps the zero-dependency, no-`go.sum`, no-network build property that all services rely on — it works for the six engines, securities-service, AND the separate `tests/reconciliation/` test module (which imports the *real* shared type to reconcile cross-service cash legs). The shared module declares `go 1.22` independently of the heavier `shared` module (which needs go1.24), so it builds with the host toolchain. Note: the engines themselves declare `go 1.25.0`, which forces `GOTOOLCHAIN=auto` to fetch go1.25 over the network at build time — a deviation from the zero-network ideal that should be reconciled.
- **Action:** For any new shared Go primitive, place it under `src/shared/pkg/...` as its own `go 1.22` zero-dep module and consume it via relative `replace`. Cross-module tests that need to exercise the real shared code (not a copy) live in a separate `tests/<name>/` module with the same `replace`. Avoid raising the shared module's `go` directive above the host toolchain version.

### Pattern: Cross-cutting refactor/audit tasks estimate at ~1x, not the 2-3x greenfield steady state (2026-06-26)
- **Context:** R-series timing (the timestamped ones): R003 migrate-6-engines 20m est / 20m actual = 1.0x; R004 securities float removal (~40 files) 20m est / 23m actual = **1.15x (over)**; R005 reconciliation tests 10m / ~9m = 0.9x; R006 money-path audit 10m / ~8m = 0.8x. R001/R002 timestamps are placeholders (round 30m).
- **Finding:** Greenfield implementation tasks overestimate at 2-3x (see all prior phases), but tasks that touch many existing files — migrations, audits, reconciliation across services — cluster tightly around 1x and R004 actually exceeded its estimate. The 2-3x buffer absorbs greenfield variance but evaporates when the work is "find and change every call site": there's no design slack, just mechanical breadth. R004 was the single largest file-count change (matching/auction/settlement engines + ~25 handler DTOs + 15 test files) and was the only R-task to run over.
- **Action:** Estimate cross-cutting tasks by file/call-site breadth, not by feature complexity. For migration/audit tasks touching >10 files, use 1.2-1.5x the greenfield estimate (effectively 1x of actual) and do not rely on the 2-3x cushion. Spec/test-only R-tasks (R001, R005) keep the tighter sub-1x profile.

### Pattern: Audit tasks are the highest-leverage way to surface debt beyond scope — require concrete follow-up tasks (2026-06-26)
- **Context:** R006 was a static money-path audit across all 21 `src/` Go modules. It shipped exactly one code fix (margin-engine haircut truncation: `int64(haircut*10000)` → half-even `decimal.NewFromFloat`) but its real value was classification.
- **Finding:** The audit confirmed the R003/R004 scope was clean AND surfaced four out-of-scope modules still carrying `float64` money math: corporate-actions (entire dividend/rights/split engine, not yet wired to any binary), compliance MCSD/FRC (`CashEntitlement`/`SettlementAmount`/`PenaltyAmount`), gateway fees + pre-trade risk (user-visible — fees charged, orders blocked), and fix-gateway price tags. It produced five concretely-scoped follow-up tasks (R007–R011) with file:line citations rather than vague "improve coverage" notes. The one fix it did make was a genuine defect: haircut 0.83 truncated to 8299 raw instead of 8300, mis-stating every pledged-receipt collateral credit.
- **Action:** Schedule a dedicated audit task at the end of any correctness-remediation phase. The audit must (1) cite exact file:line for every residual, (2) classify in-scope-clean vs out-of-scope-debt, and (3) emit named, individually-scoped follow-up tasks — never just prose. The audit's own diff should stay minimal (it found and fixed one truncation while documenting dozens of deferred sites).

### Pattern: The "documents but doesn't fix" gap is finally closed — bug backlog tasks now exist in tasks.json (2026-06-26)
- **Context:** Across March, four+ postmortem cycles documented the same 6 e2e failures (gateway auth-before-routing 401-vs-404, compliance array-vs-object, cross-service event propagation) as patterns that were never converted to fix tasks — the most-repeated process gap in this file.
- **Finding:** tasks.json now carries a real deferred backlog: R007 (Kafka fail-fast), R008/R009 (engine concurrency bugs + race tests), R010/R011/R012 (shared tenant module + real gateway tenant enforcement + tests), R013 (re-scope tenancy docs), R014 (re-enable CI build+test gate), R015 (fresh full-stack run), R016 (refresh metrics), R017 (repo hygiene). These are status `deferred` — distinct from `pending`/`rejected` — which prevents wasted re-queue compute while keeping them visible. This is the structural fix the prior patterns asked for: PostMortem/Planner now writes named fix tasks, not just documentation.
- **Action:** Keep the `deferred` backlog as the canonical home for known defects. The Planner must drain P1 backlog items (R011 gateway tenant enforcement, R014 CI gate) before new feature work. Treat any FAIL integration run as an obligation to *append a deferred fix task*, not merely a pattern.

### Pattern: Multi-tenancy is asserted in docs but bypassed in code — gateway tenant middleware is a real P1 defect (2026-06-26)
- **Context:** Integration run `run-20260626-012134` is FAIL. Builds are 19/19 green (now 15 Go modules — added corporate-actions, fix-gateway, platform-control, platform-service, securities-service, shared). The real defect: `src/gateway/internal/middleware/tenant.go` `tenantBypassPrefixes` whitelists nearly every tenant-scoped path (`/api/v1/orders`, `/api/v1/margin`, `/api/v1/participants`, `/api/v1/tickets`, clearing/settlement/warehouse/market-data), so `TenantMiddleware` skips header validation and calls handlers with no tenant in context — returning 200 where 401/403 is required. Three `TestTenantMiddleware_*` tests fail.
- **Finding:** This directly violates the 2026-04-23 platform invariant ("Tenant ID is never optional / no service accepts traffic with an unresolved tenant"). The bypass list looks like a routing allow-list that grew to swallow business paths. The directive was enforced at the *reviewer/doc* level but never at the *code* level — the gateway is the one place tenant resolution must be mandatory, and it's exactly where it was defeated. R011/R012 (deferred) target this. The remaining 26 frontend failures are test-vs-impl drift (admin-ui `market-phase` normalization + `bot-integration` path; demo-runner `sections`/`t066` fixture counts changed by merged data edits) — drift, not defects.
- **Action:** Narrow `tenantBypassPrefixes` to genuinely platform-level paths only (`/platform/`, `/api/v1/platform/`, `/api/v1/auth/`, health) and require `X-GarudaX-Tenant` on all tenant-scoped routes (R011). Prioritize this above feature work. For the frontend drift, decide source-of-truth (the `tsbuildinfo` churn and merged data changes suggest code is newer than tests) and update the lagging side (R013-class). A passing unit/build suite with a FAIL e2e/middleware tenant check should be reported FAIL — the tenant invariant is security-relevant, not cosmetic.

### Pattern: The float→decimal migration is now a templated recipe — single-module ports approve first-pass and run fast (2026-06-26)
- **Context:** R018–R021 drained the four R006 FAIL residuals: corporate-actions (R018), compliance MCSD/FRC (R019), gateway fees+pre-trade-risk (R020), fix-gateway price tags (R021). All four were APPROVED on the first pass with zero required fixes.
- **Finding:** Every one of the four handoffs cites the *identical* recipe, now battle-tested: (1) wire the shared module via relative `replace github.com/garudax-platform/decimal => ../shared/pkg/types/decimal` (no go.sum, no network); (2) **domain/computation = `decimal.Decimal`, transport JSON DTOs stay `float64`** and convert at the handler boundary (`Decimal` marshals as a bare JSON number, so the wire contract and downstream handlers are untouched); (3) `decFromFloat`/`.Float64()` shims at the `database/sql` boundary because `Decimal` implements no `sql.Scanner`/`driver.Valuer`; (4) **`Try*` (error, fail-closed) at untrusted client/JSON boundaries, convenience panic-on-overflow for internal bounded inputs**; (5) **multiply-before-divide / divide-by-basis-LAST** for fractional rates (bps, coupon rate, haircut, TERP) so a sub-tick rate isn't lost to the 4dp scale; (6) keep genuine ratios/percentages/share-counts as `float64` (e.g. `RatioOrAmount` split branch, `PriceBandPct`, `Volume30D`, `TickSize`). Because the recipe was templated by R003/R004, the per-module ports were *faster and cleaner* than the first-time R004 (which ran over).
- **Action:** For any remaining money-float module (R023 gateway reporting generator), hand the worker this six-step recipe directly in the task description rather than the spec. Reviewer should verify exactly: `==`/`!=` safety (Decimal is a comparable single-int64 struct — one representation per value, zero-value `Decimal{}` equals `decLit(0)`), the multiply-before-divide ordering on every rate path, and that the `decFromFloat` shim doesn't silently swallow `NewFromFloat`'s out-of-range error on a client-supplied DTO field (return 400 instead).

### Pattern: Single-module migrations parallelize like worktrees — schedule them as a batch (2026-06-26)
- **Context:** R018, R019, R020 all started at the same timestamp (`2026-06-26T02:28:23Z`) and ran concurrently; R021/R022 followed sequentially. Zero merge conflicts.
- **Finding:** Because each money-float residual lives in a *separate Go module* (corporate-actions, compliance-service, gateway, fix-gateway), the migrations touch disjoint file sets — the same disjoint-write property that makes worktree parallelism safe. Three parallel ports completed with no contention, no shared `tasks.json` races, no cross-module compile breakage. This is the module-isolation analog of the validated 3-parallel worker limit.
- **Action:** When a backlog item decomposes into per-module changes that touch no shared files (migrations, audits, cross-cutting concern rollouts), batch them 3-parallel. The bottleneck is the sequential review-then-merge, not the ports. Reserve sequential scheduling only for tasks that share a module (e.g. two gateway tasks — R020 fees/risk and R009/R011 middleware — should not run concurrently).

### Pattern: Re-audit after a remediation batch catches the *previous* audit's mis-classifications (2026-06-26)
- **Context:** R022 re-ran the R006 money-path audit across the four R018–R021-remediated modules plus the six engines + securities-service. It confirmed all four R006 residuals CLOSED, but surfaced one residual R006 had **mis-classified as "gateway reporting = non-money"**: `gateway/internal/reporting/generator.go` computes settlement-statement P&L, fee totals, net amount, OHLC, and large-trader positions in `float64` with a float `roundTo4`.
- **Finding:** The newly-found residual is *latent* — the `Generate*` functions have no live caller (GET handlers serve pre-stored reports; only tests invoke the generator), exactly like corporate-actions was before R018. A first-pass audit that buckets `reporting/`/`observability/`/`generator` packages as "non-money telemetry" will miss real money aggregation hiding in report builders. R022 emitted a concretely-scoped R023 follow-up (generator + `reporting/store.go` columns + DTOs) and kept its own diff at zero (audit-only) per the minimal-diff audit pattern. The R022 reviewer additionally caught a money path R022 itself omitted (securities `handlers_bond.go:180` `roundTo2DP(accrued.Float64())` display rounding) — even a re-audit's "clean" list needs scrutiny.
- **Action:** Always close a remediation phase with a *re-audit*, not just the migrations. The re-audit must re-scrutinize the prior audit's "non-money" buckets — especially `reporting/`, `generator`, and aggregation code where `(price - cost) * qty` / sums hide. Run the `roundTo2|roundTo4|round2` float-helper sweep platform-wide: a surviving money-rounding float helper is a reliable tell for a missed residual. Latent (no-live-caller) money floats are still tracked as residuals and migrated *before* the code is wired to a binary.

### Pattern: Cross-cutting refactor estimates over-shoot again once the recipe is templated — 1.2-2.25x (2026-06-26)
- **Context:** R018–R022 timing: R018 20m/~12m=1.7x, R019 18m/~8m=2.25x, R020 18m/~15m=1.2x, R021 15m/~7m=2.1x, R022 10m/~5m=2.0x. Contrast R003/R004 (the *first* migrations) which clustered at ~1.0-1.15x and R004 actually ran over.
- **Finding:** The earlier pattern "cross-cutting refactor/audit tasks estimate at ~1x" held for the *first-time* migration where the recipe was being discovered (R004, largest file-count, ran over). Once R002/R003/R004 templated the recipe, the *subsequent* single-module ports (R018–R021) reverted to the ~2x greenfield over-estimate profile, because the mechanical breadth per task dropped (one module each) and there was no design slack to consume — the work became "apply the known recipe to a small surface." R020 (1.2x) was the tightest because gateway fees+risk had the largest surface of the four (DB store + handlers + two packages).
- **Action:** Distinguish *first-of-kind* cross-cutting tasks (estimate at ~1x of greenfield, no buffer — there's discovery cost and breadth with no template) from *templated-repeat* cross-cutting tasks (estimate at the 2x greenfield steady state — the recipe absorbs the work). For R023 (templated-repeat, moderate surface) estimate ~10m. Scale by surface: single-package port ~5-8m, multi-package + DB store port ~12-15m.

### Pattern: A real P1 security defect outranks a clean P2 backlog — prioritization inverted in this batch (2026-06-26)
- **Context:** Two integration runs (`run-20260626-012134`, `run-20260626-022823`) both FAIL on the *same* gateway tenant-bypass defect (R011/R012, the multi-tenancy invariant violation). Yet the batch that actually ran was R018–R022 (money-float cleanup), while R011/R012 stayed `deferred`. The prior pattern's own Action said "Planner must drain P1 backlog items (R011 gateway tenant enforcement, R014 CI gate) before new feature work."
- **Finding:** The `deferred` backlog correctly *exists* (the "documents but doesn't fix" gap is structurally closed), but priority ordering within it was not honored: a security-relevant correctness defect (handlers invoked with no tenant context — 200 where 401/403 required) was left open across two FAIL runs while lower-severity money-float polish proceeded. The money-float work (R018–R022) is genuine debt, but none of it was a *live* defect — corporate-actions/compliance/fees were correctness-improving, not breach-fixing. The tenant bypass is exploitable today. Severity, not batch convenience, must drive ordering.
- **Action:** Tag backlog items with severity (P1 security/correctness-live, P2 correctness-latent, P3 hygiene) and have the Planner drain P1 strictly before P2 regardless of which tasks parallelize most conveniently. R011 (narrow `tenantBypassPrefixes` to `/platform/`, `/api/v1/platform/`, `/api/v1/auth/`, health; require `X-GarudaX-Tenant` on all tenant-scoped routes) + R012 (tests) must run before R023 and any further feature work. A green build with a FAIL tenant-middleware test is still a FAIL — never let build-green mask a P1.

### Pattern: Snapshot-handler-under-lock, invoke-after-unlock is the validated callback idiom for these engines (2026-06-26)
- **Context:** R008 fixed three concurrency defects across clearing/margin/settlement engines: user callbacks (`tradeHandler`, `marginHandler`, `cycleHandler`) invoked while holding the engine mutex (deadlock if the callback re-enters the engine), an unguarded concurrent map read in settlement, and unsynchronized `Set*Handler` writes.
- **Finding:** The winning fix — over `atomic.Value` — is to guard each handler field with the struct's *existing* mutex, snapshot the handler (and a value copy of the data it receives) inside the critical section via an extracted `*Locked` helper (`clearTradeLocked`, `calculateMarginLocked`, `runSettlementCycleLocked`), then call the handler after `Unlock()`. This solves the deadlock and the data-race together with one idiom, no new fields, and `atomic.Value`'s typed-nil/type-consistency footguns for `func` types are avoided. Two non-obvious traps surfaced: (1) Go `RWMutex` is **non-reentrant** — settlement's `getInstrumentConfig` (now `RLock`) could not be called from `runMultiInstrumentCycleLocked` which already holds the write lock, so a lock-free `lookupInstrumentConfig` variant was extracted to avoid `RLock`-under-`Lock` self-deadlock; (2) the margincall return changed `(*MarginCall, error)`→`(*MarginCall, bool)` where the bool flags a *newly-issued* call, preserving the "fire handler only on new issue" semantics while moving `Evaluate` and its handler outside `e.mu`.
- **Action:** For any engine callback in this codebase, never invoke it under the engine lock. Use the `*Locked` helper pattern: helper owns `defer Unlock()` and returns `(handler, valueSnapshot)`; the thin public method fires the handler after unlock. When converting a `Mutex` to `RWMutex` for read concurrency, audit every caller that already holds the write lock and give it a lock-free internal variant — Go locks do not recurse. Preserve exact handler-firing semantics (e.g. settlement does NOT fire on the early P&L-failure path).

### Pattern: `testing.Testing()` is the correct guard for "test-only" production branches — never an env var (2026-06-26)
- **Context:** R007 removed the silent in-process Kafka fallback: `NewProducerFromEnv`/`NewConsumerFromEnv` previously returned a `ChannelProducer`/`ChannelConsumer` (which cannot cross process boundaries) when `KAFKA_BROKERS` was unset, silently dropping all cross-service events in a misconfigured deployment. Now they `log.Fatal` when brokers are absent outside tests, and reach the in-process channel adapter only via `testing.Testing()` + explicit `newInProcess*` constructors.
- **Finding:** `testing.Testing()` (stdlib, Go 1.21+) is true only inside a `go test` binary and cannot be set by an operator — strictly stronger than a `KAFKA_ALLOW_INPROCESS` env flag, which would re-introduce the exact silent-drop foot-gun being removed. Importing `testing` in production files compiles fine (`testing.Init()` is never called, so no flags register) but may trip `depguard`/`forbidigo` linters under the future R014 CI gate. The one inherent limitation: the production fatal path is unreachable from any test binary by construction, so it is documented-not-tested — acceptable for a `log.Fatal` startup guard.
- **Action:** For any "behave differently under test" branch in production code (fallbacks, fixtures, fatal-on-misconfig), gate on `testing.Testing()`, not an env var. Pair it with explicit unexported constructors for the test-only path rather than inlining it in an env-absence default. Add a linter allowlist entry when R014 lands. Apply the same fail-fast to other process-crossing stubs (the channel Kafka adapter is in-process only — see the cross-service e2e gap).

### Pattern: A race-gate is only worth its name if it can be proven to fail on the pre-fix code (2026-06-26)
- **Context:** R009 delivered a `tests/race/` harness for R008's fixes: `run_race_tests.sh` (focused + `--full` modes under `go test -race`), plus `verify_prefix_failure.sh` which reconstructs the pre-R008 bug shapes (handler-in-lock, unsynchronized handler write) in a throwaway `/tmp` copy of the clearing engine and asserts the tests fail (deadlock timeout / `WARNING: DATA RACE`), then confirms they pass on committed code.
- **Finding:** The deadlock-regression tests self-fail via an internal 2s `select`/`time.After` so a regression hangs-and-fails rather than blocking CI forever. A scope constraint drove the decomposition: R008's logic lives in `internal/` packages, which a separate `tests/` module **cannot import** (Go internal-package rule), and the Test Writer must not modify `src/` — so R009's value is the runnable harness + the pre-fix soundness proof + docs that *drive* R008's in-package tests, not duplicate test files. The reviewer flagged a real vacuous-pass risk: a `-run` regex that matches no test exits 0 ("no tests to run") and reports PASS without exercising anything.
- **Action:** Every concurrency/regression gate must ship a companion that proves it fails on the broken code (reconstruct the bug in a `mktemp -d` copy via `go mod edit -replace`; never mutate `src/`). Guard focused `-run` filters against the no-match-vacuous-pass trap (assert at least one `=== RUN` line). Place engine concurrency tests in the engine's own package (R008 did); place the runnable gate + soundness proof in `tests/`.

### Pattern: The race detector is now the verification tier for engine concurrency fixes (2026-06-26)
- **Context:** Integration run `run-20260626-033236` ran `go test -race ./...` on all four trading engines (matching, clearing, margin, settlement) after the R008/R009 merge — all four PASS under `-race`, confirming the handler-in-lock / map-race / handler-field-sync fixes hold.
- **Finding:** Unit tests pass with or without the R008 fixes (the bugs are timing-dependent); only `-race` plus the deadlock-timeout tests actually exercise the defect class. This is a distinct tier from the existing unit / e2e-API / Playwright tiers: it catches data races and lock-ordering deadlocks that no functional assertion surfaces. `GOTOOLCHAIN=auto` is needed on an older host because the engines declare `go 1.25.x` (a known network-fetch deviation from the zero-network ideal).
- **Action:** When R014 re-enables the CI gate, add `go test -race` for the four engines (or `tests/race/run_race_tests.sh --full`) as a required check so handler-in-lock / map-race regressions fail automatically. Treat a green non-race run with a failing `-race` run as FAIL.

### Pattern: The live P1 tenant-bypass defect has now survived THREE FAIL runs while P2 backlog kept draining (2026-06-26)
- **Context:** `run-20260626-012134`, `-022823`, and `-033236` are all FAIL on the identical gateway `tenantBypassPrefixes` defect (R011/R012 — `TenantMiddleware` whitelists nearly every tenant-scoped path, so handlers run with no tenant in context, 200 where 401/403 required). Between those runs the pipeline drained the money-float batch (R018–R022) AND the concurrency batch (R007–R009) — both genuine but P2-latent — while R011/R012 stayed `deferred`.
- **Finding:** This is the same severity-inversion the 2026-06-26 "P1 outranks P2" pattern named, now repeated for a *second* batch. The `deferred` backlog exists and is well-maintained, but the Planner keeps selecting whichever tasks parallelize most conveniently (per-module migrations, per-engine concurrency fixes) over the one task that is a live security-relevant correctness defect violating the platform multi-tenancy invariant. R007–R009 were the right *kind* of work (real concurrency bugs) but R011 should have led, since it is exploitable today and is the sole cause of the FAIL verdict.
- **Action:** R011 (narrow `tenantBypassPrefixes` to `/platform/`, `/api/v1/platform/`, `/api/v1/auth/`, health; require `X-GarudaX-Tenant` on all tenant-scoped routes) + R012 (tests) must be the next batch — before R023, R013–R017, or any feature work. Enforce the severity gate mechanically: if the latest integration run is FAIL on a P1, the Planner may not schedule P2/P3 tasks until the P1 fix task is in-flight. Three consecutive FAIL runs on one known defect is a process failure, not a backlog-ordering preference.

### Pattern: Concurrency/fail-fast tasks estimate near 1x, unlike templated migrations (2026-06-26)
- **Context:** R007–R009 timing: R007 (Kafka fail-fast, 9 services) 12m est / ~14m actual = **0.86x (over)**; R008 (engine concurrency, 3 engines) 15m / ~9.5m = 1.6x; R009 (race-gate harness) 10m / ~6m = 1.7x. R007 is one of the few R-tasks to run over estimate.
- **Finding:** R007 ran over because "remove the silent fallback across all 9 services + write a tested predicate + explicit constructors per service" is breadth with real design choices (`testing.Testing()` vs env var, where to put the in-process constructor), not a templated rename — the same profile as the *first-of-kind* migrations (R004). R008/R009 were tighter because the snapshot-under-lock idiom, once chosen, applied mechanically across the three engines. Branch-lineage bundling recurred: the R008/R009 branches carried unrelated CLAUDE.md/`tasks.json` and earlier-R hunks because they were cut before those landed — both reviewers flagged merge hygiene.
- **Action:** Estimate cross-cutting *design* tasks (fail-fast wiring, new cross-service idiom) at ~1x of greenfield (no buffer — there is discovery cost), and *mechanical-application* tasks (apply a chosen idiom across N modules) at the 2x steady state. Rebase R-series branches onto current `main` before merge so carried CLAUDE.md/`tasks.json`/earlier-R hunks don't clobber newer state.

### Pattern: The four-FAIL P1 tenant defect is finally CLOSED — the severity gate works once actually scheduled (2026-06-26)
- **Context:** After FOUR consecutive FAIL runs on the identical gateway `tenantBypassPrefixes` defect (`run-20260626-012134`, `-022823`, `-033236`, all FAIL on the same 3 `TestTenantMiddleware_*` tests), the R010–R013 tenancy batch finally ran and `run-20260626-040705` reports the P1 **RESOLVED**: all 8 gateway tenant-middleware tests pass (`_ValidTenant`, `_MissingHeader`, `_UnknownTenant`, `_TradingRoutesEnforced`, `_UnknownRouteReturns404`, etc.) and all 2768 Go tests are green.
- **Finding:** The fix landed exactly as the repeatedly-documented "P1 outranks P2" / "severity-inversion" patterns prescribed — R011 narrowed `tenantBypassPrefixes` to `/platform/`, `/api/v1/platform/`, `/api/v1/auth/` + health, and required `X-GarudaX-Tenant` on every tenant-scoped route. The loop that took four runs to close validates that the `deferred` backlog plus a severity tag is *not* sufficient on its own; the defect only got fixed when R010–R013 were explicitly scheduled as the next batch. The cost of the delay was three wasted FAIL integration runs while P2-latent work (R007–R009, R018–R022) drained around it.
- **Action:** Treat a P1 fix landing as confirmation that the mechanical severity gate (proposed across three prior patterns: "if the latest integration run is FAIL on a P1, the Planner may not schedule P2/P3 until the P1 fix is in-flight") is the correct policy — now adopt it as a hard rule, not a recommendation. The next live-defect FAIL should be fixed in the immediately following batch, not after N convenient parallel batches.

### Pattern: Shared tenant module mirrors the decimal recipe; a type-alias shim keeps gRPC out of HTTP-only consumers (2026-06-26)
- **Context:** R010 promoted the tenant primitives out of `src/shared/internal/tenant` (un-importable from other modules by Go's internal rule) into a standalone zero-dep `go 1.22` module `github.com/garudax-platform/tenant` at `src/shared/pkg/tenant/`, consumed by gateway + securities-service via relative `replace ... => ../shared/pkg/tenant`. It also deleted the orphaned dead-duplicate `src/platform-control` registry (the live one is `src/platform-service`).
- **Finding:** This is the decimal-module recipe applied to a second primitive (context key, middleware, observability), confirming it as the standard for sharing a Go primitive in this repo. The non-obvious move: the gRPC interceptors (`grpc.go`) stay behind in the heavier `shared` module so gateway/securities (HTTP-only) don't inherit `google.golang.org/grpc` as a transitive dep; `internal/tenant/alias.go` re-exports the core symbols via **type alias** (`type TenantID = core.TenantID`), which means the context *key* is physically identical across grpc.go, the kafka tenant router, observability, gateway, and securities — so they read/write one and the same context value with zero edits to the unchanged files. A plain wrapper type (not an alias) would have created two distinct context keys and silently broken cross-package tenant resolution.
- **Action:** For any shared Go primitive: standalone `go 1.22` zero-dep module under `src/shared/pkg/...`, relative `replace`, and re-export into legacy in-tree packages via `type X = core.X` **aliases** (never wrapper types) so context keys / map keys / `==` comparisons unify. Keep heavyweight-dependency variants (gRPC interceptors) behind the dependency boundary and alias only the stdlib-only core into zero-dep consumers.

### Pattern: Tenant-before-auth reintroduces the 401-vs-404 bug at a new layer — guard with a pre-enforcement RouteChecker (2026-06-26)
- **Context:** R011 placed `TenantMiddleware` before `Auth` in the chain (RequestID → Tracing → Metrics → BodyLimit → **Tenant** → Auth → RateLimit → Router) so the tenant is resolved before any handler. But because Tenant now rejects header-less requests with 401 `TENANT_REQUIRED` before the router can run, an unknown path would 401 instead of 404 — the exact "gateway auth-before-routing 401-vs-404" bug (documented 2026-03-29) resurfacing one layer up.
- **Finding:** R011 added an optional `RouteChecker` to `TenantMiddleware` (`TenantMiddleware(validTenants []string, routeChecker ...RouteChecker)` — variadic for backward-compat with existing call sites/tests) that returns 404 for unregistered paths *before* tenant enforcement, mirroring the Auth middleware's existing guard. `main.go` passes the router as the checker. `run-20260626-040705` confirms both `TestTenantMiddleware_UnknownRouteReturns404` and `_KnownRouteStillEnforcedWithChecker` pass. The tenant is forwarded to backends as the canonical `X-GarudaX-Tenant` header in `BackendRequest.Metadata` (built fresh per request from the validated context value, so there is no client-spoof path around the whitelist).
- **Action:** Any middleware inserted *ahead of routing* that can reject a request (tenant, auth, rate-limit) must consult a route-existence checker and 404 unregistered paths first, or it will mask the real status with its own rejection. Reuse the `RouteChecker`/`RouteExists` pattern. NOTE the still-open gap (deferred): the whitelist only validates the tenant is *registered* — nothing binds the authenticated JWT subject to the requested tenant, so a user authed for `ace-commodities` can still send `X-GarudaX-Tenant: mse-equities`. Cross-tenant authorization + backend-side gRPC interceptor enforcement is the headline follow-up (R012-class / backend enforcement), not yet built.

### Pattern: Frontend fixture drift is now the sole FAIL cause — manual SPA edits outrun hard-coded test fixtures (2026-06-26)
- **Context:** With the P1 tenant defect closed, `run-20260626-040705` is FAIL *solely* on 26 frontend test failures: admin-ui (5: `market-phase.test.ts` `normalizeInstruments` field/default-phase changed from `PRE_OPEN`→`CLOSED`, Halt-All filtering; `bot-integration.test.ts` createTicket path), demo-runner (21: `sections.test.ts` + `t066_integration.test.ts` assert 13 sections/51 steps but merged data now has 14 sections/59 steps with new `admin-orderbook`/`ob-` steps). Every SPA's `tsc -b` type-check and `vite build` pass, so these are pure assertion drift, not logic/type breakage.
- **Finding:** This is the "admin-ui is actively developed between pipeline runs" pattern (2026-03-29) hardened into a recurring 26-failure block. The implementation (data files, `normalizeInstruments`) advanced via merged edits while the hard-coded fixture counts/shapes lagged — confirmed by the churning `src/demo-runner/tsconfig.tsbuildinfo` in git status. The same 26-failure set persisted identically across all four 2026-06-26 runs, so it is stable drift, not flakiness. The implementation is the source of truth here (it type-checks and builds); the tests are the lagging side.
- **Action:** Schedule a fixture-refresh task (R013-class, test-only) that realigns admin-ui `market-phase`/`bot-integration` and demo-runner `sections`/`t066` assertions with the current `sections.ts` / `normalizeInstruments` behavior — after which the run flips to PASS. Going forward, when a feature task edits a data/shape file that has a counts-based fixture test (section counts, step counts, normalization output), require it to update the co-located fixture in the same task. The integration agent should report such a run as `PARTIAL` (builds green, all backend tests pass, P1s clear, only frontend fixture drift) rather than `FAIL`, per the e2e-canary verdict-granularity pattern — a fixture-count mismatch with a green `tsc` is test debt, not a production defect.

### Pattern: Docs are a real deliverable — re-scope by verifying each claim against code, annotate "target vs today" (2026-06-26)
- **Context:** R013 rewrote `README.md` and `AiX_Project_Knowledge.md` to describe the platform as **single-tenant-with-namespacing today** (one live tenant `ace-commodities`, `ace_*` schema namespace, validate+forward tenant edge) rather than the fully-isolated multi-tenant platform the prior text asserted as fact. It fixed factual drift (11→14 Go modules, phantom `src/clearing-service`, `garudax/`→`ace-platform/` root, Java/Kotlin→Go stack, roadmap statuses).
- **Finding:** The correct test for a docs task is "are the new claims true against the code on disk?" — the reviewer independently verified every load-bearing claim (`ls src/*/go.mod` = 14; `tenantBypassPrefixes` contents; in-memory registry seeded with 2 tenants; schema-only/dry-run provisioner; `mse_*` schemas absent). Asserting full data isolation as fact when row-level `tenant_id` filtering and backend enforcement do not exist is a *security* hazard (false confidence in an unenforced boundary), so the "target vs today" annotation pattern — keep the aspirational invariants as the review contract but label each with its real status — is net-positive, not mere bookkeeping. R013 correctly stayed in scope (edited only the two named root docs + its handoff; did NOT touch the protected `CLAUDE.md`/`tasks.json`, flagging the stale "11 Go services"/`clearing-service` references there for the PostMortem step).
- **Action:** For any docs re-scope task, verify each factual claim against source (counts via `ls`/grep, enforcement via the actual middleware constants) rather than against the prior doc. Annotate aspirational claims as `(target)` with a `today:` status line instead of deleting them. Per R013's flag: this PostMortem step should reconcile the stale `CLAUDE.md` "Current State: 11 Go services" header and the removed-`src/clearing-service` learned-pattern references to "14 Go modules" so the AI-memory file matches the corrected public docs.

### Pattern: Tenancy batch (R010–R013) estimates cluster at 1.2–1.9x — tight, like other cross-cutting work (2026-06-26)
- **Context:** R010 (shared tenant module + platform-control deletion) 15m est / ~11m = 1.4x; R011 (gateway enforcement) 15m / ~8m = 1.9x; R012 (e2e tenant tests) 10m / ~6m = 1.7x; R013 (docs re-scope) 8m / ~6.5m = 1.2x.
- **Finding:** The whole R-series (financial-correctness + concurrency + tenancy) holds the established cross-cutting/refactor profile of ~1–2x, well under the 2–3x greenfield steady state. R013 (docs) at 1.2x is the tightest, consistent with the "documentation tasks read code rather than write it" pattern. The tenancy batch parallelized cleanly where modules were disjoint (R012/R013 started at the same timestamp), but R010→R011 were correctly sequential — both touch the gateway tenant middleware, so they could not run concurrently (the "reserve sequential scheduling for tasks that share a module" rule held).
- **Action:** Keep estimating R-series cross-cutting tasks (module extraction, enforcement wiring, e2e tests, docs) at ~1.5x of the greenfield estimate. Continue gating co-located tasks sequentially (R010/R011 both in gateway middleware) while parallelizing module-disjoint ones (R012 tests + R013 docs).

### Pattern: Metric reconciliation — the March "final pipeline totals" baselines are HISTORICAL; current reality is ~2769 Go tests, 14 modules (2026-06-26)
- **Context:** R016 refresh, driven by R015's fresh full-stack run (`run-20260626-051039`, PASS). Numerous March-2026 patterns in this file assert "final pipeline totals" of 857 / 887 / 1199 tests, **9 Go services**, and a 66% coverage baseline — and the 2026-03-29 patterns assert "6 unfixed e2e bugs" across three bug classes as a live FAIL. Both narratives are now stale by three months of R-series work (R001–R022). The codebase grew from 9 to 14 Go modules (added corporate-actions, fix-gateway, platform-service, securities-service, shared, plus 2 shared zero-dep sub-modules), and the test count is ~3.6x the last-quoted total.
- **Finding (current reality, measured on `main` 2026-06-26):**
  - **Go modules:** **14** (`ls src/*/go.mod`) + 2 shared sub-modules (`shared/pkg/types/decimal`, `shared/pkg/tenant`). The repeated "9 services" / "11 Go services" / "15 Go modules" counts are all superseded by **14**.
  - **Go tests:** **~2769 `func Test*`** across `src/**/_test.go`, plus **~515 `t.Run` subtests** (the "828 Go tests" / "689 unit tests" / "2768 Go tests" figures are all historical snapshots; R015 ran the full Go suite green). The platform-wide "1199 total tests" milestone is **historical** — it predates the entire R-series.
  - **e2e:** **32 top-level / 141 subtests / 8 graceful skips / 0 fail** against a live gateway + real Kafka broker on a freshly-rebuilt stack (R015). This is the canonical e2e baseline; the older "20 all-skip" and "55 (6 fail)" e2e figures are historical.
  - **Coverage:** **65.0% statement-weighted** (12 modules, 41.5%–88.3%) / **69.5% business-logic-only** (87 pkgs) — consistent with, and supersedes, the ~66% March baseline. Race tier: all 4 engines PASS `-race`.
  - **Latest integration verdict:** `run-20260626-051039` **PASS**, superseding the four 2026-03-29 FAIL runs *and* the four 2026-06-26 pre-R010 FAIL runs (`-012134/-022823/-033236/-040705`).
  - **"6 unfixed e2e bugs" narrative — RESOLVED.** Bug class #1 (gateway 404-before-auth / RouteChecker) = **FIXED** (R011, verified live). Bug class #2 (compliance KYC array-vs-object) = **FIXED** (verified live). Bug class #3 (cross-service Kafka propagation) = **OPEN but NOT-IMPLEMENTED, not failing** — engines never wire `internal/kafka` into `cmd/main.go`, so no topics are created and `TestFullTradingLifecycle` passes only vacuously; tracked as **R024**, not as an e2e regression. The 4-FAIL-run "documents but doesn't fix" loop for these bugs is closed.
- **Action:** Treat every pre-2026-06 "final/definitive pipeline totals" and "6 e2e bugs" pattern as **historical**, not current — do not quote 857/887/1199 tests or "9 services" as the baseline. The canonical baseline going forward: **14 Go modules, ~2769 Go test funcs, e2e 32/141/8-skip PASS, 65% stmt / 69.5% biz-logic coverage, latest verdict `run-20260626-051039` PASS.** Re-measure (don't copy) test/module counts each refresh — they drift fast. Known open follow-ups from R015: **R024** (wire real Kafka into engine mains → close bug class #3), **R025** (zero-pad migration filenames + dedupe `market_data.trades` V8/V16 so a clean DB initializes), **R026** (Dockerfile build context `./src` so fresh images include the shared `replace` modules — the cause of the 06-22 image staleness).

### Pattern: A required CI status check that can't run on a PR bricks every merge — reconcile branch-protection with workflow triggers (2026-06-26)
- **Context:** R014 (re-enable CI build+test gate) — the only REJECTED task in this batch. The gate itself (dynamic module-discovery matrix, Go 1.25 + `GOTOOLCHAIN: auto`, non-gating coverage, `-race` without forcing `CGO_ENABLED=0`) was correct and an improvement over the old hardcoded `src/*/` loop.
- **Finding:** It was rejected for one fatal interaction: guarding the *entire* `terraform.yml` to `workflow_dispatch`-only removed the `pull_request` trigger from its `Validate` job, but `.github/branch-protection.json` lists `"Validate"` as a `required_status_checks.context` on `main` with `strict: true`. A required check that never reports on a PR leaves every PR permanently "Expected"/pending and **blocks the merge** — the exact opposite of the task's goal. Applying that branch-protection after the change would brick all merges to `main`. The same trap looms for `security.yml`'s `Security Gate` (its `trivy-config` IaC scan over `infrastructure/` would block merges on a no-infra PR if it fails) and produced dead `deploy.yml` `promote-staging`/`promote-prod` jobs whose `if:` requires a `workflow_run` context that can no longer occur under dispatch-only.
- **Action:** The required-check set in `branch-protection.json` must exactly equal the set of checks that report on a normal PR. When guarding a cloud-mutating workflow against auto-apply, keep its **read-only** jobs (`terraform-validate`/`terraform-plan`) on `pull_request` and guard only the mutating `*-apply-*` jobs (which already carry `environment:` approval gates) — never make a whole workflow dispatch-only when one of its job names is a required status context. When re-enabling the gate (R014 rework), also (a) drop or re-trigger the now-dead `deploy.yml` `promote-*` jobs, (b) confirm `Security Gate` passes green on an infra-untouched PR, and (c) remember `branch-protection.json` is data only — it must be `PUT` to the repo to take effect.

### Pattern: A genuinely-fresh `docker compose` bring-up is its own verification tier — source-green tests never catch deploy-pipeline rot (2026-06-26)
- **Context:** R015 fresh full-stack run (`run-20260626-051039`, e2e PASS). Every `ace-platform-*` image on disk was built 2026-06-22, predating the *entire* R007–R022 R-series; the first e2e pass against those images reproduced pre-R011 (pre-fix) tenant-bypass behaviour. A genuinely fresh stack required rebuilding all 12 backend binaries from current source and re-deploying.
- **Finding:** All Go / `-race` / frontend suites were green against `main` source, yet the *deployed images* were 4 days / one whole R-series stale — a defect invisible to every source-level test. Root cause (→R026): each service Dockerfile uses build context `./src/<svc>`, but after R002 (`shared/pkg/types/decimal`) and R010 (`shared/pkg/tenant`) those modules consume `replace ../shared/...` which sits **outside** that context → `replacement directory ../shared/... does not exist`. The image build broke the instant the shared-module refactor merged, silently freezing every image. A second deploy-tier defect (→R025): migration files aren't zero-padded, so Postgres `docker-entrypoint-initdb.d` applies `V10/V11/...` *before* `V1/V2` lexicographically → `schema "exchange" does not exist`, plus a duplicate `market_data.trades` (`V8__` `executed_at` vs `V16__` `traded_at` — the shape the service code actually uses). **A clean DB cannot initialize and fresh images cannot build with the committed artifacts** — both were worked around at orchestration time (temp `/tmp` artifacts only, no repo source changed).
- **Action:** Treat "rebuild all images from `./src` + `docker compose up` on a fresh Postgres volume + run `tests/e2e` against the live gateway" as a **distinct required verification tier** alongside unit / e2e-API / `-race`. It is the only tier that catches Dockerfile-context, migration-ordering, and image-drift defects, all of which are invisible to `go test`. Rules that follow: (1) when a shared module is introduced via relative `replace`, the consuming service's Docker build context must widen to include it (`./src` or a root builder / vendored modules); (2) zero-pad migration filenames (`V001__`…) from the start so `initdb.d` lexicographic ordering equals version ordering; (3) wire the image rebuild into the CI gate (R014) so drift fails automatically rather than being discovered 4 days late at integration time. Bug class #3 (cross-service Kafka, R024) is the substantive open item: engine `cmd/*/main.go` never wires `internal/kafka`, so `TestFullTradingLifecycle` passes only vacuously — pair the wire-up with seeding a registered instrument so the lifecycle stops passing on an empty result.

### Pattern: Orphan-stub triage in repo hygiene — implement-if-referenced, delete-if-context-free, document-if-library; flag deploy-affecting scope creep (2026-06-26)
- **Context:** R017 repo hygiene drained the file-level backlog left after a manual cleanup of 119 stale branches.
- **Finding:** The correct action per stub depends entirely on what *depends on it*, and the four cases diverged: (1) `vpc`/`security-groups` were empty `.tf` stubs *instantiated by `main.tf`* and consumed by `eks`/`rds`/`msk` (`module.vpc.{vpc_id,private_app_subnet_ids,private_data_subnet_ids}`) — a `terraform apply` would already fail, so they were **implemented** to the exact consumed output names, not removed. (2) `elasticache` had no `module` block but its `redis_node_type/redis_num_shards/redis_replicas_per_shard` were declared in root `variables.tf` *and* set in `dev.tfvars.example` — unambiguous intent, so **implemented and wired in**. (3) `iam`/`s3` had zero supporting context (no variables, no references anywhere) — pure orphans, **deleted**. (4) `corporate-actions` is `package corporateactions` with no external importer and absent from every service list — **documented as a library** (no speculative `main.go`/Dockerfile fabricated). (5) V2–V5 were zero-byte Flyway stubs (real DDL is V1 then V6+) — **deleted** (Flyway tolerates version gaps on fresh deploys). The scope risk: a "12-minute hygiene" task expanded into ~330 lines of new production Terraform and **added a `module "elasticache"` to `main.tf`**, so the next `terraform apply` now provisions a Redis replication group + KMS key that did not exist — a deploy-affecting architectural addition made inside a cleanup task, and (like T003/T054) un-validatable locally because `terraform` is not installed in the worker env.
- **Action:** For orphan files, decide by dependency rather than by emptiness: referenced-but-empty → implement to the exact consumed input/output names; declared-variable-or-tfvars-backed → implement; zero-context → delete; library-by-design with no importer → document, don't fabricate a binary. Any hygiene change that touches `main.tf` module wiring (or otherwise changes what a `plan`/`apply` provisions) must be called out as **deploy-affecting** in the handoff, not buried as cleanup. All new/changed HCL must be gated behind `terraform fmt -check -recursive` + `terraform validate` in CI before any real `plan` — local hand-review cannot catch the cross-resource reference bugs that sank T003/T054, and the worker env has no `terraform` binary to catch them either.

### Pattern: Fresh-bring-up integration tasks blow past estimates when the deploy pipeline is broken; doc/hygiene tasks stay in the 1.5–2.5x band (2026-06-26)
- **Context:** R014–R017 timing. R014 (CI gate) 12m est / ~11m = 0.9x (rejected); **R015 (fresh run) 10m est / ~24m actual = 2.4x OVER**; R016 (metrics refresh) 5m / ~2.4m = ~2x under; R017 (hygiene) 12m / ~7m = 1.7x under.
- **Finding:** R015 is one of the rare R-tasks to *exceed* its estimate, and by a lot — not because the e2e suite is slow (it ran in ~16s) but because a "fresh full-stack run" silently became "rebuild 12 binaries from source + bake alpine images + work around 3 infra defects (migration ordering, V8/V16 dedupe, Docker build-context) at orchestration time." The estimate priced the happy path (`docker compose up` + `go test ./tests/e2e`); the actual was dominated by *repairing a broken deploy pipeline*. Every other R-task this batch (doc refresh, hygiene, even the rejected CI-gate authoring) stayed in the familiar sub-2.5x over-estimate band.
- **Action:** Estimate fresh-bring-up / "fresh integration run" tasks for the **unhappy path** — assume images may be stale and the deploy pipeline may not build or init cleanly, and budget for rebuild + workaround time (R015 needed ~2.4x its happy-path estimate). Once R025/R026 land (clean DB init + buildable images from committed Dockerfiles) this should collapse back toward ~1x and the task becomes the cheap `compose up` + e2e it was estimated as. Keep doc/audit/refactor/hygiene R-tasks at the 1.5–2.5x band.

### Pattern: The R006 float→decimal remediation is COMPLETE — R023 closed the last (latent) residual; no live float money math remains (2026-06-26)
- **Context:** R023 migrated `gateway/internal/reporting/generator.go` + `store.go` (settlement-statement P&L, fee totals, net amount, market-summary OHLC) to the shared `decimal.Decimal`, draining the single residual the R022 re-audit had surfaced. With R018–R021 (corporate-actions, compliance MCSD/FRC, gateway fees+risk, fix-gateway) and this, every R006-flagged money path — live and latent — is now on the shared fixed-point type. R023 was APPROVED first-pass and merged to `main` (commit `06c8c75`). Estimate 15m / ~10.4m actual = **1.44x** (templated-repeat, multi-package+DB-store surface — tighter than the 2x band, consistent with R020's 1.2x for the same shape).
- **Finding:** Two reusable specifics this port pinned down. (1) **No DB migration was needed because the columns were already `DECIMAL(18,4)`** (`V22__reporting_tables.sql`), so the value round-trips `Decimal → .Float64() → NUMERIC(18,4) → decFromFloat → Decimal` losslessly — *but only below ~1e11*; above that the `Float64()` DB-hop loses the least-significant 4dp digit. This is acceptable for ace-commodities notionals and is the documented trigger to switch reporting money columns to raw `int64` (`*_raw`, via `Raw()`/`DecimalFromRaw()`, the R011 pattern) when MSE-equities scale arrives. (2) **A retained `roundTo4`/`roundTo2` float helper is no longer automatically a "missed money residual"** — R023 keeps `roundTo4` but scopes it to the surviving non-money float fields (volume, open-interest, pct-of-OI) with an inline comment, so the platform-wide float-helper grep (a CI sweep under R014) must treat documented quantity/ratio rounders as expected, not as residuals.
- **Action:** Treat the money-correctness phase as closed for the current codebase; do not re-open it for new modules without re-running the R006/R022 audit method (`float64` sweep + `int64(float*N)` truncation hunt + `roundTo2|roundTo4` helper sweep). For any *new* money surface use the validated six-step recipe directly (domain=Decimal / DTO=float64 at handler edge / `decFromFloat` at the `database/sql` boundary / multiply-before-divide / `Try*` at untrusted edges / columns already-`DECIMAL(18,4)`-or-`*_raw`). **Open debt to schedule:** `decFromFloat` is now triplicated across gateway `fees`, `risk`, and `reporting` packages — three+ copies is the trigger to give the shared `decimal` type `sql.Scanner`/`driver.Valuer` so every consumer drops its shim (a one-time shared-module change, higher leverage than a fourth copy).

### Pattern: Frontend fixture-count drift is now durable test debt — 6+ consecutive non-green runs, still no scheduled fix task (2026-06-26)
- **Context:** Every integration run from `run-20260626-040705` onward (`-040705`, `-045052`, `-051039` backend-only, `-053211`, all post-merge) is non-green *solely* because of the **identical 26 frontend failures**: admin-ui `market-phase.test.ts` (4: `normalizeInstruments` now defaults missing phase to `CLOSED` not `PRE_OPEN`, extra field, Halt-All TRADING filter) + `bot-integration.test.ts` (1: `createTicket` POST path), and demo-runner `sections.test.ts`/`t066_integration.test.ts` (21: data grew to 14 sections / 59 steps with new `admin-orderbook`/`ob-` steps, fixtures still assert 13/51). All three SPAs `tsc -b` type-check and `vite build` cleanly, so this is assertion drift, not logic/type breakage — the implementation is the source of truth and the co-located counts-based fixtures lag.
- **Finding:** This is the test-debt analog of the long-documented "documents but doesn't fix" gap: the drift has been named in CLAUDE.md and flagged in *every* run report as an R013-class fixture-refresh, yet across six runs **no fixture-refresh task was ever scheduled in `tasks.json`**, so the integration verdict stays stuck at PARTIAL/FAIL while the platform is substantively healthy (all builds green, ~2769 Go tests pass, race tier clean, e2e PASS against the live stack). The recurring root cause is that the feature/data tasks that edited `sections.ts` / `normalizeInstruments` did not update their co-located counts-based fixtures in the same task — exactly the failure the "require the feature task to update the co-located fixture" action warned about.
- **Action:** Stop re-documenting the drift and *schedule the fix*: add a single test-only `deferred`→`pending` task (R013-class) that realigns admin-ui `market-phase`/`bot-integration` and demo-runner `sections`/`t066` assertions to current behavior — it flips every subsequent run to PASS/PARTIAL-clean. Going forward, enforce the same-task fixture rule mechanically: a task whose diff touches `sections.ts` or `normalizeInstruments` (or any data file with a counts-based fixture test) is not "done" until its co-located `*_test.ts` counts are updated in the same diff. The integration agent's PARTIAL verdict (green builds + green backend + only fixture drift) is the correct granularity and should continue to be reported as PARTIAL, not FAIL.

### Pattern: "Not implemented" vs "implemented-but-not-wired" — verify the `cmd/main.go` composition root, not just the package (2026-06-26)
- **Context:** Historical e2e bug class #3 (cross-service event propagation) was documented for *months* (every 2026-03-29 FAIL run + the R015 fresh-stack run) as "channel-based Kafka stubs don't bridge separate processes." R024 found that diagnosis was only half right: the **real wire-protocol adapters already existed** (`segmentio/kafka-go` `KafkaProducer`/`KafkaConsumer` in each engine's `internal/kafka/`, already selected by `NewProducerFromEnv`/`NewConsumerFromEnv` when `KAFKA_BROKERS` is set, hardened by R007). The actual defect was that **no engine `cmd/*/main.go` ever called those constructors** — the kafka package was exercised only by unit tests, so with a real broker running and `KAFKA_BROKERS=kafka:9092` injected, zero application topics were created and `TestFullTradingLifecycle` passed *vacuously* (no instrument seeded → no trade → nothing to propagate).
- **Finding:** Unit-test coverage of a package (82%+ on every `internal/kafka`) gave false confidence that a feature was "implemented." The propagation was never wired into the binaries that run in production. The R015 fresh-stack run is what finally distinguished "OPEN-not-implemented" from "intermittently-failing" — because it inspected `kafka-topics --list` (empty) and engine logs (no producer/consumer lines) and grepped `cmd/main.go` (no kafka reference), rather than trusting a green `TestFullTradingLifecycle`. A passing lifecycle test against an **unseeded** demo stack tolerates empty cross-service results via the graceful-skip pattern, so it cannot prove propagation works.
- **Action:** When an integration capability spans services, verify it at the **composition root** (`cmd/*/main.go` actually constructs and starts the producer/consumer), the **runtime** (topics created, producer/consumer log lines, `KAFKA_BROKERS` consumed), and a **seeded** end-to-end flow — not just package unit tests. A green cross-service e2e test against an unseeded stack is not evidence the wiring exists; require it to register/seed the instrument it trades (the R024 follow-up: seed `WHT-HRW-2026M07-UB` so the lifecycle stops passing vacuously). Treat "package exists + unit-tested" and "wired into the running binary" as distinct checklist items.

### Pattern: `internal/eventbus` composition-root is the validated way to wire Kafka into engine binaries (2026-06-26)
- **Context:** R024 closed bug class #3 by adding a per-engine `internal/eventbus` package to matching/clearing/margin/settlement and wiring it into each `cmd/*/main.go`, so events now flow across processes: matching → `ace-commodities.trades.executed` → clearing → `ace-commodities.clearing.novated` → margin (`margin.call-issued`) and settlement (`settlement.completed`). APPROVED first-pass, merged to `main` (`70b0923`, merge `9b7a29e`). Estimate 25m / ~18m actual = **1.4x** — the *first-of-kind* wire-up/design profile (~1–1.5x, like R007/R004), not the 2x templated-repeat band, because it had real composition decisions (where the decode→engine-call→publish bridge lives, single-handler conflict resolution) across 4 engines.
- **Finding:** The validated recipe: (1) **`eventbus` is the composition root** — it is the one package allowed to depend on *both* `internal/kafka` and `internal/engine`/`internal/types`, keeping the `kafka` package free of an engine dependency. The decode→engine-call→publish bridge lives here so it is unit-testable against the in-process **channel** `Producer`/`Consumer` without a broker, while production selects the real wire adapter via `NewProducerFromEnv`. (2) **Gate activation on `eventbus.Enabled()` (= `KAFKA_BROKERS` set)** — when unset, skip Kafka wiring entirely and still serve gRPC; this does *not* regress R007 fail-fast (eventbus simply never calls `*FromEnv` when disabled, and `*FromEnv` still `log.Fatal`s if called without brokers outside tests). (3) **Single-handler engines (margin/settlement support one `MarginCallHandler`/`CycleHandler`, last-write-wins): expose `PublishMarginCall`/`PublishCycle` and compose persist+log+publish in `main.go`** rather than letting the Runtime claim the handler and clobber the existing persistence/logging one. (4) **R008 snapshot-after-unlock is preserved for free** because all publish calls fire from handlers the engines already invoke *outside* their locks. (5) **Idempotency/DLQ/retry are inherited** from the real `KafkaConsumer` (`processedIDs` dedupe); bridges return `nil` (commit+drop) for permanently-bad records and `err` (retry) for transient failures. (6) Use **deterministic downstream keys** (`cycle-<tradeID>`) so reprocessing a novation is naturally idempotent. The reviewer flagged one residual: the consumer goroutine (`rt.Start`) is launched *before* `SetMarginCallHandler`/`SetCycleHandler` is composed in margin/settlement `main.go` — an event in that startup window would run the default non-publishing handler. Move handler composition before `rt.Start(ctx)` to close it.
- **Action:** For any new cross-service event flow, add an `internal/eventbus` composition package per service (depends on kafka+engine; bridge logic unit-tested via the channel adapter), gate it on `Enabled()`, and compose persist+log+publish in `main.go` for single-handler engines. Keep the genuine cross-process assertion in a separate graceful-skip module (`tests/kafka-e2e/`) that the integration agent runs against a live broker — the unit tier proves bridge *logic*, only the live tier proves *propagation*. Compose all handlers **before** starting the consumer goroutine. The back-edges defined in kafka wiring but not yet bridged (warehouse→margin `receipt-pledged`, settlement→clearing) follow the same recipe when those flows are exercised.

### Pattern: A rename/move task is not done until every hardcoded reference is updated AND the repo test suites are re-run — Docker-only verification is insufficient (2026-06-26)
- **Context:** R025 (zero-pad migration filenames `V1__`→`V001__` + dedupe the V8/V16 `market_data.trades` conflict) was the **only REJECTED task** in this batch. The DB-init diagnosis and fix were genuinely correct — the worker did a real fresh-volume `docker compose up`, confirmed 0 init errors and the correct `traded_at` hypertable, and the reviewer independently verified the lexicographic-ordering bug (`V11__matching_engine_tables.sql` ran before the `exchange` schema existed), the authoritative V16 shape matching `market-data-service/internal/store/postgres.go`, and the dead misspelled `garudax_marketdata_svc` role. It was rejected for a single missed reference: `tests/compliance/test_kyc_aml_spec.py:21` opens the migration by **exact hardcoded path** (`"V7__kyc_aml_tables.sql"`), so the rename to `V007__` made the fixture raise `FileNotFoundError` and every dependent test ERROR.
- **Finding:** The worker's grep for filename references ("No Flyway config, deploy manifest, or k8s manifest references migration filenames") missed an executable Python test that *reads* the file by name, and the Docker-init verification (correct and thorough for the DB tier) gave false confidence that the change was complete — the repo's own `tests/` suites were never run against the renamed branch. A rename/move is a cross-cutting change whose blast radius is "every literal that names the old artifact," which spans test code, not just config/manifests. This is the deploy-tier analog of the K8s/Terraform cross-resource bug class: each file is individually valid, but a reference between files is broken.
- **Action:** For any rename/move task: (1) grep for the bare artifact name (`V7__kyc`, not just `V7__kyc_aml_tables.sql`) across **all** file types including `tests/**/*.py|*.go|*.ts` and update every hit; (2) re-run the affected existing test suites on the renamed branch before handoff — Docker/runtime verification of the changed subsystem does not substitute for the repo's unit/spec tests; (3) adopt the worker's own suggested R014 CI check asserting zero-padded filenames + no duplicate version numbers, since this defect class is cheap to gate. R025 should be re-queued with the one-line `tests/compliance/test_kyc_aml_spec.py` path fix injected; the rest of the change is sound.

### Pattern: Widening a service's Docker build context to `./src` is the validated fix once a shared `replace` module is introduced (2026-06-26)
- **Context:** R026 closed the R015-predicted P1 deploy defect: per-service Dockerfiles used a narrow `context: ./src/<svc>`, but after R002 (`shared/pkg/types/decimal`) and R010 (`shared/pkg/tenant`) the consuming modules use relative `replace ... => ../shared/...`, which points **outside** that context (`replacement directory ../shared/... does not exist`). This silently broke image builds the instant the shared-module refactor merged — the cause of every on-disk image being 4 days / one whole R-series stale at R015. APPROVED first-pass; **verified with real `docker compose build` for all 12 Go services**, no `/tmp` workarounds. Estimate 15m / ~14m actual = 1.07x (deploy-tier, tight like other first-of-kind infra work).
- **Finding:** The validated recipe: switch only the services whose `go.mod` has a `replace => ../shared` directive to `context: ./src` + `dockerfile: <svc>/Dockerfile`; rewrite each Dockerfile as `WORKDIR /build` → `COPY shared ./shared` → `COPY <svc> ./<svc>` → `WORKDIR /build/<svc>` → build to `/build/bin/<svc>` (the binary must go to `bin/`, since `/build/<svc>` is now the source dir). Scope is derived, not hardcoded: `grep 'replace .* => ../shared'` returns exactly the 10 services to widen; `auth-service`/`platform-service` (no shared replace) and `corporate-actions` (library, no Dockerfile) are correctly left narrow/untouched. Add `src/.dockerignore` (excludes `node_modules`/`dist`/`*.tsbuildinfo`) so the wide context stays ~1 MB. The CI `docker-build.yml` matrix must become context-aware too — emit `{service, context, dockerfile}` per service (wide vs narrow by the same grep) and switch from `service:` to `include:`, using root-relative `file:`. Keep `CGO_ENABLED=0`, non-root `appuser`, and `GOTOOLCHAIN=auto` in the builder (engines declare `go 1.25.x`).
- **Action:** Whenever a new shared Go primitive is consumed via a relative `replace`, the consuming service's Docker build context **must** widen to include the replace target (`./src` here), or its image build breaks silently and images freeze at the last buildable commit. Pair this with R025 (clean DB init) — both are required for a hands-off fresh bring-up, and R027 (live fresh-stack verification of cross-service Kafka, bug class #3) remains blocked until R025 lands. Best fix going forward: wire a non-pushing `docker compose build` into the R014 CI gate so context/image-drift defects fail at PR time instead of being discovered days later at integration (the exact R015 root cause).

### Pattern: A "fix" task and its "live verification" are separate deliverables — bug class #3 needed the full R024+R025+R026 chain before R027 could prove it FIXED (2026-06-27)
- **Context:** R027 live fresh-stack verification (`run-20260627-021403`, **PASS**) closes the months-old e2e "6 bugs / bug class #3" (cross-service Kafka propagation) narrative as **CONFIRMED-FIXED** against a real `cp-kafka` broker. R024 had already wired `internal/kafka` into engine `cmd/main.go` (via `internal/eventbus`), but that wire-up alone could not be *verified*.
- **Finding:** R024 (the fix) was unverifiable in isolation because two orthogonal deploy-tier defects blocked a genuinely fresh bring-up: R015 had shown every on-disk image was a whole R-series stale, a clean Postgres couldn't even initialize (migration ordering), and the committed Dockerfiles couldn't rebuild from source (`../shared` outside build context). The "FIXED" verdict only became *provable* once **R025** (zero-padded migrations + V8/V16 dedupe — merged after a one-line rework) and **R026** (`./src` build context) landed. R027 then built all images from committed Dockerfiles with **no `/tmp` workarounds**, came up on fresh `pgdata`+`kafkadata` volumes with **0 DB init errors**, and observed concrete live evidence: the four `ace-commodities.*` topics in `kafka-topics --list`, real `using real Kafka producer/consumer` log lines on all four engines, and `tests/kafka-e2e` PASS across process boundaries. A green unit/package suite (every `internal/kafka` at 82%+) had asserted "implemented" for months while the capability was neither wired nor verifiable.
- **Action:** When closing a long-standing cross-service integration defect, schedule the wire-up fix, its deploy-tier prerequisites (clean DB init, buildable images), and a dedicated **live-verification task** as explicit separate tasks with `blockedBy` edges — verification can be blocked for multiple tasks by defects orthogonal to the fix. Never mark a cross-service capability "fixed" off unit/package tests; require a live-stack closing artifact (`kafka-topics --list` + engine producer/consumer logs + a seeded e2e flow on a fresh volume). Verify at the composition root, the runtime, AND live — three distinct checklist items.

### Pattern: Transport works ≠ economic completion works — a REAL gateway-submitted trade surfaces downstream data/seed/topic gaps a synthetic test hides (2026-06-27)
- **Context:** R027. `tests/kafka-e2e` PASSES (propagation confirmed: matching→clearing→settlement, margin consumes clearing.novated), and HTTP `tests/e2e` PASSES (32/0/8-skip live). But a *real, gateway-submitted matched trade* does not complete the full economic lifecycle, blocked by three newly-surfaced defects filed as **R028 (P2)**: **D1** matching publishes `trades.executed` with **empty** participant/order IDs → clearing novation rejects ("both buyer and seller participant IDs are required"); **D2** margin has no risk params seeded for `WHT-HRW-2026M07-UB` → margin calc fails; **D3** `ace-commodities.dlq.*` topics don't exist → exhausted-retry events are dropped, not dead-lettered.
- **Finding:** The synthetic `kafka-e2e` test only passed because it **hand-sets** `P-BUY`/`P-SELL` participant IDs on its payload; the real matching-engine payload leaves them empty. This is the "seed the instrument so the lifecycle stops passing vacuously" pattern one level deeper: even with propagation working AND an instrument registered, the *payload contents* (participant/order IDs) and *downstream seed data* (margin params, DLQ topics) must be real for economic completion. The best-effort vs required watcher split was designed correctly — `margin.call-issued` was best-effort, so D2 did **not** produce a false propagation failure (it correctly did not conflate "no margin call issued" with "event did not propagate"). The bug-class-#3 verdict is therefore sound: transport is proven; D1/D2/D3 are economic-completion gaps downstream of a working bus, not transport regressions.
- **Action:** A cross-service propagation test proves transport, not settlement. Add a separate **seeded** end-to-end assertion driven through the gateway (real payload with populated participant/order IDs, registered instrument, seeded margin params, pre-created DLQ topics) before declaring a trade lifecycle complete. Keep best-effort vs required watcher splits so a missing downstream economic event doesn't mask a transport regression. Schedule R028 to drain D1/D2/D3.

### Pattern: R025 rework confirms the rename-task rule in practice — the single missed hardcoded reference was the reviewer's exact flag, fixed in one line (2026-06-27)
- **Context:** R025 (zero-pad migration filenames) was the only REJECTED task of its batch — rejected solely because `tests/compliance/test_kyc_aml_spec.py:21` opened `"V7__kyc_aml_tables.sql"` by exact name, so the rename to `V007__` raised `FileNotFoundError`. The rework (commit `dafe18e`) changed it to `"V007__"` and merged (`486e5fe`/`fb98673`), unblocking R027.
- **Finding:** The DB-init diagnosis and fix were entirely correct on the first pass — verified with a real fresh-volume Docker bring-up — yet the task was rejected because the worker's filename-reference grep missed an *executable Python test that reads the migration by name*, and thorough Docker-init verification of the changed subsystem gave false confidence that the whole change was complete. The rework was a precise one-line path fix exactly as the reviewer specified; nothing else changed. This is the cheapest-possible rework and validates that the rejection was surgical and actionable (the reviewer cited the exact `file:line` and provided the fix). It confirms the prior learned rule rather than discovering a new one.
- **Action:** (validated, not new) For any rename/move: grep the **bare artifact token** (`V7__kyc`, not the full filename) across `tests/**/*.{py,go,ts}` plus config/manifests, AND re-run the repo's existing test suites on the renamed branch before handoff — subsystem (Docker) verification of the changed area is not a substitute for the repo unit/spec suites. Adopt the R025 worker's own suggested R014 CI check: assert migration filenames are zero-padded and no two share a version number (cheap to gate, catches this whole class).

### Pattern: Deploy-tier batch estimates — fresh-bring-up and reject+rework blow past estimate; wire-up/build-context/verification against repaired infra stay ~1–1.4x (2026-06-27)
- **Context:** R024–R027 timing. R024 (Kafka wire-up) 25m/~18m=1.4x; **R025 (DB init) 15m est / ~36m actual = 0.42x (OVER)**; R026 (Docker context) 15m/~14m=1.07x; R027 (live verification) 25m/~18m=1.4x.
- **Finding:** R025 is the outlier-over (and the batch's only rejection) for two compounding reasons: it ran a real fresh-volume Docker DB bring-up to verify (slow, like R015's 2.4x overrun), AND it incurred a reject+rework cycle. The "real-infra verification" cost dominates, exactly as the R015 fresh-bring-up pattern predicted. R024/R026/R027 — wire-up, build-context, and verification against now-repaired infra — landed in the familiar ~1–1.4x band. R027 at 1.4x confirms the R015 prediction that once R025/R026 land, the live verification collapses toward ~1x of its (correctly larger) 25m estimate rather than the multi-x overrun R015 itself suffered.
- **Action:** Estimate any task that does a real fresh-volume Docker bring-up (DB-init verification, image rebuild, live e2e) for the **unhappy path** and budget reject+rework risk — these run 2–3x a naive happy-path estimate while the deploy pipeline is broken. Wire-up/build-context/verification tasks against working infra stay ~1–1.4x. Now that R025+R026 are merged, future fresh-stack runs should be cheap (`compose build` + `up` on fresh volumes + e2e).

### Pattern: The R001–R027 remediation arc is COMPLETE — deploy pipeline is hands-off-reproducible; two debts remain unscheduled (2026-06-27)
- **Context:** End of the R-series. R027 is the closing live verification (`run-20260627-021403` PASS). The money-correctness arc (R001–R023) and the deploy/concurrency/tenancy arcs are all closed.
- **Finding:** All three R015-predicted P1 deploy defects are now closed and verified live: **R025** (clean DB init from committed migrations, 0 errors on a fresh volume), **R026** (images build from committed Dockerfiles with shared `replace` modules in scope, no workarounds), **R024** (cross-service Kafka wired + verified). A genuinely fresh `docker compose build` + `up` + `tests/e2e` now works from committed artifacts. All three historical e2e bug classes are FIXED (#1 404-before-auth, #2 compliance KYC object, #3 cross-service Kafka). Remaining open items are all ≤P2: **R028** (D1/D2/D3 economic-completion data gaps), and the **durable 26-test frontend fixture drift** — now reported across ~8 consecutive PARTIAL runs (`-040705` through `-021403`) and named as an R013-class fix in every report, yet **still never scheduled in tasks.json**. That fixture drift is now the single longest-standing "documents-but-doesn't-fix" gap in the pipeline.
- **Action:** New baseline for the next pipeline: deploy pipeline is hands-off-reproducible; latest live verdict `run-20260627-021403` PASS; ~2769 Go test funcs + 515 subtests green; 14 Go modules. Two debts to schedule **before** new feature work: (1) the **R013-class frontend fixture-refresh** — one test-only task (realign admin-ui `market-phase`/`bot-integration` and demo-runner `sections`/`t066` count assertions) flips every run from PARTIAL to clean; stop re-documenting it and create the task. (2) **R028** for economic completion (populate participant/order IDs in matching's published trade, seed margin params for `WHT-HRW-2026M07-UB`, pre-create `ace-commodities.dlq.*`). Also wire `docker compose build` + a fresh-volume init smoke into the R014 CI gate so the R015 defect class fails at PR time.

### Pattern: D1 root cause was a composition-root field omission, not a missing guard or transport — set the field where identity enters, don't weaken the check downstream (2026-06-27)
- **Context:** R028 drained the three economic-completion gaps R027 surfaced. The headline one (D1): clearing novation rejected every real gateway-submitted trade with `novation: both buyer and seller participant IDs are required`, even though cross-service Kafka transport (R024) was proven working.
- **Finding:** The transport, the orderbook copy (`order.ParticipantID` → trade), `eventbus.PublishTrade`, AND the novation guard were all correct. The defect was a single unset field three layers upstream: `matching-engine/internal/server.SubmitOrder` built `types.Order` with only `AccountID` set, leaving `ParticipantID` empty, so every matched `types.Trade` carried empty `Buyer/SellerParticipantID`. The fix populated `order.ParticipantID` (resolved from the gateway-forwarded `X-Participant-Id` header → JWT participant claim, falling back to `AccountID`) and made `orderbook.SubmitOrder` assign `order.OrderID` when empty. **The novation guard was NOT weakened** — the data feeding it was fixed. The synthetic `tests/kafka-e2e` test masked this for months because it hand-sets `P-BUY`/`P-SELL` directly on the Kafka payload; only a real gateway-submitted order exercised the unset-field path. D2 (margin had no SPAN risk params seeded for `WHT-HRW-2026M07-UB`) and D3 (`ace-commodities.dlq.*` topics didn't exist, so exhausted-retry events were dropped not dead-lettered) were the other two gaps.
- **Action:** When a downstream guard rejects valid-looking data, trace the field to the **composition root where identity/data first enters the domain object** (here, the HTTP handler building `types.Order`), not the guard or the transport — both can be correct while an intermediate constructor drops the field. Never relax a correctness guard (participant-ID-required) to make a flow pass; populate the field. Seed config/reference data (margin SPAN params via a dedicated `internal/seed` package + `MARGIN_RISK_PARAMS_FILE` env, mirroring the collateral-source convention) and pre-create operational topics (DLQ) as code/config, not in business logic. A synthetic transport test that hand-sets the very IDs the real path must produce will hide a composition-root omission — pair it with a seeded end-to-end test that sets nothing.

### Pattern: Live cross-service event verification has four non-obvious gotchas — correlate per-event, read FirstOffset, run in-network, and seed-don't-hand-set (2026-06-27)
- **Context:** R029 (`run-20260627-062805`, PASS) is the closing live proof that a real, authenticated, tenant-scoped, gateway-submitted order pair settles end-to-end matching→clearing→margin→settlement on a live `cp-kafka` broker. Its single deliverable, `tests/kafka-e2e/gateway_seeded_test.go::TestSeededGatewayTradeSettles`, surfaced four reusable gotchas for any live-stack propagation test in this codebase.
- **Finding:** (1) **Correlate each hop by what that event actually carries, not one universal token.** `trades.executed`/`clearing.novated` carry the participant IDs; `settlement.completed` re-keys correlation to the deterministic idempotency key `cycle-<tradeID>` (R024 pattern, matched by **containment** not `==`); `margin.call-issued` carries **neither trade_id nor instrument** — its only stable correlator is `participant_id` (keyed by `CallID=mc-N`). A first cut matching margin by `trade_id` produced a false "HOP 3 BROKE" while the call had genuinely fired. (2) **Read from `FirstOffset`, never `LastOffset`, on a fresh stack** — a brand-new consumer group reading "latest" races the producer and seeks past the event; reading from the start is race-free because the match token is a nanosecond-unique per-run participant ID that can't false-match prior residue. (3) **Run the Go test suites from inside the compose network** — the broker advertises `kafka:9092` (unresolvable from the host), so a host-side *consumer* can't fetch partitions; use an ephemeral `golang:1.22` container on the project network (the produce path gateway→matching is in-network and unaffected). (4) **Seed, don't hand-set** — the seeded gateway test sets distinct unique-per-run participant IDs in the *order body* (reaching the broker only via gateway→matching→eventbus) to prove the D1 *data flow*, distinct from the synthetic transport test that hand-sets `P-BUY`/`P-SELL` on the payload to prove only *transport*.
- **Action:** For any new cross-service live e2e: keep a separate watcher per topic, match each by that event's stable correlator (consult the payload shape — they differ per hop), read from `FirstOffset` with a run-unique token, execute inside the docker network, and make the assertion seeded-and-gateway-driven (set nothing the system under test must itself produce). Make `clearing.novated`+`settlement.completed` REQUIRED hops; keep `margin.call-issued` correlatable-but-gated only when a deficit is guaranteed. Adopt the handoff's follow-up: add a `trade_id` field to `margin.call-issued` so the margin hop is correlatable to the originating trade (helps both this test and operational tracing).

### Pattern: The R001–R029 remediation arc is FULLY COMPLETE — a real gateway-submitted trade settles end-to-end live; one debt (frontend fixture drift) remains, still unscheduled after ~10 runs (2026-06-27)
- **Context:** Closing the R-series. R028 drained D1/D2/D3 (economic completion); R029 is the live closing proof (`run-20260627-062805` PASS). This supersedes the earlier "R001–R027 arc is COMPLETE" pattern, which still listed R028 as an open ≤P2 debt.
- **Finding:** Economic completion is now verified live: a real, authenticated, tenant-scoped, gateway-submitted `WHT-HRW-2026M07-UB` trade flows `trades.executed` (populated buyer/seller participant **and** order IDs) → `clearing.novated` → `margin.call-issued` (seeded instrument, real deficit) → `settlement.completed` on a freshly-built, freshly-initialised stack from committed artifacts (R026 build context + R025 clean DB init, no `/tmp` workarounds). All three historical e2e bug classes are FIXED and re-confirmed live. The **only** remaining debt is the durable **26-test frontend fixture drift** (admin-ui `market-phase`/`bot-integration`, demo-runner `sections`/`t066`) — now reported PARTIAL across ~10 consecutive runs (`-040705` through `-062805`), named as an R013-class fix in every single report, and **still never scheduled in tasks.json**. Its shape has even shifted between runs (13/51 → 14/59 → 12-section variants), confirming the *data* is the moving source of truth and the fixtures lag. This is now the single longest-standing "documents-but-doesn't-fix" gap in the entire pipeline history.
- **Action:** New definitive baseline for the next pipeline: deploy pipeline is hands-off-reproducible and economically complete; latest live verdict `run-20260627-062805` PASS; ~2795 Go test funcs + 521 subtests green; 14 Go modules; race tier clean. Schedule the **R013-class frontend fixture-refresh as the very next task** — one test-only diff realigning the count assertions to current `sections.ts`/`normalizeInstruments` behavior flips every subsequent run from PARTIAL to clean, and stop re-documenting it. Enforce the same-task fixture rule mechanically: a diff touching `sections.ts` or `normalizeInstruments` is not "done" until its co-located counts-based fixture is updated in the same diff. Wire `docker compose build` + a fresh-volume init smoke + the in-network `tests/kafka-e2e`/`tests/e2e` run into the R014 CI gate so the R015-class drift/build-context/propagation defects fail at PR time rather than days later at integration.

### Pattern: Live fresh-stack tasks keep overrunning their estimate; data/seed/topic fixes price like cross-cutting work (2026-06-27)
- **Context:** R028/R029 timing. R028 (drain D1/D2/D3, code+seed+config across matching/margin/kafka-bootstrap) 35m est / ~12m actual = **2.9x (under)**; **R029 (live seeded e2e proof) 30m est / ~35m actual = 0.86x (OVER)**.
- **Finding:** R029 joins R015 (2.4x over) and R025 (the rework overrun) as the third "real fresh-volume Docker bring-up + live multi-hop e2e" task to exceed its estimate — even now that the deploy pipeline is *repaired*. The cost driver is the live verification loop itself (fresh `down -v`/`up`, in-network test container, per-hop broker inspection, the correlation-gotcha debugging), not broken infra. R028, by contrast, was a focused multi-file code/seed/config change (the D1 field fix + `internal/seed` package + `create-topics.sh` DLQ topics) and landed comfortably under its estimate like other templated cross-cutting work. The earlier prediction that "once R025/R026 land, live verification collapses toward ~1x" held for R027 (1.4x) but R029's *seeded gateway-driven* assertion was heavier than R027's transport-only check.
- **Action:** Budget any live seeded end-to-end verification at ~1x of a generous estimate (assume the unhappy path and per-hop debugging), distinct from a transport-only propagation check. Estimate data/seed/config-completion tasks (populate a field, add a seed package, add bootstrap topics) at the ~2-3x-under cross-cutting band. Keep transport-proof and economic-completion-proof as separate tasks/estimates — they have different cost profiles.

### Pattern: The architect-bot/presenter knowledge base is a THIRD metric-drift doc surface — re-derive every claim from the tree, and a stale-presenter claim is security-relevant (2026-06-27)
- **Context:** R030 refreshed `src/architect-bot/knowledge/*.md` (the "Chief Architect" presenter's sole source of truth, shown to the MSE board / FRC). The files were dated 2026-04-29 and predated the entire R001–R029 arc, so the bot was stating materially false facts: "Kafka channel-based stubs" (now a real `segmentio/kafka-go` wire bus, R024/R027/R029), "11 Go services" (now 14 modules), "resolves the tenant header before business logic" (it was *bypassed* in April, fixed by R011), and stale 2,540/887/313-test metrics. APPROVED first-pass, merged (`5e9bff7`/`6029e51`). Estimate 12m / ~5.5m actual = ~2.2x — squarely in the doc-task band (matches R013 1.2x, R016 ~2x).
- **Finding:** This is the **third** doc surface (after README/AiX in R013 and CLAUDE.md in R016) that drifted stale because metrics were copied, not re-derived. The validated discipline held: re-derive each load-bearing number from source (`ls src/*/go.mod`=14; `grep '^func Test'`=~2,795; `segmentio/kafka-go` in 18 files; `internal/eventbus` wired into engine `cmd/main.go`; `tenantBypassPrefixes` contents) and quote conservative ranges ("~94 routes", "800+ frontend tests") with an explicit "counts drift" caveat rather than brittle exact figures. Critically, a stale *presenter* claim is a **security hazard**, not cosmetic: asserting full multi-tenant data isolation as fact to an FRC/board audience invites false confidence in a boundary that is only edge-enforced (backend cross-tenant authZ is still roadmap). R030 applied the R013 "target-vs-today" labelling to keep the marketing claim honest. The worker correctly left `securities-domain.md` (pure domain reference) and `competitive-landscape.md` (no stale platform claims) untouched after verifying them — no forced edits.
- **Action:** Treat `src/architect-bot/knowledge/*.md` as a versioned, board-facing source-of-truth that MUST be re-verified against code on every refresh, exactly like README/AiX/CLAUDE.md. For any presenter/marketing doc: verify each claim against source (not the prior doc), label aspirational claims `(target)` with a `today:` status, and never assert an unenforced boundary (data isolation) as fact. **Finally adopt the recommendation now made in three consecutive doc-refresh handoffs (R013/R016/R030):** add a lightweight CI check (under R014) asserting the module-count / test-count figures in README, CLAUDE.md, and the architect-bot knowledge files stay within tolerance of the tree — this stale-metric class is the recurring failure mode these refreshes keep remediating by hand.

### Pattern: The frontend fixture-drift debt has now survived ~14 consecutive PARTIAL runs and is STILL unscheduled — the longest documents-but-doesn't-fix gap in pipeline history (2026-06-27)
- **Context:** Every integration run from `run-20260626-040705` through `run-20260627-071231` (the latest, post-R030 merge) is non-green *solely* because of the identical ~26 frontend fixture-count failures: admin-ui `market-phase.test.ts` (4: `normalizeInstruments` default phase `PRE_OPEN`→`CLOSED`, extra field, Halt-All TRADING filter) + `bot-integration.test.ts` (1: `createTicket` POST path), and demo-runner `sections.test.ts`/`t066_integration.test.ts` (21: section/step counts). All three SPAs `tsc -b` type-check and `vite build` cleanly — pure assertion drift, not a production defect.
- **Finding:** This is the same debt the 2026-06-26 ("durable test debt") and the R029-closing ("one debt remains") patterns already named as the next task to schedule — yet across ~14 runs and the entire R-series, **no R013-class fixture-refresh task was ever added to tasks.json**. It is now the single longest-standing "documents-but-doesn't-fix" gap in the pipeline's history, longer-lived than the March 6-e2e-bug loop that the `deferred` backlog was created to close. The fixture *shape* keeps shifting between runs (13/51 → 14/59 → a 12-section variant in `run-20260627-054127`/`-071231`), confirming the data files are the live source of truth and the hard-coded counts perpetually lag — and that the longer this is deferred, the larger the realignment diff grows. The PostMortem→CLAUDE.md→Planner loop demonstrably improves *future* planning but has never, on its own, *scheduled a fix for a current defect* — the structural gap the `deferred` backlog was meant to close still leaks for frontend test debt because no agent converts these PARTIAL verdicts into a `tasks.json` entry.
- **Action:** Stop re-documenting this. The PostMortem agent (or Planner) must, this cycle, append a concrete test-only `pending` task to `tasks.json`: realign admin-ui `market-phase`/`bot-integration` and demo-runner `sections`/`t066` count assertions to the current `sections.ts`/`normalizeInstruments` output (one diff, no production code). It flips every subsequent run from PARTIAL to clean. Enforce the same-task fixture rule mechanically going forward: a diff touching `sections.ts` or `normalizeInstruments` is not "done" until its co-located counts-based fixture is updated in the same diff. Treat a recurring PARTIAL-on-fixture-drift verdict as an obligation to *create the fix task*, not merely note it — the same escalation that finally closed the gateway-tenant P1 after four FAIL runs.

<!-- LEARNED PATTERNS END — do not remove this comment -->

---

## File Layout Convention

```
project-root/
  CLAUDE.md              ← this file (session memory + learned patterns)
  tasks.json             ← current task graph
  handoff/               ← agent-to-agent communication
    task-<id>.md
    task-<id>-review.md
    integration-<run>.md
  pipeline/              ← AI pipeline orchestrator
    run.sh               ← main entry point (./pipeline/run.sh "requirement")
    lib/                 ← shell libraries (state, context, worktree, log)
    prompts/             ← agent role prompt templates
  venues/
    ace-commodities/   ← commodity exchange tenant config, trading calendar, listing rules
    mse-equities/      ← MSE flagship tenant config, FRC reporting templates
  src/                   ← application code
  tests/                 ← test suite
```

---

*This file is both documentation and executable memory.
Keep it committed. Keep it accurate. The pipeline is only as good as what is written here.*
