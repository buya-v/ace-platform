apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: garudax-platform-ingress
  namespace: garudax
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "86400"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "86400"
    nginx.ingress.kubernetes.io/websocket-services: "gateway"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - garudax.asla.mn
      secretName: garudax-asla-mn-tls
    - hosts:
        - admin.garudax.asla.mn
      secretName: admin-garudax-asla-mn-tls
    - hosts:
        - trade.garudax.asla.mn
      secretName: trade-garudax-asla-mn-tls
    - hosts:
        - demo.garudax.asla.mn
      secretName: demo-garudax-asla-mn-tls
  rules:
    # ── garudax.asla.mn — API + redirect root to trade ────────────────────
    - host: garudax.asla.mn
      http:
        paths:
          - path: /api/
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 8080
          - path: /healthz
            pathType: Exact
            backend:
              service:
                name: gateway
                port:
                  number: 8080
          - path: /readyz
            pathType: Exact
            backend:
              service:
                name: gateway
                port:
                  number: 8080
    # ── admin.garudax.asla.mn — Admin Dashboard ───────────────────────────
    - host: admin.garudax.asla.mn
      http:
        paths:
          - path: /api/
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin-ui
                port:
                  number: 3001
    # ── trade.garudax.asla.mn — Trading Web UI ────────────────────────────
    - host: trade.garudax.asla.mn
      http:
        paths:
          - path: /api/
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-ui
                port:
                  number: 3000
    # ── demo.garudax.asla.mn — Demo Runner ────────────────────────────────
    - host: demo.garudax.asla.mn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-runner
                port:
                  number: 3002
