apiVersion: apps/v1
kind: Deployment
metadata:
  name: matching-engine
  namespace: garudax-exchange
  labels:
    app.kubernetes.io/name: matching-engine
    app.kubernetes.io/part-of: garudax-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: matching-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: matching-engine
        app.kubernetes.io/part-of: garudax-platform
    spec:
      serviceAccountName: matching-engine
      terminationGracePeriodSeconds: 30
      containers:
        - name: matching-engine
          image: garudax-platform/matching-engine:latest
          ports:
            - name: grpc
              containerPort: 50051
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          envFrom:
            - configMapRef:
                name: service-discovery
            - secretRef:
                name: garudax-db-credentials
            - secretRef:
                name: garudax-jwt-signing-key
                optional: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
