apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-ui
  namespace: ace-services
  labels:
    app.kubernetes.io/name: web-ui
    app.kubernetes.io/part-of: ace-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: web-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: web-ui
        app.kubernetes.io/part-of: ace-platform
    spec:
      containers:
        - name: web-ui
          image: ace-platform/web-ui:latest
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          env:
            - name: API_BASE_URL
              value: "http://gateway.ace-services.svc.cluster.local:8080"
