apiVersion: apps/v1
kind: Deployment
metadata:
  name: warehouse-service
  namespace: garudax-services
  labels:
    app.kubernetes.io/name: warehouse-service
    app.kubernetes.io/part-of: garudax-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: warehouse-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: warehouse-service
        app.kubernetes.io/part-of: garudax-platform
    spec:
      serviceAccountName: warehouse-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: warehouse-service
          image: garudax-platform/warehouse-service:latest
          ports:
            - name: grpc
              containerPort: 50058
              protocol: TCP
            - name: health
              containerPort: 8088
              protocol: TCP
          envFrom:
            - configMapRef:
                name: service-discovery
            - secretRef:
                name: garudax-db-credentials
            - secretRef:
                name: garudax-jwt-signing-key
                optional: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
