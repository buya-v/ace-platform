apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: garudax-services
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: garudax-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: auth-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: auth-service
        app.kubernetes.io/part-of: garudax-platform
    spec:
      serviceAccountName: auth-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: auth-service
          image: garudax-platform/auth-service:latest
          ports:
            - name: grpc
              containerPort: 50055
              protocol: TCP
            - name: health
              containerPort: 8085
              protocol: TCP
          envFrom:
            - configMapRef:
                name: service-discovery
            - secretRef:
                name: garudax-db-credentials
            - secretRef:
                name: garudax-jwt-signing-key
                optional: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
