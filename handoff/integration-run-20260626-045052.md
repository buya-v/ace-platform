# Integration Test — Run run-20260626-045052

**Date:** 2026-06-26T05:27:43Z
**Result:** PARTIAL  *(strict binary = FAIL — see verdict note)*

---

## Verdict Note

- **All builds green** (16/16 Go modules, 3/3 frontend SPAs `tsc`+`vite build`).
- **All backend tests green**: 16/16 Go modules pass, 0 failures; race tier 0 data races; e2e against the **live full stack** 0 failures.
- **Only failures are 26 frontend unit tests**, all of which are hard-coded **fixture-count / assertion drift** (the implementation data files advanced; the co-located tests lagged). Every affected SPA type-checks and builds cleanly, so this is **test debt, not a production defect**.
- Per the documented verdict-granularity pattern (green builds + green backend + only frontend fixture drift → `PARTIAL`), this run is reported **PARTIAL**. Under a strict PASS/FAIL binary it is **FAIL** solely because 26 tests fail.

---

## Build Results

| Service | Status | Notes |
|---------|--------|-------|
| auth-service | PASS | `go build ./...` clean (GOTOOLCHAIN=auto → go1.25) |
| clearing-engine | PASS | |
| compliance-service | PASS | |
| corporate-actions | PASS | |
| fix-gateway | PASS | |
| gateway | PASS | |
| margin-engine | PASS | |
| market-data-service | PASS | |
| matching-engine | PASS | |
| platform-service | PASS | |
| securities-service | PASS | |
| settlement-engine | PASS | |
| shared | PASS | |
| warehouse-service | PASS | |
| shared/pkg/tenant | PASS | zero-dep go1.22 module |
| shared/pkg/types/decimal | PASS | zero-dep go1.22 module |
| web-ui (SPA) | PASS | `tsc -b && vite build`, 0 errors |
| admin-ui (SPA) | PASS | `tsc && vite build`, 0 errors |
| demo-runner (SPA) | PASS | `tsc -b && vite build`, 0 errors |

**Build totals: 19/19 buildable artifacts PASS (16 Go modules + 3 SPAs).**
`architect-bot` has no test/build script — not a buildable artifact in this run.

> Toolchain note: host is go1.22.5; 12 modules declare `go 1.25.x`, satisfied via `GOTOOLCHAIN=auto` (network fetch of go1.25.0 — known deviation from the zero-network ideal). Build/test pass/fail measured with `-cover` off, because the auto-fetched go1.25 toolchain is missing the `covdata` tool, which spuriously exits non-zero on packages with **no test files** when `-cover` is set. Coverage numbers below were read from the per-package `coverage:` lines of the `-cover` run (those emit correctly).

## Test Results

| Suite | Passed | Failed | Skipped | Coverage |
|-------|--------|--------|---------|----------|
| Go unit/integration (16 modules) | all | 0 | — | ~69.9% avg over 95 tested pkgs |
| Go race tier (`-race`, 4 engines) | 4 mods | 0 | — | 0 data races |
| E2E (live stack, `tests/e2e`) | 32 top-level / 173 incl. subtests | 0 | 8 | gateway :8080 reachable |
| Frontend — web-ui (vitest) | 157 | 0 | 0 | builds clean |
| Frontend — admin-ui (vitest) | 547 | **5** | 0 | builds clean |
| Frontend — demo-runner (vitest) | 113 | **21** | 0 | builds clean |

**Go test inventory:** 2,769 `func Test*` + 515 `t.Run` subtests + 3 benchmarks.
**Frontend test inventory:** 843 tests (157 + 552 + 134); 817 pass, **26 fail**, 0 skip.
**E2E:** 32 top-level tests (all pass) / 181 RUN lines (173 pass, 0 fail, 8 graceful skip) against the running stack (all 10 backend containers + gateway up).

### Go per-module coverage (avg of tested packages)

| Module | Coverage | Module | Coverage |
|--------|----------|--------|----------|
| auth-service | 55.2% (4) | platform-service | 88.4% (4) |
| clearing-engine | 84.1% (8) | securities-service | 73.7% (7) |
| compliance-service | 73.7% (10) | settlement-engine | 84.3% (8) |
| corporate-actions | 100.0% (1) | shared | 76.2% (4) |
| fix-gateway | 82.1% (5) | warehouse-service | 44.8% (4) |
| gateway | 43.8% (17) | shared/pkg/tenant | 100.0% (1) |
| margin-engine | 80.9% (9) | shared/pkg/types/decimal | 92.1% (1) |
| market-data-service | 71.7% (6) | matching-engine | 62.7% (6) |

**Overall: 69.9% average across 95 tested packages** (unweighted package average). Lowest modules (gateway 43.8%, warehouse 44.8%) are dragged down by 0%-covered `cmd/`, `server/`, and `types/` packages, consistent with prior baselines.

## Failures

All 26 failures are in two frontend SPAs; all are fixture-count/shape assertion drift. No logic or type-check breakage (all three SPAs `npm run build` clean, 0 errors).

**admin-ui — 5 failures** (`src/__tests__/`):
- `market-phase.test.ts` (4): `normalizeInstruments` — instruments-from-`{instruments:[…]}`, instruments-from-array, default-phase-when-missing (test asserts `PRE_OPEN`, impl now `CLOSED`); `Halt All … only targets TRADING instruments`.
- `bot-integration.test.ts` (1): `createTicket > sends POST to /bot/tickets with ticket data`.

**demo-runner — 21 failures** (`src/__tests__/`):
- `sections.test.ts` + `t066_integration.test.ts`: assert 13 sections / 51 steps, but merged `sections.ts` data now has **14 sections / 59 steps** (new `admin-orderbook` / `ob-` steps, settlement-trigger `extractState`, circuit-breaker PUT body, auth-header expectations).

These are the exact failures documented in the 2026-06-26 "frontend fixture drift is now the sole FAIL cause" pattern. Root cause: feature/data edits were merged without updating the co-located counts-based fixtures. **Fix is a test-only fixture-refresh task** (realign admin-ui `market-phase`/`bot-integration` and demo-runner `sections`/`t066` to current data); no production code change required.

## Summary

The platform is healthy: 19/19 build artifacts compile, all 2,769+ Go tests pass with 0 failures and 0 data races, and the full e2e suite passes 0-fail against a live, fully-running stack (tenant-enforcement invariant verified). The run is non-green only because of 26 stale frontend fixture-count assertions (test debt) in admin-ui and demo-runner — all three SPAs still type-check and build cleanly — so the substantive verdict is **PARTIAL** (strict-binary **FAIL** attributable entirely to frontend test drift, not to any production defect).
