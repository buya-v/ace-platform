# Integration Test — Run run-20260626-033236

**Date:** 2026-06-26T04:02:00Z
**Result:** FAIL

---

## Summary

Builds are fully green — all 19 Go modules and all 3 React SPAs compile cleanly. The newly
merged work since the last run (R007 Kafka fail-fast, R008 engine concurrency fixes, R009
concurrency race-gate) is healthy: the four trading engines now pass cleanly under the Go
**race detector** (`go test -race`), confirming the R008 fixes hold. However, the overall
verdict is **FAIL**, unchanged from the two prior runs (`run-20260626-012134`,
`run-20260626-022823`): the same **1 real Go defect** (gateway tenant-enforcement middleware,
3 failing tests) plus **26 frontend assertion failures** (admin-ui 5, demo-runner 21) that are
test-vs-implementation drift from merged data/UI edits. No regression and no new fixes — the
P1 gateway tenant-scoping defect (deferred tasks R011/R012) is still open.

---

## Build Results

| Service / App | Status | Notes |
|---|---|---|
| auth-service | PASS | `go build ./...` clean |
| clearing-engine | PASS | clean |
| compliance-service | PASS | clean |
| corporate-actions | PASS | clean |
| fix-gateway | PASS | clean |
| gateway | PASS | builds clean (test failure below) |
| margin-engine | PASS | clean |
| market-data-service | PASS | clean |
| matching-engine | PASS | clean |
| platform-control | PASS | clean |
| platform-service | PASS | clean |
| securities-service | PASS | clean |
| settlement-engine | PASS | clean |
| shared | PASS | clean |
| shared/pkg/types/decimal | PASS | clean (nested module) |
| warehouse-service | PASS | clean |
| tests/e2e | PASS | clean |
| tests/load | PASS | clean |
| tests/reconciliation | PASS | clean |
| web-ui (SPA) | PASS | vitest/Vite toolchain |
| admin-ui (SPA) | PASS | builds |
| demo-runner (SPA) | PASS | builds |

**Build verdict: 19/19 Go modules + 3/3 SPAs PASS.**

> Notes:
> - Host toolchain go1.22.5; all modules build without a network toolchain fetch.
> - `architect-bot` / `admin-bot` are plain Node servers with no build/test scripts — nothing to run.

---

## Test Results

### Go modules (`go test ./...`)

| Module | Status | Coverage (avg across tested pkgs) |
|---|---|---|
| auth-service | PASS | — |
| clearing-engine | PASS | 84.1% |
| compliance-service | PASS | — |
| corporate-actions | PASS | ~100% |
| fix-gateway | PASS | — |
| **gateway** | **FAIL** | 43.6% avg; 3 tenant-middleware tests fail (see Failures) |
| margin-engine | PASS | 80.9% |
| market-data-service | PASS | — |
| matching-engine | PASS | 62.7% |
| platform-control | PASS | — |
| platform-service | PASS | — |
| securities-service | PASS | — |
| settlement-engine | PASS | 84.3% |
| shared | PASS | 76.2% |
| shared/pkg/types/decimal | PASS | 92.1% |
| warehouse-service | PASS | — |
| tests/e2e | PASS | graceful-skip (no live gateway) |
| tests/reconciliation | PASS | — |
| tests/load | PASS | builds & runs as tests |

**Go business-logic coverage:** ~70% simple-mean across tested packages (statement-weighted
per package), materially unchanged from the prior run. `cmd/`, `server/`, and most `store/`
packages are intentionally low/untested. Coverage methodology: business-logic packages only;
`cmd/`/`server/`/`config/` excluded per the standing CLAUDE.md convention.

### Race detector (R008/R009 verification — `go test -race ./...`)

| Engine | Status |
|---|---|
| matching-engine | PASS (race) |
| clearing-engine | PASS (race) |
| margin-engine | PASS (race) |
| settlement-engine | PASS (race) |

All four engines are clean under the race detector — no data races reported. This confirms the
R008 concurrency fixes (handler-in-lock, map race, handler field sync) and the R009 race-gate.

### Frontend SPAs (`vitest run`)

| Suite | Passed | Failed | Skipped | Total |
|---|---|---|---|---|
| web-ui | 157 | 0 | 0 | 157 |
| admin-ui | 547 | 5 | 0 | 552 |
| demo-runner | 113 | 21 | 0 | 134 |

**Aggregate frontend: 817 passed / 26 failed / 843 total.**

---

## Failures

### 1. gateway — `internal/middleware` tenant enforcement (3 failures) — REAL DEFECT (P1)

```
--- FAIL: TestTenantMiddleware_ValidTenant    (expected tenant in context, got "")
--- FAIL: TestTenantMiddleware_MissingHeader   tenant_test.go:32 handler should not have been called
                                               tenant_test.go:40 expected 401, got 200
--- FAIL: TestTenantMiddleware_UnknownTenant   tenant_test.go:47 handler should not have been called
                                               tenant_test.go:56 expected 403, got 200
FAIL  github.com/garudax-platform/gateway/internal/middleware
```

**Root cause:** `src/gateway/internal/middleware/tenant.go` `tenantBypassPrefixes` whitelists
essentially every tenant-scoped API path (`/api/v1/orders`, `/api/v1/margin`,
`/api/v1/participants`, `/api/v1/tickets`, plus clearing/settlement/warehouse/market-data).
The tests exercise `/api/v1/orders`, which matches a bypass prefix, so `TenantMiddleware` skips
header validation and calls the downstream handler with **no tenant in context** — returning
200 where 401/403 is required.

**Impact:** Defeats tenant scoping at the gateway and contradicts the platform invariant
("Tenant ID is never optional" / "no service accepts traffic with an unresolved tenant",
2026-04-23 multi-tenancy directive). Security-relevant correctness defect, not test drift.
Tracked by deferred backlog tasks **R011/R012**. **Not fixed by this agent (report-only).**

### 2. admin-ui — `market-phase.test.ts` (4) + `bot-integration.test.ts` (1) — test/impl drift

```
FAIL bot-integration.test.ts > createTicket sends POST to /bot/tickets with ticket data
FAIL market-phase.test.ts   > normalizeInstruments from { instruments: [...] } response
FAIL market-phase.test.ts   > normalizeInstruments from array response
FAIL market-phase.test.ts   > defaults phase to PRE_OPEN when missing
FAIL market-phase.test.ts   > Halt All only targets TRADING instruments
```

`normalizeInstruments` field-mapping/default-phase behavior and the bot `createTicket`
path/payload diverge from the suite's expectations — recently merged implementation changes the
tests were not updated for.

### 3. demo-runner — `sections.test.ts` (9) + `t066_integration.test.ts` (12) — data/test drift

The `sections.ts` demo-script data was changed (section count 12 vs expected 14, total step
count 59 vs expected 51, per-section step counts) but assertions still encode the prior fixture
values:

```
sections > has 14 sections                       → got 12
sections > environment setup has 4 steps         → got 3
T066 > allSections has exactly 14 entries         → got 12
T066 > total step count is 51                     → got 59
T066 > new admin sections / step IDs              → fixtures out of date
```

21 assertions across two suites encode stale fixture counts. The modified
`src/demo-runner/tsconfig.tsbuildinfo` in git status corroborates a recent demo-runner edit;
the data appears newer than the tests.

---

## Recommendation

- **P1 (correctness/security):** Fix gateway `tenantBypassPrefixes` — narrow it to genuinely
  platform-level paths only (`/platform/`, `/api/v1/platform/`, `/api/v1/auth/`, health) and
  require `X-GarudaX-Tenant` on all tenant-scoped routes (deferred tasks R011/R012). This is a
  multi-tenancy invariant violation and should precede further feature work.
- **P2:** Reconcile admin-ui `market-phase`/`bot-integration` and demo-runner `sections`/`t066`
  suites with their merged implementations — code looks newer than tests; update the lagging side.
- Playwright UAT specs under `tests/playwright/` were **not executed** — they need a live full
  stack + browsers (out of scope for this static build/test run).

**Overall health:** Build pipeline is healthy (19/19 Go modules + 3/3 SPAs compile) and the
newly merged R007–R009 concurrency work is verified clean under the race detector. Test suite is
**FAIL** — 1 real gateway tenant-enforcement defect plus 26 frontend assertions out of sync with
merged code. No regression vs the two prior runs; identical failure set.
