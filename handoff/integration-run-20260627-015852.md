# Integration Test — Run run-20260627-015852

**Date:** 2026-06-27T02:30:00Z
**Result:** PARTIAL

> Strict template verdict would be **FAIL** (the frontend suites have failing tests),
> but per the documented integration-verdict-granularity guidance in CLAUDE.md
> (e2e-canary / fixture-drift patterns), this run is reported **PARTIAL**: every build
> is green, the entire Go backend and all three SPA production builds pass, no P1
> defects are present, and the only failures are the long-standing **frontend
> fixture-count drift** (test debt, not a production defect). See Failures section.

---

## Build Results

| Service / Module | Status | Notes |
|------------------|--------|-------|
| src/auth-service | PASS | go 1.25 (GOTOOLCHAIN=auto) |
| src/clearing-engine | PASS | |
| src/compliance-service | PASS | |
| src/corporate-actions | PASS | library module (go 1.22) |
| src/fix-gateway | PASS | |
| src/gateway | PASS | go 1.22 |
| src/margin-engine | PASS | |
| src/market-data-service | PASS | |
| src/matching-engine | PASS | |
| src/platform-service | PASS | |
| src/securities-service | PASS | |
| src/settlement-engine | PASS | |
| src/shared | PASS | go 1.24 |
| src/shared/pkg/tenant | PASS | shared zero-dep module |
| src/shared/pkg/types/decimal | PASS | shared zero-dep module |
| src/warehouse-service | PASS | |
| web-ui (SPA) | PASS | tsc + vite, 0 errors |
| admin-ui (SPA) | PASS | tsc + vite, 0 errors |
| demo-runner (SPA) | PASS | tsc + vite, 0 errors |

**Build totals: 19/19 buildable artifacts PASS** (16 Go modules + 3 React SPAs).
`architect-bot` has no test/build script and is excluded.

## Test Results

| Suite | Passed | Failed | Skipped | Coverage |
|-------|--------|--------|---------|----------|
| Go — auth-service | all | 0 | — | 46.5% (4 pkgs) |
| Go — clearing-engine | all | 0 | — | 82.6% (9 pkgs) |
| Go — compliance-service | all | 0 | — | 73.7% (10 pkgs) |
| Go — corporate-actions | all | 0 | — | 100.0% |
| Go — fix-gateway | all | 0 | — | 82.1% (5 pkgs) |
| Go — gateway | all | 0 | — | 55.2% |
| Go — margin-engine | all | 0 | — | 79.6% (10 pkgs) |
| Go — market-data-service | all | 0 | — | 71.7% (6 pkgs) |
| Go — matching-engine | all | 0 | — | 63.6% (7 pkgs) |
| Go — platform-service | all | 0 | — | 88.4% (4 pkgs) |
| Go — securities-service | all | 0 | — | 73.7% (7 pkgs) |
| Go — settlement-engine | all | 0 | — | 83.4% (9 pkgs) |
| Go — shared | all | 0 | — | 69.0% |
| Go — shared/pkg/tenant | all | 0 | — | 100.0% |
| Go — shared/pkg/types/decimal | all | 0 | — | 92.1% |
| Go — warehouse-service | all | 0 | — | 44.8% (4 pkgs) |
| Go — tests/e2e | — | 0 | 32 | graceful skip (no live gateway) |
| Go — tests/reconciliation | all | 0 | — | cross-service cash-leg reconcile |
| Frontend — web-ui | 157 | 0 | 0 | (157 total) |
| Frontend — admin-ui | 547 | **5** | 0 | (552 total) |
| Frontend — demo-runner | 113 | **21** | 0 | (134 total) |

**Go:** 16/16 modules PASS (build & test). 2785 `func Test*` + 515 `t.Run` subtests.
**Frontend:** 817 passed / **26 failed** / 843 total.
**e2e:** 32 top-level tests, all SKIP (graceful-skip without a running gateway).
**Coverage (Go):** combined package-level average ≈ **74.6%**; business-logic packages
range 44.8%–100%. (Statement-weighted whole-suite merge could not be produced — see Notes.)

## Failures (if any)

All 26 failures are in two frontend suites and are **fixture-count assertion drift**, the
exact, repeatedly-documented test-debt class in CLAUDE.md (2026-06-26 "Frontend fixture
drift is now durable test debt"). The implementation is the source of truth — every SPA
`tsc -b` type-checks and `vite build`s with **zero errors**; the co-located counts-based
fixtures simply lag the merged data/logic changes.

**admin-ui (5 failures):**
- `market-phase.test.ts` (4): `normalizeInstruments` now defaults a missing phase to
  `CLOSED` (tests still assert `PRE_OPEN`), plus an extra field and the Halt-All
  `TRADING`-filter shape changed.
- `bot-integration.test.ts` (1): `createTicket` POST path changed.

**demo-runner (21 failures):**
- `sections.test.ts` + `t066_integration.test.ts`: data grew to **14 sections / 59 steps**
  (new `admin-orderbook` / `ob-*` steps), but fixtures still assert the old **13 sections /
  51 steps**.

No backend, build, race, reconciliation, or e2e failures. No P1/security defects.

## Notes

- **Toolchain:** host Go is 1.22.5 but most modules declare `go 1.25.0`; builds/tests use
  `GOTOOLCHAIN=auto` (downloads go1.25.x), as documented.
- **Coverage methodology caveat:** the downloaded go1.25 toolchains are missing the
  `covdata` tool, so `go test ./... -cover` (which merges across packages) aborts with
  `go: no such tool "covdata"`. This is an **environment/tooling limitation, not a test
  failure** — verified by re-running every module without `-cover` (all PASS). Coverage was
  therefore collected per-package (single-package coverage needs no merge) and reported as a
  package-count average per module. go1.22/1.24 modules (corporate-actions, gateway, shared,
  tenant, decimal) report true statement-weighted totals. Recommend a follow-up to ensure
  `covdata` ships with the auto-downloaded toolchain (or pin a complete local go1.25) so the
  canonical statement-weighted figure can be produced.
- Per the rules, no code or tests were modified — this run is report-only.

## Summary

The platform is substantively healthy: all 19 build artifacts compile, all 16 Go modules
and both cross-service Go integration suites (e2e graceful-skip, reconciliation) pass, the
race-fixed engines build clean, and Go coverage holds at ~74.6% package-level. The only
defects are 26 frontend **fixture-count drift** failures (5 admin-ui, 21 demo-runner) — known,
durable test debt where hard-coded fixtures lag the implementation, not a production
regression — which is why the verdict is **PARTIAL** rather than PASS. Recommended follow-up:
schedule the long-deferred R013-class fixture-refresh task to realign admin-ui
`market-phase`/`bot-integration` and demo-runner `sections`/`t066` assertions, which flips
this run to fully green.
