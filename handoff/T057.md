# Handoff — T057: Demo Runbook Document

**Status:** DONE
**Agent:** Coder
**Phase:** 6

---

## Summary
Created `docs/DEMO_RUNBOOK.md` — a comprehensive 9-section step-by-step guide for demonstrating the full ACE platform. All curl commands and API routes were derived from reading the actual source code (gateway routes, auth-service handlers, matching-engine types, warehouse-service models, websocket handlers). The document covers environment setup, auth/KYC flow, trading, post-trade, physical delivery, market data, compliance/risk, admin operations, and a production readiness checklist.

## Key Decisions
1. Used `/api/v1/` prefix consistently — this is what the gateway router registers (not `/v1/` as some specs suggested).
2. Documented the `AccessToken` vs `access_token` casing issue (missing json struct tags on TokenPair) and provided a jq workaround.
3. Noted that warehouse-service and market-data-service have no gateway REST routes — documented gRPC-direct access via `grpcurl`.
4. Flagged the missing `POST /settlement/cycle` endpoint as a known gap with a grpcurl workaround.
5. Used integer enum values for order fields (side: 1=BUY, order_type: 1=LIMIT, etc.) matching the Go type definitions.
6. Included a complete API route reference table at the end covering all 50+ gateway endpoints.

## Blockers Found
None

## Suggested Follow-ups
- Add gateway REST routes for warehouse-service and market-data-service (candles, ticker, receipts, deliveries)
- Add `POST /api/v1/settlement/cycle` endpoint in gateway to trigger settlement
- Add `json:` struct tags to auth-service `TokenPair` struct for consistent API contract
- Performance benchmark suite (mentioned as gap in Section 9.4)
- Prometheus `/metrics` endpoints on all services (mentioned as gap in Section 9.5)

## Deliverables
- `docs/DEMO_RUNBOOK.md` — comprehensive demo runbook (9 sections + API reference)
- `handoff/T057.md` — this file
