# Handoff — T042: Clean Up Service Naming Stubs

**Status:** DONE
**Agent:** Coder
**Phase:** 4

---

## Summary
Removed the stub `src/clearing-service/` directory which contained only a `.gitkeep` file and conflicted with the real implementation at `src/clearing-engine/`. Verified all other `src/*/` directories: `auth-service/` is a stub (`.gitkeep` only) but has no conflicting counterpart — it is the correct location for T040 to populate with real code. All remaining directories contain real Go source code (`go.mod`, `cmd/`, `internal/`).

## Key Decisions
1. Removed `src/clearing-service/` — it was a stub (`.gitkeep` only) conflicting with real code at `src/clearing-engine/`.
2. Kept `src/auth-service/` — although it's also a stub, there is no `auth-engine/` counterpart. T040 will rebuild it with real code at this location.
3. No config file updates needed — no docker-compose.yml, Makefile, or CI workflow files reference `clearing-service`.

## Blockers Found
None

## Suggested Follow-ups
- T040 (Auth Service rebuild) should populate `src/auth-service/` with real code.
- Consider documenting the naming convention in a project ADR: `*-engine` for real-time trading pipeline services, `*-service` for supporting services, bare name for `gateway`.

## Deliverables
- Removed: `src/clearing-service/.gitkeep` (and the directory)
- Created: `handoff/T042.md`
