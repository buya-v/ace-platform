# Integration Test — Run run-20260627-054127

**Date:** 2026-06-27T05:41:27Z
**Result:** PARTIAL

> All builds green, all 16 Go modules pass (0 failures), race tier clean, and 1 of 3
> SPAs fully green. The only failures are the **26 pre-existing frontend fixture-drift
> tests** (admin-ui `market-phase`/`bot-integration`, demo-runner `sections`/`t066`) that
> have been documented across ~8 consecutive runs and are test debt, not production
> defects (every SPA `tsc -b` type-checks and builds clean). Per the established
> verdict-granularity pattern, this is reported **PARTIAL**, not FAIL.

---

## Build Results

| Service | Status | Notes |
|---------|--------|-------|
| auth-service | PASS | go build ./... |
| clearing-engine | PASS | go build ./... |
| compliance-service | PASS | go build ./... |
| corporate-actions | PASS | library module |
| fix-gateway | PASS | go build ./... |
| gateway | PASS | go build ./... |
| margin-engine | PASS | go build ./... |
| market-data-service | PASS | go build ./... |
| matching-engine | PASS | go build ./... |
| platform-service | PASS | go build ./... |
| securities-service | PASS | go build ./... |
| settlement-engine | PASS | go build ./... |
| warehouse-service | PASS | go build ./... |
| shared | PASS | go build ./... |
| shared/pkg/tenant | PASS | zero-dep submodule |
| shared/pkg/types/decimal | PASS | zero-dep submodule |
| web-ui (SPA) | PASS | `tsc -b` clean + Vite |
| admin-ui (SPA) | PASS | `tsc -b` clean + Vite |
| demo-runner (SPA) | PASS | `tsc -b` clean + Vite |

**Build totals: 16/16 Go modules build · 3/3 SPAs type-check & build.**
Toolchain note: 11 modules declare `go 1.25.0`; host is `go1.22.5`, so builds run with
`GOTOOLCHAIN=auto` (fetches go1.25). The `go: no such tool "covdata"` message seen under
`go test -cover` on no-test packages is a coverage-summary artifact of the fetched
toolchain — **not** a build or test failure (confirmed by clean `go build` and by
re-running `go test` without `-cover`).

## Test Results

### Go (backend) — 16/16 modules PASS, 0 failures

| Module | Passed pkgs | Failed tests | Mean stmt coverage (test-bearing pkgs) |
|--------|-------------|--------------|-----------------------------------------|
| auth-service | 4 | 0 | 46.5% |
| clearing-engine | 9 | 0 | 82.6% |
| compliance-service | 10 | 0 | 73.7% |
| corporate-actions | 1 | 0 | 100.0% |
| fix-gateway | 5 | 0 | 82.1% |
| gateway | 12 | 0 | 43.7% |
| margin-engine | 11 | 0 | 80.6% |
| market-data-service | 6 | 0 | 71.7% |
| matching-engine | 7 | 0 | 65.4% |
| platform-service | 4 | 0 | 88.4% |
| securities-service | 7 | 0 | 73.7% |
| settlement-engine | 9 | 0 | 83.4% |
| warehouse-service | 4 | 0 | 44.8% |
| shared | 4 | 0 | 76.2% |
| shared/pkg/tenant | 1 | 0 | 100.0% |
| shared/pkg/types/decimal | 1 | 0 | 92.1% |

Go test corpus: **2795 `func Test*` + 521 `t.Run` subtests**, all passing.
(Coverage column = arithmetic mean of per-package statement coverage across packages that
have tests; it excludes no-test `cmd/`/`config/`/`server/`/`types/` packages, so it tracks
business-logic coverage rather than the lower all-packages number.)

### Concurrency (race tier) — 4/4 engines PASS

| Engine | `go test -race ./internal/engine/...` | Data races |
|--------|----------------------------------------|------------|
| matching-engine | PASS | 0 |
| clearing-engine | PASS | 0 |
| margin-engine | PASS | 0 |
| settlement-engine | PASS | 0 |

### Frontend (SPA unit/Vitest)

| Suite | Passed | Failed | Total | Status |
|-------|--------|--------|-------|--------|
| web-ui | 157 | 0 | 157 | PASS |
| admin-ui | 547 | 5 | 552 | FAIL (fixture drift) |
| demo-runner | 113 | 21 | 134 | FAIL (fixture drift) |

### Tiers not executed this run

| Tier | Status | Reason |
|------|--------|--------|
| HTTP e2e (`tests/e2e`) | NOT RUN | No live stack (`docker compose` down, gateway :8080 unreachable). |
| Kafka cross-service e2e (`tests/kafka-e2e`) | NOT RUN | Requires live broker + engines. |

This is a source-tier integration run (build + unit + race). The most recent **live
full-stack** verification was R027 `run-20260627-021403` (**PASS**), which confirmed clean
DB init, image rebuild from committed Dockerfiles, and cross-service Kafka propagation.

## Failures

All 26 failures are **pre-existing frontend fixture-count/shape drift** — the
implementation (data files + `normalizeInstruments`) advanced via merged edits while the
co-located, count-based test fixtures lagged. All three SPAs `tsc -b` type-check and Vite
build cleanly, so this is assertion drift, not logic or type breakage.

**admin-ui (5):**
- `market-phase.test.ts` (4) — `normalizeInstruments` now defaults a missing phase to
  `CLOSED` (tests assert `PRE_OPEN`), adds a field, and the Halt-All filter targets only
  `TRADING` instruments.
- `bot-integration.test.ts` (1) — `createTicket` POST path expectation lags implementation.

**demo-runner (21):**
- `sections.test.ts` + `t066_integration.test.ts` — fixtures assert **14 sections / 51
  steps** but the current `sections.ts` data resolves to **12 sections** (e.g.
  `expected … to have a length of 14 but got 12`; `total step count is 51` mismatch).
  Note the data shape has shifted again relative to the last documented 14/59 state — the
  fixtures and data must be reconciled to a single source of truth in one task.

No stack traces — all failures are Vitest assertion mismatches (`expected X to be Y`).

## Summary

Platform is substantively healthy: **16/16 Go modules build and pass (0 failures, 2795
test funcs + 521 subtests), the race tier is clean on all four engines, and web-ui is fully
green.** The run is PARTIAL solely because of the long-standing 26-test frontend
fixture drift in admin-ui and demo-runner — test debt with green builds/type-checks, not a
production defect. Recommended action (per CLAUDE.md): schedule the single test-only
R013-class fixture-refresh task to realign admin-ui `market-phase`/`bot-integration` and
demo-runner `sections`/`t066` assertions with current data, which flips this run to clean.
