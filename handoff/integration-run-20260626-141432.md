# Integration Test — Run run-20260626-141432

**Date:** 2026-06-26T14:14:32Z
**Result:** PARTIAL — all 16 Go modules + 4 test modules + 3 SPAs build clean; the entire Go test suite passes (2785 test funcs + 515 subtests, 0 failures); the **only** failures are the 26 known frontend fixture-count drift assertions (test debt, not production defects). Per the documented e2e-canary verdict-granularity pattern (green builds + green backend + only fixture drift → PARTIAL, not FAIL).

**Toolchain:** host `go1.22.5`; modules declaring `go 1.25.0`/`go 1.24.0` built/tested via `GOTOOLCHAIN=auto` (network-fetched go1.25). Node `v22.22.0`, npm 10.x, Vitest/Vite for SPAs.

**Scope note:** This run exercised the **source tiers**: Go build, Go unit/integration tests (`go test ./...`), and frontend Vitest + Vite builds. The live-stack tiers (`tests/e2e`, `tests/kafka-e2e`, `tests/race` against a running `docker compose` stack) were **build-verified only** — they compile cleanly but were not executed against a live gateway/Kafka broker in this run. See "Not Executed" below.

---

## Build Results

| Service / Module | Status | Notes |
|------------------|--------|-------|
| auth-service | PASS | `go build ./...` clean |
| clearing-engine | PASS | |
| compliance-service | PASS | |
| corporate-actions | PASS | |
| fix-gateway | PASS | |
| gateway | PASS | |
| margin-engine | PASS | |
| market-data-service | PASS | |
| matching-engine | PASS | |
| platform-service | PASS | |
| securities-service | PASS | |
| settlement-engine | PASS | |
| warehouse-service | PASS | |
| shared | PASS | |
| shared/pkg/tenant | PASS | zero-dep `go 1.22` module |
| shared/pkg/types/decimal | PASS | zero-dep `go 1.22` module |
| tests/e2e | PASS | compiles (not run against live stack) |
| tests/kafka-e2e | PASS | compiles (not run against live stack) |
| tests/load | PASS | compiles |
| tests/reconciliation | PASS | compiles |
| web-ui (SPA) | PASS | `tsc -b && vite build` clean |
| admin-ui (SPA) | PASS | `tsc && vite build` clean |
| demo-runner (SPA) | PASS | `tsc -b && vite build` clean |

**Build totals: 16/16 Go modules + 4/4 test modules build; 3/3 React SPAs build. Zero compilation errors.**

## Test Results

| Suite | Passed | Failed | Skipped | Coverage (pkg-avg, pkgs w/ tests) |
|-------|--------|--------|---------|-----------------------------------|
| Go — auth-service | all | 0 | — | 55.2% (4 pkgs) |
| Go — clearing-engine | all | 0 | — | 82.6% (9 pkgs) |
| Go — compliance-service | all | 0 | — | 73.7% (10 pkgs) |
| Go — corporate-actions | all | 0 | — | 100.0% (1 pkg) |
| Go — fix-gateway | all | 0 | — | 82.1% (5 pkgs) |
| Go — gateway | all | 0 | — | 43.7% (17 pkgs) |
| Go — margin-engine | all | 0 | — | 79.6% (10 pkgs) |
| Go — market-data-service | all | 0 | — | 71.7% (6 pkgs) |
| Go — matching-engine | all | 0 | — | 63.6% (7 pkgs) |
| Go — platform-service | all | 0 | — | 88.4% (4 pkgs) |
| Go — securities-service | all | 0 | — | 73.7% (7 pkgs) |
| Go — settlement-engine | all | 0 | — | 83.4% (9 pkgs) |
| Go — warehouse-service | all | 0 | — | 44.8% (4 pkgs) |
| Go — shared | all | 0 | — | 76.2% (4 pkgs) |
| Go — shared/pkg/tenant | all | 0 | — | 100.0% (1 pkg) |
| Go — shared/pkg/types/decimal | all | 0 | — | 92.1% (1 pkg) |
| **Go TOTAL** | **2785 funcs + 515 subtests** | **0** | — | **~75.7% cross-module pkg-avg** |
| Frontend — web-ui | 157 | 0 | 0 | — |
| Frontend — admin-ui | 547 | 5 | 0 | — |
| Frontend — demo-runner | 113 | 21 | 0 | — |
| **Frontend TOTAL** | **817** | **26** | **0** | business-logic ~high; component tiers low (no RTL) |

**Coverage methodology:** the per-module number is the unweighted package-average over packages that have test files (no-test `cmd/`, `internal/server`, `internal/config`, `internal/types` packages excluded — they would dilute to 0%). Cross-module package-average ≈ **75.7%**. This is consistent with (and slightly above) the ~69.5% business-logic baseline, which uses a different inclusion set. A statement-weighted all-package number would be lower; it is not reported here because the `covdata` tool is unavailable in the network-fetched go1.25 toolchain (see note below).

> **`-cover` tooling note:** `go test -cover ./...` reports `go: no such tool "covdata"` for packages with **no test files** under the network-fetched go1.25 toolchain, causing a non-zero exit on those modules. This is an environment/toolchain artifact, **not** a code or test failure — confirmed by re-running `go test ./...` (without `-cover`) on all 11 affected modules: all return rc=0 with 0 test failures. Per-package coverage for packages that have tests is captured correctly (the numbers above).

## Failures (26 — all frontend fixture-count drift)

All 26 failures are stale, hard-coded count/shape assertions in co-located fixture tests that lag merged data/logic edits. Every SPA `tsc` type-checks and `vite build`s cleanly, so these are assertion drift, not logic or type breakage — the implementation is the source of truth.

**admin-ui (5):**
- `market-phase.test.ts` (4) — `normalizeInstruments` now defaults a missing phase to `CLOSED` (test expects `PRE_OPEN`), emits an extra field, and Halt-All now filters to `TRADING` instruments. Sample: `expected 'CLOSED' to be 'PRE_OPEN'`.
- `bot-integration.test.ts` (1) — `createTicket` POST path/args changed (`expected "spy" to be called with arguments: [ …(2) ]`).

**demo-runner (21):** `sections.test.ts` (9) + `t066_integration.test.ts` (12) — the demo `sections.ts` data grew to **14 sections / 59 steps** (new `admin-orderbook` section + `ob-`-prefixed steps), while the fixtures still assert **13 sections / 51 steps**. Samples: `expected 59 to be 51`, `new section admin-orderbook should exist: expected undefined not to be undefined`, `steps with prefix ob-: expected 0 to be greater than 0`.

This is the same stable drift set that has appeared in every run since `run-20260626-040705` (6+ consecutive runs). It is durable test debt that should be drained by a single test-only fixture-refresh task (R013-class): realign admin-ui `market-phase`/`bot-integration` and demo-runner `sections`/`t066` assertions to current behavior. No scheduled fix task exists yet.

## Not Executed (build-verified only)

- **`tests/e2e`, `tests/kafka-e2e`** — require a live `docker compose` stack (gateway + Kafka broker + Postgres). Compiled clean but not run against a live stack in this run. Latest live-stack e2e verdict on record remains `run-20260626-051039` PASS (32 top-level / 141 subtests / 8 graceful skips / 0 fail).
- **`tests/race`** (engine `-race` tier) — not run in this pass; build of the four engines is green.

## Summary

The platform is healthy: 16/16 Go modules and all 3 SPAs build with zero compilation errors, and the **entire Go test suite passes** (2785 test funcs + 515 subtests, 0 failures) with a ~75.7% cross-module package-average coverage. The only red is the long-standing 26-assertion frontend fixture-count drift (admin-ui `market-phase`/`bot-integration`, demo-runner `sections`/`t066`) — pure test debt against current, type-checking, building source. Verdict **PARTIAL**: green builds + green backend + only fixture drift. Recommended next action: schedule the R013-class fixture-refresh task to flip subsequent runs to clean.
