# Integration Run — run-20260627-062805 (R029)

**Verdict:** **PASS** — "a real gateway-submitted trade settles end-to-end across the bus" is **CONFIRMED**.

**Task:** R029 — Live e2e proof that a genuine, authenticated, tenant-scoped order
pair submitted *through the gateway* crosses, matches, and flows end-to-end on a
real Kafka broker: matching → clearing → margin → settlement, with **populated**
participant/order IDs (proving R028 D1/D2/D3 closed the economic-completion gaps).

---

## Environment (genuinely fresh bring-up)

- Images **built from committed Dockerfiles** (R026 `./src` build context) — all
  12 backend Go services build cleanly, no `/tmp` workarounds.
- Stack brought up on **fresh `pgdata` + `kafkadata` volumes** (`docker compose
  down -v` then `up`). Postgres initialised with **0 errors** (R025 zero-padded
  migrations). Real `confluentinc/cp-kafka:7.5.0` broker with `KAFKA_BROKERS=kafka:9092`
  injected into every engine.
- Services up & healthy: postgres, zookeeper, kafka, matching/clearing/margin/
  settlement engines, auth-service, compliance-service, platform-service, gateway.
- Host port override only (`POSTGRES_PORT=15432` to avoid a pre-existing 5432
  listener); all inter-service comms unchanged.
- Test execution note: the broker advertises `kafka:9092` (unresolvable from the
  host), so the Go test suites were run from **inside the compose network** (an
  ephemeral `golang:1.22` container on `…_garudax-network`, `KAFKA_BROKERS=kafka:9092`,
  `GATEWAY_URL/E2E_BASE_URL=http://gateway:8080`). The produce path (gateway →
  matching) always runs in-network; only the host-side *consumer* needed the
  in-network vantage point. This is an execution-environment detail, not a defect.

---

## Topics (R028 D3 — `kafka-topics --list`, ace-commodities)

```
ace-commodities.trades.executed        ace-commodities.dlq.trades.executed
ace-commodities.clearing.novated       ace-commodities.dlq.clearing.novated
ace-commodities.margin.call-issued     ace-commodities.dlq.margin.call-issued
ace-commodities.settlement.completed   ace-commodities.dlq.settlement.completed
```

All 4 cross-service topics **and** all 4 `dlq.*` topics exist.

## Engine wiring (live log evidence — bug class #3)

```
[matching-engine]  using real Kafka producer, brokers=[kafka:9092]   kafka_publisher_enabled topic=trades.executed
[clearing-engine]  using real Kafka producer/consumer  group=clearing-engine   cross-service consumer enabled (trades.executed -> clearing.novated)
[margin-engine]    Seeded SPAN risk parameters for 1 instrument(s) from built-in demo default   (R028 D2)
[margin-engine]    using real Kafka producer/consumer  group=margin-engine     cross-service consumer enabled (clearing.novated -> margin.call-issued)
[settlement-engine] using real Kafka producer/consumer group=settlement-engine cross-service consumer enabled (clearing.novated -> settlement.completed)
```

---

## Test results

### `tests/kafka-e2e` — PASS (2/2)
- **`TestSeededGatewayTradeSettles` (NEW, R029) — PASS (16.6s).** Drives a REAL
  authenticated, tenant-scoped order pair through the gateway (no hand-set IDs),
  and asserts every hop on the live broker:
  - HOP 1 `trades.executed`: `trade_id=id-33`, `buyer="P-R029-BUY-…"`,
    `seller="P-R029-SELL-…"`, `buy_order="id-29"`, `sell_order="id-31"` — **all
    NON-EMPTY** (R028 D1 proven: IDs flow gateway → matching → eventbus, not
    hand-set on the payload).
  - HOP 2 `clearing.novated`: observed for the run's participant — **no
    "participant IDs are required" rejection**.
  - HOP 3 `margin.call-issued`: observed for the seeded instrument (R028 D2).
  - HOP 4 `settlement.completed`: observed (correlated `cycle-<tradeID>`).
  - Live engine logs corroborate: `CLEARED: trade=id-… buyer=P-R029-BUY-… seller=P-R029-SELL-…`
    and `MARGIN CALL: participant=P-R029-BUY-… deficit=5025000`.
- **`TestCrossServiceTradePropagation` (synthetic, R024/R027) — PASS (8.0s).**
  Transport-only: clearing.novated + settlement.completed observed; margin
  best-effort (its watcher keys on the injected correlation, which margin re-keys
  to `mc-N`, so it is logged-not-gated — consistent with R027).

### `tests/e2e` (HTTP, through the live gateway) — PASS
- **32 top-level tests / 174 sub-test PASS / 0 FAIL / 7 graceful skips.**
- Skips are backend-availability graceful-skips (matching admin halt/resume/
  mass-cancel gRPC ops, token-refresh session fields, a few `TestTradeLifecycle`
  steps) — not failures.

---

## Bug-class re-confirmation (historical "6 e2e bugs")

| Class | Description | Status |
|---|---|---|
| **#1** | gateway 404-before-auth (unknown path → 401 instead of 404) | **FIXED** — `TestGatewayHealth/unknown_endpoint_returns_404` PASS; `RouteChecker` (R011) live. |
| **#2** | compliance KYC array-vs-object | **FIXED** — `TestComplianceKYCFlow/submit_KYC_application` PASS. |
| **#3** | cross-service Kafka propagation | **FIXED + economically complete** — live topics, real producer/consumer logs, and a seeded gateway trade settling through all 4 hops. |

---

## Frontend fixture drift (out of scope, per task)

The known 26 frontend fixture-drift unit tests (admin-ui `market-phase`/
`bot-integration`, demo-runner `sections`/`t066`) were **not run** in this
backend/live-stack verification and are out of R029 scope. Per the e2e-canary
verdict-granularity pattern they are **PARTIAL test-debt**, not a FAIL — the
backend/live verdict stands as PASS. They remain the single longest-standing
unscheduled R013-class fix.

---

## VERDICT

**CONFIRMED.** A real, authenticated, tenant-scoped, gateway-submitted trade
(`WHT-HRW-2026M07-UB`) settles end-to-end across the live broker —
`trades.executed` (populated buyer/seller participant + order IDs) →
`clearing.novated` → `margin.call-issued` → `settlement.completed` — on a fresh
stack built and initialised from committed artifacts. R028's D1/D2/D3 fixes are
verified live; bug classes #1/#2/#3 remain FIXED.
