# Handoff — R030: Refresh architect-bot knowledge base with R001–R029 hardening

**Status:** DONE
**Agent:** Coder
**Phase:** Remediation (R-series follow-up)

---

## Summary
Updated the architect-bot's knowledge base (the 'Chief Architect' presenter's sole source of truth, loaded from `src/architect-bot/knowledge/*.md`) so it speaks accurately to the MSE board / FRC. The files were dated 2026-04-29 and predated the entire R001–R029 remediation, causing the bot to state facts that were materially false (channel-based Kafka stubs, 11 services, bypassed tenant enforcement) or stale (2,540-test / 887-test metrics). Corrected the now-false claims, refreshed all metrics from the current tree, and added the financial-correctness, race-testing, reproducible-deploy, and live-Kafka hardening story — all kept honest with target-vs-today labelling. Persona/marketing tone and structure preserved; corrected and augmented rather than rewritten.

## Key Decisions
1. **Re-derived every metric from the tree, did not hand-copy.** Verified each load-bearing number against source before writing it (commands below). This is the R013/R016 "verify each claim against code" discipline.
2. **Described tenancy as enforced-at-edge but honestly scoped.** Stated tenant enforcement is real in code (gateway validates `X-GarudaX-Tenant` before routing, 404 RouteChecker guard, narrow bypass list) — but explicitly labelled full backend-side cross-tenant authZ as roadmap-not-today, because asserting full data isolation as fact would be a security hazard (false confidence in an unenforced boundary). Reordered the tenant table to put the live tenant (ace-commodities) first.
3. **Added Financial Correctness + Platform Hardening sections** to garudax-overview.md as board-relevant value props (regulated-venue correctness selling points), not as a changelog.
4. **Promoted financial correctness to an 8th MIT differentiator** rather than burying it — it is verifiable in source vs MIT's closed-source assertion.
5. **Used conservative derived figures** ("800+ frontend tests", "~2,795 Go test funcs", "~94 routes") with an explicit "figures derived from the current tree; counts drift" caveat, so the bot doesn't over-precisely commit to numbers that move.
6. **Left securities-domain.md untouched** — it is pure domain reference (order types, auctions, surveillance patterns) with no stale metrics or false platform claims.
7. **Left competitive-landscape.md factually unchanged** — cross-checked it for the stale 'stubs'/'11 services' claims the task flagged; neither appears there (its Kafka mentions are generic open-stack stack-lists, now accurate; its 'STUB' grades refer to MIT-feature depth, not service stubs). No correction was warranted, so none was forced.

## Factual Corrections (old -> new) and source verified against

### garudax-overview.md
1. Messaging stack: "Kafka (channel-based stubs with wire-protocol interface)" -> "Apache Kafka — real wire-protocol event bus (segmentio/kafka-go), live cross-service event propagation".
   *Source:* 18 files reference `segmentio/kafka-go` in `src/*/internal/kafka/`; `internal/eventbus` wired into all 4 engine `cmd/*/main.go`; CLAUDE.md R024/R027/R029 (live verified, `run-20260627-062805` PASS).
2. "Services (11 Go Microservices)" + table listing admin-bot as a Go service -> "Services (14 Go Modules)" with the real 14-module list + note that admin-bot/architect-bot are Node.js.
   *Source:* `ls src/*/go.mod` = 14; `ls src/admin-bot/package.json src/architect-bot/package.json` (Node, no go.mod).
3. Gateway "101 registered routes" -> "~94 registered routes" (two places: service table + metrics table).
   *Source:* `grep -rhE '\.Handle\("(GET|POST|...)"' src/gateway/internal/ --include='*.go' | grep -v _test.go | wc -l` = 94.
4. Codebase Metrics table fully refreshed:
   - Go services 11 -> Go modules 14 (+2 shared sub-modules). *Source:* `ls src/*/go.mod`.
   - Go test functions "2,540+" -> "~2,795 (+ ~520 sub-tests)". *Source:* `grep -rhE '^func Test' src/ --include='*_test.go' | wc -l` = 2795; `t.Run` = 521.
   - Frontend tests "313" -> "800+". *Source:* `it()/test()` count across the 3 SPAs = 821 (157 web-ui + 530 admin-ui + 134 demo-runner).
   - E2E API "58" -> "32 top-level / 141 sub-tests"; added "live cross-service Kafka e2e verified PASS". *Source:* CLAUDE.md R016/R029 canonical e2e baseline.
   - Added coverage "~65% stmt / ~70% biz-logic". *Source:* CLAUDE.md R016.
   - Dropped stale unverifiable rows (LOC ~136k, 65 structs, 48 store interfaces, "6 PostgreSQL stores"); added AI-bots row.
5. Multi-Tenant Design paragraph: "resolves the header ... before any business logic" (implied working but was bypassed in April) -> accurate description of enforced-before-routing + RouteChecker 404 guard + narrow bypass list + validated-tenant-forwarded, with explicit today/roadmap split.
   *Source:* `src/gateway/internal/middleware/tenant.go` `tenantBypassPrefixes` = `{/platform/, /api/v1/platform/, /api/v1/auth/}`; CLAUDE.md R011.
6. Development Model: "60 tasks across 7 phases with 887+ tests and 66% coverage" -> narrative covering the correctness/hardening remediation arc + fresh full-stack run against a live broker (no stale fixed numbers).
7. NEW sections: **Financial Correctness** (shared Decimal, no float64 money, 128-bit overflow check, banker's rounding, error-on-divide-by-zero) and **Platform Hardening** (live Kafka event bus + fail-fast, race-tested engines, reproducible deploys, enforced tenancy).
   *Source:* `src/shared/pkg/types/decimal/decimal.go` (`ErrDivideByZero` L85; 128-bit overflow comment L12-13; half-even in decimal.go/_test.go); 10 Dockerfiles `COPY shared` (R026); CLAUDE.md R001–R029.

### millenniumit-comparison.md
8. Differentiator #6 "2,540+ Go tests, 313 frontend tests, 58 e2e tests, 13 Playwright tests" -> "~2,795 Go test functions (+~520 sub-tests), 800+ frontend tests, e2e API tests, live cross-service Kafka verification; engines race-tested".
9. Differentiator #7 (open infrastructure) augmented: real wire-protocol Kafka event bus + reproducible-from-committed-Dockerfiles/clean-DB-init.
10. Added Differentiator #8 "Provable financial correctness" (shared Decimal, no float64 money, verifiable in source vs MIT's closed-source assertion); header "7 GarudaX Differentiators" -> "8".

### mse-context.md
11. Multi-Tenant Capability bullet: "Each venue maintains complete isolation (separate tenant context, trading rules, participants)" -> accurate "own tenant context/rules/participants with per-tenant schema namespacing; validated+enforced at the gateway edge; full backend-side cross-tenant authZ on the roadmap". (Honesty fix per the R013 "don't assert unenforced isolation as fact" principle.)

## Blockers Found
None. Every claim the task flagged was independently verifiable against the source tree.

## Suggested Follow-ups
- When mse-equities goes live and backend-side cross-tenant authorization lands, update the today/roadmap labels in garudax-overview.md (Multi-Tenant Design) and mse-context.md to promote isolation from "edge-enforced" to "fully enforced end-to-end".
- The "9 MCP tools" claim (millenniumit-comparison #2, competitive-landscape, mse-context) was carried over and not re-verified in this task — a future refresh should re-derive the MCP tool count from the bot source.
- These knowledge files have no automated test; consider a lightweight CI check asserting the module count / test-count figures stay within tolerance of the tree so they don't drift stale again (the recurring failure mode this task remediated).

## Deliverables
- `src/architect-bot/knowledge/garudax-overview.md` (modified — Tier 1 + Tier 2)
- `src/architect-bot/knowledge/millenniumit-comparison.md` (modified — differentiators 6/7/8)
- `src/architect-bot/knowledge/mse-context.md` (modified — isolation honesty fix)
- `handoff/R030.md` (this file)
- Unchanged (verified, no correction needed): `competitive-landscape.md`, `securities-domain.md`
