# T209 — Bot + Ticket Integration Tests

## Status: DONE

## Summary

Created 73 tests across 4 test files covering the bot orchestrator (router, nano-handler, daily-admin) and admin-ui bot integration (botApi).

## Files Created

- `src/admin-bot/orchestrator/vitest.config.ts` — Vitest config for orchestrator (node environment)
- `src/admin-bot/orchestrator/src/__tests__/router.test.ts` — 20 tests: keyword classification for all 6 categories, edge cases (empty, long, unicode, special chars), case insensitivity, classifyRequest fallback without API key
- `src/admin-bot/orchestrator/src/__tests__/nano-handler.test.ts` — 18 tests: status_query (healthy, array format, non-ok, unreachable, empty), ticket_create (POST, priority extraction, failure, network error), admin_action (confirmation), report_generate (success, error), ticket_update (context ID, prompt, extract from message), gateway config (custom URL, auth token)
- `src/admin-bot/orchestrator/src/__tests__/daily-admin.test.ts` — 11 tests: constructor with defaults/custom hours, 3 cron job registration, custom cron expressions, default hours validation, stop lifecycle, restart, log messages
- `src/admin-ui/src/__tests__/bot-integration.test.ts` — 20 tests: URL construction (chat, suggestions, tickets, health, missing slash), auth headers, request body builders, sendBotMessage (POST, context, error, network), getBotSuggestions (GET, URL encoding, error), createTicket (POST, error, auth headers)

## Files Modified

- `src/admin-bot/orchestrator/package.json` — Added vitest dev dependency, added test script

## Test Results

- Orchestrator: 53 tests passing (3 test files)
- Admin-ui: 323 tests passing (18 test files, including 20 new bot-integration tests)
- Zero regressions on existing tests

## Decisions Made

- Used `vi.stubGlobal('fetch', mockFetch)` for fetch mocking in both orchestrator and admin-ui tests
- Used `vi.mock('node-cron')` to avoid actual cron scheduling in daily-admin tests
- Mocked `../config` and `../services/api` modules in admin-ui tests to provide deterministic config/token values
- Deleted and restored `process.env` in orchestrator tests to avoid cross-test env leakage

## Suggested Follow-ups

- T210+ integration test should verify total test count increased from baseline
- Consider adding tests for `claude-handler.ts` (complex_analysis path) — requires mocking child_process.spawn
- Consider adding tests for `api.ts` (HTTP server routes) — requires starting the server
