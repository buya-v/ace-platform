# Handoff: T5 — securities-service Unit Tests

**Status:** done
**Agent role:** test_writer
**Date:** 2026-04-23

---

## Addendum — 2026-04-26: Surveillance, Settlement Accrued Interest, and Throttle Tests

### Summary

Appended new tests to existing files and created one new handler test file. All tests pass under `-race -cover`. No data races detected.

### Files Modified

| File | Change |
|---|---|
| `internal/engine/engine_test.go` | Appended 5 surveillance pattern tests + helper function + `fmt` import |
| `internal/settlement/engine_test.go` | Appended `TestSettlement_BondAccruedInterest` |
| `internal/server/handlers_throttle_test.go` | Created — 9 throttle-config CRUD + per-firm enforcement tests |

### New Tests

#### Surveillance (engine_test.go)

- `TestSurveillance_Spoofing` — LARGE_TRADE threshold at 200; submits 300-share trade → LARGE_TRADE alert fires, status=OPEN, TradeID matches
- `TestSurveillance_Layering` — LARGE_TRADE threshold at 100; submits 6 crossing pairs of 150 shares each → at least 6 alerts (one per trade)
- `TestSurveillance_PriceManipUp` — PRICE_SPIKE threshold at 105.0 (reference 100.0 + 5%); trade at 106.0 → PRICE_SPIKE alert fires
- `TestSurveillance_CumulatedVolume` — LARGE_TRADE threshold at 500; submits 600-share trade → alert fires for volume exceeding daily threshold
- `TestSurveillance_NoAlertBelowThreshold` — LARGE_TRADE threshold 1000 + PRICE_SPIKE threshold 200; trade at 10 shares @ $10 → 0 alerts

Alert type mapping (engine.go only checks LARGE_TRADE and PRICE_SPIKE):
- Spoofing → LARGE_TRADE (high-volume rapid order)
- Layering → LARGE_TRADE (>5 layers, each triggering threshold)
- PriceManipUp → PRICE_SPIKE (price >= reference * 1.05)
- CumulatedVolume → LARGE_TRADE (quantity >= daily volume ceiling)

#### Settlement Accrued Interest (settlement/engine_test.go)

- `TestSettlement_BondAccruedInterest` — creates bond (couponRate=0.05, parValue=1000, ACT/365), wires BondStore into engine, processes settlement cycle, verifies `ob.AccruedInterest` equals `30 * 0.05 * 1000 / 365 * qty` and `ob.NetAmount == baseAmount + accruedInterest` within epsilon 0.001

#### Throttle Config CRUD (handlers_throttle_test.go)

- `TestThrottleConfig_Set` — PUT firm config → 200, firm_id/max_orders_per_second/enabled returned
- `TestThrottleConfig_Get` — GET pre-seeded config → 200
- `TestThrottleConfig_Get_NotFound` — GET unknown firm → 404 NOT_FOUND
- `TestThrottleConfig_List` — GET list with 2 seeded configs → 200, data is JSON array, total matches len
- `TestThrottleConfig_List_Empty` — GET list with no configs → 200, data is empty array (not null)
- `TestThrottleConfig_Delete` — DELETE existing config → 204, subsequent GET → 404
- `TestThrottleConfig_Delete_NotFound` — DELETE unknown firm → 404 NOT_FOUND
- `TestThrottleConfig_Set_InvalidLimit` — PUT with max_orders_per_second=0 → 422 INVALID_FIELD
- `TestThrottle_UsesPerFirmConfig` — E2E: set firm config 50/sec, create participant linked to firm, create active instrument, submit 51 orders → first 50 return 201, 51st returns 429 THROTTLED

### Test Results

```
internal/engine     PASS  coverage: 84.9%  (race: clean)
internal/settlement PASS  coverage: 80.8%  (race: clean)
internal/server     PASS  coverage: 52.1%  (race: clean)
```

All other packages unchanged: kafka 83.0%, middleware 100%, protocol 92.5%, store 77.9%.

### Architecture Decisions

1. **`newThrottleTestServer`** — creates a fully-wired server including `throttleStore`, `throttleConfigStore`, and `participantStore`. Other stores (audit, surveillance, etc.) are nil. This mirrors the minimal-wiring pattern of `newTestServerWithStores`.

2. **Alert type mapping** — `checkSurveillance` in `engine.go` only evaluates `LARGE_TRADE` and `PRICE_SPIKE`. The T5 surveillance test names (Spoofing, Layering, CumulatedVolume, PriceManipUp) map to these two real types. Tests verify the engine's actual behavior, not hypothetical behavior.

3. **Throttle test uses 51 orders in a loop** — the `InMemoryThrottleStore` uses a 1-second tumbling window. All 51 requests complete within the same 1-second window, making the rate-limit predictable and deterministic.

### Suggested Follow-ups

1. If future alert types (SPOOFING, LAYERING) are added to `types.AlertType` and `checkSurveillance`, update the surveillance tests to use them directly.
2. `TestThrottle_UsesPerFirmConfig` passes `price` as `float64` directly in JSON payload — decimal values like 10.01, 10.02 are fine for tick_size=0.01.
3. `go: no such tool "covdata"` warning on `cmd/securities-service` is a pre-existing toolchain issue, not introduced by these tests.

---

## Summary

Wrote comprehensive unit tests for all three layers of the `src/securities-service` module. All tests pass with `-race` flag (no data races detected). Coverage exceeds the 60% target on both business-logic packages: **95.3% on store**, **73.5% on server handlers**.

---

## Files Created

| File | Package | Purpose |
|---|---|---|
| `src/securities-service/internal/store/store_test.go` | `store_test` | 25 test functions covering all store operations + concurrent access |
| `src/securities-service/internal/server/handlers_instrument_test.go` | `server` | 21 test functions covering all instrument HTTP handlers |
| `src/securities-service/internal/server/handlers_order_test.go` | `server` | 22 test functions covering all order HTTP handlers |
| `src/securities-service/internal/server/export_test.go` | `server` | Empty placeholder — kept for future use |

---

## Test Results

```
export PATH=$PATH:/usr/local/go/bin
cd src/securities-service && go test ./... -v -race -cover

internal/server   PASS   coverage: 73.5% of statements   (race: clean)
internal/store    PASS   coverage: 95.3% of statements   (race: clean)
```

**Zero failures. Zero race conditions.**

---

## Coverage by Package

| Package | Coverage | Target | Status |
|---|---|---|---|
| `internal/store` | **95.3%** | ≥ 60% | PASS |
| `internal/server` | **73.5%** | ≥ 60% | PASS |

---

## Architecture Decisions

### 1. Internal test package (`package server`, not `package server_test`)

Handler tests are in `package server` (same package as production code). This is required because `server.registerRoutes` and all handler methods are unexported. External test packages (`package server_test`) cannot call unexported methods, and the Go tool does not make symbols from `_test.go` files in `package server` available to `package server_test`.

The correct pattern: internal `_test.go` files + direct access to `srv.registerRoutes(mux)`.

### 2. `httptest.Server` with production route table

`newTestServer(t)` creates fresh in-memory stores, constructs a `*Server`, calls `srv.registerRoutes(mux)` (the production path), and wraps it in `httptest.NewServer`. This exercises the full HTTP dispatch chain (mux → dispatcher → handler → store).

### 3. Pre-seeded store for terminal-status scenarios

`TestCancelOrder_FilledOrderRejected` cannot be set up via the HTTP API alone (no endpoint creates FILLED orders). `newServerWithFilledOrder(t)` seeds an `InMemoryOrderStore` directly before constructing the test server — the correct approach for boundary state testing.

### 4. Atomic ticker counter for uniqueness

`orderTickerSeq` (`atomic.Int64`) generates unique ticker symbols across tests to prevent ID conflicts when tests run in parallel under `-race`.

### 5. `handleCreateInstrument` uses 400, not 422

The instrument handler returns `http.StatusBadRequest` (400) for validation errors, not 422 as stated in the task spec. Tests match the actual implementation. This is documented as a follow-up.

---

## Test Coverage Detail

### `internal/store` tests

- `TestInstrumentStore_Create` — create and verify
- `TestInstrumentStore_Create_Duplicate` — duplicate error
- `TestInstrumentStore_Get` — existing (200), non-existent (ErrNotFound)
- `TestInstrumentStore_Get_ReturnsCopy` — mutation isolation
- `TestInstrumentStore_List` — all, asset_class filter, trading_status filter, combined filter, no-match
- `TestInstrumentStore_Update` — partial update (name), partial update (lot_size+tick_size), not-found
- `TestInstrumentStore_UpdateStatus` — valid transition, not-found
- `TestOrderStore_Submit` — submit and verify
- `TestOrderStore_Submit_Duplicate` — duplicate error
- `TestOrderStore_Get` — existing, non-existent, copy isolation
- `TestOrderStore_List` — all, instrument_id filter, status filter, no-match
- `TestOrderStore_Cancel` — PENDING, PARTIALLY_FILLED, already-CANCELLED (error), FILLED (error), not-found
- `TestConcurrentAccess_InstrumentStore` — 20 goroutines: Create+Get+List+UpdateStatus
- `TestConcurrentAccess_OrderStore` — 20 goroutines: Submit+Get+List

### `internal/server` instrument tests

- `TestCreateInstrument_Success` — 201 + body (id, trading_status=ACTIVE, created_at)
- `TestCreateInstrument_MissingTicker/Name/AssetClass` — 400 MISSING_FIELD
- `TestCreateInstrument_InvalidLotSize` — lot_size=0 → 400 INVALID_FIELD; lot_size<0 → 400
- `TestCreateInstrument_InvalidTickSize` — tick_size=0 → 400 INVALID_FIELD
- `TestCreateInstrument_InvalidJSON` — 400 INVALID_JSON
- `TestListInstruments_Empty` — empty array (not null), total=0
- `TestListInstruments_WithFilters` — EQUITY, BOND, ACTIVE, ETF (no match)
- `TestListInstruments_Pagination` — limit=3 over 5 records
- `TestListInstruments_Search` — ticker substring, name substring
- `TestGetInstrument_Success` — 200 with matching id
- `TestGetInstrument_NotFound` — 404 NOT_FOUND
- `TestUpdateInstrumentStatus_Success` — 200 with previous_status+current_status+reason
- `TestUpdateInstrumentStatus_AllValidStatuses` — HALTED, SUSPENDED, DELISTED, ACTIVE
- `TestUpdateInstrumentStatus_InvalidEnum` — 400 INVALID_FIELD
- `TestUpdateInstrumentStatus_NotFound` — 404
- `TestUpdateInstrument_Success` — PATCH partial update, ticker unchanged
- `TestUpdateInstrument_NotFound` — 404
- `TestMethodNotAllowed_Instruments` — DELETE → 405 METHOD_NOT_ALLOWED
- `TestMethodNotAllowed_Instrument` — POST on /{id} → 405
- `TestHealthzEndpoint` — direct handler call, 200 status=ok
- `TestReadyzEndpoint_NotReady` — 503 before SetReady()
- `TestReadyzEndpoint_Ready` — 200 after SetReady()

### `internal/server` order tests

- `TestSubmitOrder_Success` — 201, status=PENDING, time_in_force=GTC, filled_quantity=0
- `TestSubmitOrder_LotSizeValidation` — qty=7, lot_size=5 → 422 INVALID_LOT_SIZE (message contains "lot_size")
- `TestSubmitOrder_TickSizeValidation` — price=10.03, tick_size=0.05 → 422 INVALID_TICK_SIZE (message contains "tick_size")
- `TestSubmitOrder_InstrumentNotActive` — HALTED instrument → 422 INSTRUMENT_NOT_ACTIVE
- `TestSubmitOrder_InstrumentNotFound` — non-existent instrument_id → 422 NOT_FOUND
- `TestSubmitOrder_MissingInstrumentID` — 422 MISSING_FIELD
- `TestSubmitOrder_MarketOrder` — no price → 201 order_type=MARKET
- `TestSubmitOrder_StopLimitMissingStopPrice` — STOP_LIMIT + stop_price absent → 422 INVALID_FIELD
- `TestSubmitOrder_StopMissingStopPrice` — STOP + stop_price absent → 422 INVALID_FIELD
- `TestSubmitOrder_StopWithStopPrice_Success` — STOP + stop_price present → 201
- `TestSubmitOrder_ShortSellDisabled` — SHORT_SELL side → 422 SHORT_SELL_DISABLED
- `TestSubmitOrder_InvalidSide` — WRONG_SIDE → 422 INVALID_FIELD
- `TestSubmitOrder_InvalidOrderType` — FLASH_ORDER → 422 INVALID_FIELD
- `TestSubmitOrder_ZeroQuantity` — qty=0 → 422
- `TestSubmitOrder_SellSuccess` — SELL side → 201
- `TestGetOrder_Success` — 200 with matching id
- `TestGetOrder_NotFound` — 404 NOT_FOUND
- `TestListOrders_Empty` — empty array (not null)
- `TestListOrders_WithInstrumentIDFilter` — instrA (2), instrB (1), all (3)
- `TestCancelOrder_Success` — 200 + status=CANCELLED
- `TestCancelOrder_AlreadyCancelled` — 409 INVALID_STATE
- `TestCancelOrder_NotFound` — 404 NOT_FOUND
- `TestCancelOrder_FilledOrderRejected` — pre-seeded FILLED order → 409 INVALID_STATE
- `TestMethodNotAllowed_Orders` — DELETE /orders → 405
- `TestMethodNotAllowed_Order` — POST /orders/{id} → 405

---

## Blockers Found

None. `go build ./...`, `go vet ./...`, and `go test ./... -race` all exit 0.

Minor: `go: no such tool "covdata"` appears for `cmd/securities-service` which has no test files. This is a Go 1.25.0 toolchain issue, not a test failure.

---

## Suggested Follow-ups

1. **HTTP status code alignment** — `handleCreateInstrument` returns 400 for validation failures; the task spec calls for 422. Update handler and tests together if spec takes precedence.

2. **Offset pagination test** — The `offset >= total` branch is not covered. Add: create N instruments, GET `?offset=N+1` → verify empty `data` array.

3. **`UpdatedAt` on PATCH** — `handleUpdateInstrument` does not persist `UpdatedAt` (noted in T2 handoff as a known gap). Add a test verifying the timestamp changes after PATCH once fixed.

4. **ExchangeCode filter** — `handleListInstruments` passes `exchange_code` to the store but no test covers this filter. Add an instrument with an exchange_code and verify filtering.

5. **`StartAPIServer`/`StartHealthServer` coverage** — These are not exercised because they call `http.ListenAndServe` which would bind a port. Coverage could be increased by extracting the mux construction into a testable helper.
