# T3 Handoff — RBAC Role & Privilege Tests

**Date:** 2026-04-26
**Status:** COMPLETE
**Agent:** Test Writer (softhouse)

---

## Summary

Added tests covering the full RBAC Role layer: `InMemoryRoleStore`, `PrivilegeEngine`, and the five role CRUD HTTP handlers.

All tests pass: `go test ./... -race -cover` — all 7 internal packages green, race detector clean.

---

## New / Modified Files

| File | Action | Tests Added |
|---|---|---|
| `internal/store/store_test.go` | Appended | 5 new functions: Create_Get, GetByName, List, Update, Delete |
| `internal/engine/privilege_test.go` | Created | 9 functions: FromParticipant, FromRole, Merged, AdminFull, Allowed, Denied, UnknownParticipant, Suspended, NilRoleStore |
| `internal/server/handlers_role_test.go` | Created | 8 test functions + helpers: Create, List, Get, Update, Delete, Unconfigured, JSONRoundTrip |

---

## Test Coverage by Function

### store/store_test.go — RoleStore

| Test | What it verifies |
|---|---|
| `TestRoleStore_Create_Get` | Create → Get round-trip; field equality; copy isolation (mutation doesn't affect store); duplicate Create error; ErrNotFound on miss |
| `TestRoleStore_GetByName` | Seeds ADMIN/TRADER/VIEWER roles; finds each by name using List() + scan (no GetByName method on interface) |
| `TestRoleStore_List` | Returns at least 3 seeded roles; all three names present |
| `TestRoleStore_Update` | Name/description/permissions mutated; UpdatedAt refreshed; ErrNotFound on unknown id; empty-name preserves old name |
| `TestRoleStore_Delete` | Role removed; Get → ErrNotFound; deleted role absent from List; deleting twice → ErrNotFound |

### engine/privilege_test.go — PrivilegeEngine

| Test | What it verifies |
|---|---|
| `TestResolvePermissions_FromParticipant` | Active participant with direct permissions passes; missing perm denied |
| `TestResolvePermissions_FromRole` | Participant with RoleID, no direct permissions; inherits from role |
| `TestResolvePermissions_Merged` | Direct + role permissions both available; neither duplicated; missing perm denied |
| `TestHasPermission_AdminFull` | Participant in ADMIN role with all 10 platform perms; all pass |
| `TestHasPermission_Allowed` | ACTIVE participant + required perm → nil error |
| `TestHasPermission_Denied` | ACTIVE participant missing required perm → non-nil error |
| `TestHasPermission_UnknownParticipant` | ID not in store → non-nil error |
| `TestHasPermission_SuspendedParticipant` | SUSPENDED participant denied even with permission |
| `TestHasPermission_NilRoleStore` | nil roleStore falls back to direct permissions; no panic |

### server/handlers_role_test.go — HTTP Handlers

| Test | HTTP Contract Verified |
|---|---|
| `TestCreateRole` | POST → 201; missing name → 400; nil permissions → empty array in response |
| `TestListRoles` | GET → 200; ADMIN/TRADER/VIEWER all present in response |
| `TestGetRole` | GET /{id} → 200 with id/name/permissions; unknown id → 404 |
| `TestUpdateRole` | PUT /{id} → 200; fields updated; old permissions gone; unknown id → 404; subsequent GET reflects change |
| `TestDeleteRole` | DELETE /{id} → 204; GET → 404; second DELETE → 404; unknown id → 404 |
| `TestRoleHandlers_Unconfigured` | All 5 role routes → 503 when roleStore is nil |
| `TestCreateRole_JSONRoundTrip` | Full `types.Role` decode: id, name, description, permissions, created_at, updated_at all present |

---

## Test Infrastructure

`newRoleTestServer(t *testing.T) (*httptest.Server, store.RoleStore)` — new helper in `handlers_role_test.go` that calls `New()` with live `InMemoryRoleStore` + `InMemoryParticipantStore` + `PrivilegeEngine`. Returns both the HTTP server and the roleStore for direct seeding. Uses the same `TenantMiddleware` + `X-GarudaX-Tenant: ace-commodities` pattern as existing handler tests.

---

## Test Run Results

```
ok  github.com/garudax-platform/securities-service/internal/engine      1.109s  coverage: 85.3%
ok  github.com/garudax-platform/securities-service/internal/kafka       1.052s  coverage: 83.0%
ok  github.com/garudax-platform/securities-service/internal/middleware  1.034s  coverage: 74.5%
ok  github.com/garudax-platform/securities-service/internal/protocol    1.029s  coverage: 92.5%
ok  github.com/garudax-platform/securities-service/internal/server      2.384s  coverage: 50.7%
ok  github.com/garudax-platform/securities-service/internal/settlement  1.032s  coverage: 80.8%
ok  github.com/garudax-platform/securities-service/internal/store       2.151s  coverage: 59.4%
```

Zero FAIL lines. Race detector: PASS across all packages.

Pre-existing PATH issue (`cmd/securities-service: exec: "go": executable file not found in $PATH`) is not caused by this task — Go binary is at `/usr/local/go/bin/go`, not on system PATH.

---

## Key Decisions

1. **No `GetByName` on `RoleStore` interface** — The interface has only `Get(id)`, `List()`, `Create`, `Update`, `Delete`. `TestRoleStore_GetByName` iterates `List()` results, which is how application code would do it. This tests the interface as actually implemented.

2. **`ADMIN_FULL` as enumeration, not wildcard** — `privilege.go` uses exact string matching. There is no wildcard `ADMIN_FULL` token. `TestHasPermission_AdminFull` tests the pattern of putting all platform permissions on an ADMIN role, documenting the intended usage.

3. **Separate `newRoleTestServer` helper** — Rather than extending `newTestServerWithStores` (which would require changing its signature and all callers), a new role-specific helper was created. This avoids breaking the 80+ existing handler tests.

4. **`TestRoleHandlers_Unconfigured`** — Explicitly passes `nil` for `privilegeEngine` and `roleStore` to verify the `s.roleStore == nil` guard in each handler returns 503 (not a panic).

---

## Suggested Follow-ups

- **Silent role fetch errors**: `privilege.go` ignores errors from `roleStore.Get()` other than checking `rerr == nil`. If the store returns an unexpected error (e.g., timeout), the engine silently denies via the fallthrough path. Consider returning the store error explicitly for observability.
- **PermissionCheck middleware integration**: No test currently exercises `X-Participant-ID` + `PermAdminRoleManage` against the role endpoints via the `PermissionCheck` middleware. A future task should add a test where a participant with/without `ADMIN_ROLE_MANAGE` hits POST /roles.
- **Server coverage**: `internal/server` is at 50.7%. The role handler paths are now covered; the remaining gap is in many other handler files that lack tests.
- **`GetByName` convenience method**: Adding `GetByName(name string) (*types.Role, error)` to the `RoleStore` interface would make application code cleaner (currently callers must List + scan). The store test already demonstrates this lookup pattern.
