# T3 Handoff — Platform Admin: Tenant Management Page

**Date:** 2026-04-23
**Status:** COMPLETE — all tests pass (529/529)
**Agent:** Coder (softhouse)

---

## Summary

Implemented the full Platform Admin — Tenant Management feature in `src/admin-ui`:

### Part A — API functions (`src/admin-ui/src/services/api.ts`)
Added three exported functions at end of file:
- `fetchTenants(signal?)` — GET `/platform/v1/tenants`
- `createTenant(data: {id, name, governance_tier?})` — POST `/platform/v1/tenants`
- `updateTenantStatus(id, status)` — PUT `/platform/v1/tenants/{id}/status`

### Part B — Navigation + Route
- **Sidebar.tsx**: `{ label: 'Platform', path: '/dashboard/platform', adminOnly: true, icon: ShieldIcon }` inserted as the **first** item in `operationsNav`.
- **App.tsx**: Added `import { PlatformAdminPage }` and `<Route path='platform' element={<PlatformAdminPage />} />` inside the admin RoleGuard block.

### Part C — Page (`src/admin-ui/src/pages/PlatformAdmin.tsx`)
Full-featured page following the `SecuritiesInstruments.tsx` pattern:
- Title: "Platform — Tenant Management"
- DataGrid columns: id (mono, sortable), name (sortable), status (StatusBadge), flagship (→ "Yes"/"—"), governance_tier, created_at (formatted)
- Actions column: Activate (SUSPENDED/ONBOARDING), Suspend (ACTIVE) — each with aria-label
- Create Tenant modal (role=dialog, aria-modal=true):
  - id: text, required, pattern [a-z0-9-], placeholder "e.g. mse-bonds", hint text
  - name: text, required
  - governance_tier: select STANDARD/FLAGSHIP/SANDBOX (default STANDARD)
  - Blur-triggered inline errors (touch-aware), submit disabled until valid
  - Calls createTenant → showToast 'Tenant created' → refresh
- usePolling(fetchTenants, 30000), loading={isLoading}
- Error state: user-friendly message + Retry button (role=alert)
- Empty state: handled by DataGrid emptyMessage prop
- useToast for all mutations
- Exported `validateTenantForm(form)` as pure function

### Part C — CSS Module (`src/admin-ui/src/pages/PlatformAdmin.module.css`)
Follows project CSS variable convention (`--accent-blue`, `--bg-secondary`, `--border-primary`, `--radius-sm`, etc.). Includes responsive breakpoint at 768px (modal → bottom-sheet on mobile).

### Part D — Tests (`src/admin-ui/src/__tests__/platform-admin.test.ts`)
25 tests across 6 describe blocks:
- TestValidateTenantForm_Valid — 6 tests
- TestValidateTenantForm_MissingId — 3 tests
- TestValidateTenantForm_MissingName — 3 tests
- TestValidateTenantForm_InvalidSlug — 7 tests (uppercase, spaces, underscores, special chars, dots)
- Both fields missing — 1 test (exactly 2 errors)
- Platform Tenant API — 5 tests (fetch GET, create POST with/without governance_tier, status PUT ACTIVE and SUSPENDED)

---

## Test Results

```
npx tsc --noEmit → 0 errors
npm test -- --run → 529 passed (529), 0 failed, 24 test files
                    +25 new tests in platform-admin.test.ts
```

---

## CX Acceptance Criteria

- Loading state: DataGrid skeleton rows via `loading={isLoading}`
- Empty state: `emptyMessage="No tenants found. Create the first tenant to get started."`
- Error state: `role="alert"` div with user-friendly message + Retry button
- Responsive: CSS breakpoint at 768px; modal becomes bottom-sheet
- Keyboard navigation: all interactive elements reachable via Tab; autoFocus on first modal input
- Accessible: role=dialog, aria-modal, aria-label on action buttons, aria-invalid + aria-describedby on inputs, role=alert on errors
- Toast/feedback: showToast called for create, activate, and suspend (success + error paths)
- Form validation: inline errors on blur (touched fields only), submit disabled until valid
- No raw technical errors: all catch blocks use user-friendly toast messages
- Consistent styling: all CSS uses project design tokens

---

## Files Modified / Created

| File | Action |
|---|---|
| `src/admin-ui/src/services/api.ts` | Modified — added 3 platform tenant functions |
| `src/admin-ui/src/components/Sidebar.tsx` | Modified — Platform nav item first in operationsNav |
| `src/admin-ui/src/App.tsx` | Modified — import + route |
| `src/admin-ui/src/pages/PlatformAdmin.tsx` | Created |
| `src/admin-ui/src/pages/PlatformAdmin.module.css` | Created |
| `src/admin-ui/src/__tests__/platform-admin.test.ts` | Created |

---

## Suggested Follow-ups

- `StatusBadge.tsx` does not have colour entries for `ONBOARDING` or `SUSPENDED` tenant statuses — add to `statusColors` for correct badge colours.
- Component render tests (modal open/close, button clicks) require React Testing Library — add in an RTL-enabled batch.
- Verify gateway routes for `/platform/v1/tenants` align with the API_BASE_URL path once backend (T1/T2) is wired.
