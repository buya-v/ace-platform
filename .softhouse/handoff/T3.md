# T3 Handoff — CLAUDE.md Multi-Tenancy Pivot Update

**Task:** Update CLAUDE.md to reflect the GarudaX multi-tenant platform pivot per GarudaX_Strategy_Directive.md §7
**Status:** COMPLETE
**Date:** 2026-04-23
**Artifact:** `/home/vcp/ace-platform/CLAUDE.md`

---

## Summary

Applied five targeted updates to `CLAUDE.md` per the directive §7 requirements. All existing content (Softhouse architecture, agent roles, state management, known limitations, task JSON schema, all learned patterns) was preserved. Only the five specified sections were modified.

## Changes Made

### 1. Product Vision section (lines 1–47, replaced)
- Replaced heading `# GarudaX — Universal Exchange Platform` with `# GarudaX — Multi-Tenant AI-Native Trading Platform`
- Replaced the "Three Pillars" / "Target Market" narrative with the multi-tenant framing:
  - Tenant table: ace-commodities (ACTIVE), mse-equities (ONBOARDING, flagship)
  - Platform Invariant callout

### 2. Current State (replaced inline within Product Vision section)
- Updated service count from 10 to 11 (adds securities-service)
- Reflects Phase 7 securities module as complete
- Adds multi-tenant platform pivot in progress note

### 3. Roadmap (replaced inline within Product Vision section)
- Replaced Phase 7–11 linear roadmap with Phase 0.5–10 multi-tenant roadmap
- Phase 0.5 marked complete; Phase 0.6–0.8 are the retrofit and build sequence

### 4. Learned Patterns — appended new entry
- Added `### Pattern: Multi-tenancy directive (2026-04-23)` before the `<!-- LEARNED PATTERNS END -->` comment
- Documents the directive context, finding, and action for all downstream agents

### 5. File Layout Convention — added venues/ subtree
- Inserted `venues/ace-commodities/` and `venues/mse-equities/` entries into the project layout block (between `prompts/` and `src/`)

## Source Documents Consumed

- `GarudaX_Strategy_Directive.md` — §7 (Updates to Project Memory) for exact wording requirements
- `CLAUDE.md` — full file read to understand existing structure and preserve all content
- `.softhouse/handoff/T1.md` — phase numbering and architecture decisions (V29-V30, migration phasing 0.5→0.6→0.7→0.8)

## Key Decisions Made

1. **T3.md overwrite:** The prior T3.md contained Securities Orders & Positions page work from an earlier softhouse run. This handoff overwrites it as T3 is the designated ID for this CLAUDE.md update task.

2. **Learned Patterns placement:** New pattern appended immediately before `<!-- LEARNED PATTERNS END -->` comment, following the established convention of all prior patterns.

3. **No content deleted:** All existing learned patterns (60+ patterns from Phase 0 through Phase 7), agent role descriptions, state management tables, known limitations, and task JSON schema were left unchanged.

4. **venues/ placement:** Inserted between `prompts/` and `src/` in the file layout block — logical grouping (platform config before application code).

## Suggested Follow-ups

- **Phase 0.6 tasks:** Implementation agents should read `docs/platform-architecture.md` Appendix A.1 (schema rename map), A.2 (topic rename map), A.3 (IRSA role map) as primary input for the ace-commodities retrofit.
- **Reviewer Agent:** Should now enforce the new rejection rule from the learned pattern — any task without tenant context in its API surface is rejected.
- **Planner Agent:** On next planning pass, read the updated Learned Patterns section (especially the multi-tenancy directive pattern) before generating the Phase 0.6 task graph. The ace-commodities retrofit cluster must be fully sequenced before any mse-equities builder work.
- **`AiX_Project_Knowledge.md`:** §1 and §2 still reference the old single-tenant framing — these updates were out of scope for this task but are recommended per directive §7.
