# T3 Handoff — Securities Service: Matching Engine + Store + Kafka Tests

**Status:** done
**Agent role:** test_writer
**Run date:** 2026-04-23

---

## Summary

Created comprehensive unit tests for the securities-service matching engine, store layer, and kafka package. All tests pass under the Go race detector. Engine package coverage reached **85.6%**, exceeding the 80%+ target.

## New Files

**`src/securities-service/internal/engine/engine_test.go`**
- Package: `engine_test` (black-box testing via exported API)
- 17 test functions covering all specified scenarios plus additional edge cases
- Key insight: `MatchOrder` calls `orderStore.Update(incomingOrder)` after a fill, so the incoming order must be submitted to the store before calling `MatchOrder`. Created `submitAndMatch` helper that does both steps.
- Helpers: `testStores` struct, `setup()` for ACTIVE-instrument tests, `submitAndMatch()` for safe matching

**`src/securities-service/internal/kafka/producer_test.go`**
- Package: `kafka_test` (black-box)
- 12 test functions: producer publish, drain idempotency, hook invocation, close behavior, nil-safety for all 3 wiring functions, full payload round-trip for PublishTradeExecuted

## Modified Files

**`src/securities-service/internal/store/store_test.go`**
- Appended: `TestTradeStore_Create`, `TestTradeStore_Create_Duplicate`, `TestTradeStore_Get`, `TestTradeStore_ListByInstrument`
- Appended: `TestPositionStore_GetOrCreate`, `TestPositionStore_Update`, `TestPositionStore_List`
- Appended: `TestOrderStore_Update`, `TestOrderStore_Update_NotFound`

## Test Coverage Results

| Package | Coverage | Notes |
|---|---|---|
| `internal/engine` | **85.6%** | Exceeds 80%+ target |
| `internal/kafka` | **82.4%** | producer + wiring + nil-safety |
| `internal/server` | **73.7%** | Existing tests (unchanged) |
| `internal/store` | **44.1%** | pg_store.go has no unit tests (requires PostgreSQL), pulls metric down |

Zero test failures, zero race conditions detected across all packages.

## Engine Tests (17 total)

1. `TestMatchOrder_LimitBuySellExactCross` — exact price cross, both FILLED, trade at 50.00
2. `TestMatchOrder_LimitBuyAboveSell` — aggressor buys above ask, executes at resting price (50.00 not 55.00)
3. `TestMatchOrder_NoMatch` — bid < ask, 0 trades, both remain PENDING
4. `TestMatchOrder_PartialFill` — sell 100, buy 60 → sell PARTIALLY_FILLED, buy FILLED
5. `TestMatchOrder_MultipleFills` — 2 resting sells at different prices, buy sweeps both
6. `TestMatchOrder_MarketBuy` — market order crosses any resting sell
7. `TestMatchOrder_MarketBuyNoLiquidity` — market order with empty book stays PENDING
8. `TestMatchOrder_PositionUpdate` — buyer +qty, seller -qty verified in position store
9. `TestMatchOrder_AvgCostCalculation` — two buys at different prices → weighted avg = 55.00
10. `TestMatchOrder_HaltedInstrument` — error returned, no match attempted
11. `TestMatchOrder_PriceTimePriority` — same price, two sells, earlier timestamp matched first
12. `TestMatchOrder_KafkaEventPublished` — 1 record on TopicTradeExecuted, key = trade ID
13. `TestMatchOrder_UnknownInstrument` — instrument lookup error path covered
14. `TestMatchOrder_IncomingSellMatchesBuy` — incoming SELL branch in updatePositions
15. `TestMatchOrder_IncomingSellPartialFill` — resting buy exhausted, sell partially filled
16. `TestMatchOrder_MarketSell` — market sell vs resting limit buy
17. `TestMatchOrder_PartiallyFilledRestingOrder` — PARTIALLY_FILLED resting orders picked up correctly

## Decisions Made

- **`submitAndMatch` helper**: the engine calls `orderStore.Update(incomingOrder)` after every fill. `Update` requires the record to pre-exist, so every test must `Submit` the incoming order before calling `MatchOrder`. The helper encapsulates this.
- **Fixed timestamps**: `ts(offset int)` returns deterministic RFC3339 strings anchored to 2026-01-01T00:00:00Z, ensuring price-time priority tests are reproducible.
- **`MaxRetries: 0`** in kafka test config: avoids exponential backoff sleeps (100ms × 2^n) when no Kafka broker is present.
- **store coverage floor**: `pg_store.go` brings the store package metric to 44.1% because it requires a live PostgreSQL connection. The in-memory implementations that the engine uses are fully exercised.

## Suggested Follow-ups

- Add pg_store integration tests using `testcontainers-go` (would bring store coverage to ~70%+)
- Add `TestMatchOrder_SellerAvgCostPreserved` — seller AvgCost unchanged after selling
- Add `GET /api/v1/securities/trades` and `GET /api/v1/securities/positions` endpoints with handler tests
- Wire a real Kafka adapter behind the `Producer` interface for production deployment

---

*(Original T3 content for "Integration Test Run: Gateway Wiring Verification" preserved below)*

---

---

## Summary

Full test suite executed against the Gateway Wiring changes. All Go tests pass, frontend tests pass, e2e tests compile, and all key structural requirements are satisfied.

---

## Test Results by Service

### 1. `src/securities-service` — `go test ./... -race -cover`

| Package | Result | Coverage | Notes |
|---|---|---|---|
| `internal/server` | PASS | 73.5% | 49 top-level tests (+ subtests) |
| `internal/store` | PASS | 33.5% | 15 top-level tests (+ subtests); in-memory store |
| `internal/types` | N/A | — | No test files (type declarations only) |
| `cmd/securities-service` | Build warning | — | `go: no such tool "covdata"` — Go version lacks covdata; tests still pass |

**Total securities-service: PASS. 64+ tests, 0 failures.**

Coverage note: `store` coverage (33.5%) is low because `pg_store.go` (PostgreSQL implementation) is not tested — only the in-memory `store.go` is exercised. This is expected at this stage; pg_store requires a live database.

### 2. `src/gateway` — `go test ./...`

| Package | Result |
|---|---|
| `internal/auth` | PASS |
| `internal/bot` | PASS |
| `internal/fees` | PASS |
| `internal/handler` | PASS (coverage: 53.1%) |
| `internal/kafka` | PASS |
| `internal/middleware` | PASS |
| `internal/observability` | PASS |
| `internal/refdata` | PASS |
| `internal/reporting` | PASS |
| `internal/risk` | PASS |
| `internal/router` | PASS |
| `internal/tickets` | PASS |

**Total gateway: 12/12 packages PASS, 0 failures.**

### 3. `tests/e2e` — `go build ./...`

**PASS — compiled successfully.** No build errors. (Tests skip gracefully at runtime when gateway is not reachable; the build step confirms no broken imports or syntax errors.)

### 4. `src/admin-ui` — `npm test -- --run`

**PASS — 20 test files, 404 tests, 0 failures.**

```
 Test Files  20 passed (20)
      Tests  404 passed (404)
   Duration  10.11s
```

---

## Structural Verification

### Port Collision: 8089 (not 8085)?

**RESOLVED. No collision.**

- `securities-service` uses `API_PORT: 8089`, exposed as `"8089:8089"` in `docker-compose.yml`
- Port 8085 belongs to `auth-service` — a different service entirely
- `SecuritiesServiceAddr` in gateway config defaults to `localhost:50059` (gRPC), and `main.go` derives the HTTP address as `securities-service:8089`

### DB Store Present?

**YES.** `src/securities-service/internal/store/pg_store.go` exists. It provides the `PgInstrumentStore` and `PgOrderStore` implementations backed by PostgreSQL. The in-memory `store.go` is used for tests; `pg_store.go` is the production path.

### Gateway Config: `SecuritiesServiceAddr` present?

**YES.** `src/gateway/internal/config/config.go` line 39:

```go
SecuritiesServiceAddr: envStr("SECURITIES_SERVICE_ADDR", "localhost:50059"),
```

And `docker-compose.yml` passes `SECURITIES_SERVICE_ADDR: securities-service:50059` to the gateway container.

### Gateway Routes Wired?

**YES.** `src/gateway/internal/handler/routes.go` registers 9 securities routes:

```
GET    /api/v1/securities/instruments        → ListSecuritiesInstruments
POST   /api/v1/securities/instruments        → CreateSecuritiesInstrument
GET    /api/v1/securities/instruments/{id}   → GetSecuritiesInstrument
PATCH  /api/v1/securities/instruments/{id}   → UpdateSecuritiesInstrument
PUT    /api/v1/securities/instruments/{id}   → UpdateSecuritiesInstrument
GET    /api/v1/securities/orders             → ListSecuritiesOrders
POST   /api/v1/securities/orders             → SubmitSecuritiesOrder
GET    /api/v1/securities/orders/{id}        → GetSecuritiesOrder
DELETE /api/v1/securities/orders/{id}        → CancelSecuritiesOrder
```

All handlers in `handler.go` forward via `h.forward(w, r, "securities-service", "SecuritiesService/<Op>")`. The gateway's `main.go` maps `"securities-service"` to `http://securities-service:8089`.

Admin health endpoint (`GET /api/v1/admin/health`) includes `securities-service` at port 8089 in its service list.

---

## Coverage Summary

| Service | Business Logic Coverage | Notes |
|---|---|---|
| securities-service/server | 73.5% | Handler + validation paths |
| securities-service/store | 33.3% | In-memory only; pg_store untested (needs DB) |
| gateway/handler | 53.1% | Securities routes not individually tested in handler_test.go (covered via route registration check) |
| admin-ui | 404/404 frontend tests passing | |

---

## Blockers / Issues Found

1. **`go: no such tool "covdata"`** — The installed Go version does not include the `covdata` binary. This only affects coverage aggregation for the `cmd/` binary package; all library packages test and report coverage normally. Not a blocker.

2. **`pg_store.go` not covered by tests** — PostgreSQL store requires a live database. Store coverage (33.3%) reflects in-memory store only. This is acceptable for a dev-phase test suite but should be addressed before production readiness.

3. **Securities routes not explicitly in `handler_test.go`** — `TestRouteRegistration` checks `len(routes) >= 48` but does not assert the 9 securities routes by name. The routes are verified to exist via code inspection; a future task should add explicit securities route assertions to the test.

---

## Suggested Follow-ups

1. Add explicit securities route assertions to `internal/handler/handler_test.go` (`TestRouteRegistration` should enumerate all 9 `/api/v1/securities/` routes).
2. Add integration tests for `pg_store.go` using `testcontainers-go` or a local PostgreSQL instance.
3. Increase `pg_store.go` coverage as part of a dedicated database test task.
4. Add `securities-service` forwarding path to `TestServiceForwarding` in handler_test to verify the proxy chain end-to-end in unit tests.
