# Handoff: T607 — Create /softhouse-uat skill for on-demand UAT runs

## Changes Made
- Created `/home/vcp/.claude/skills/softhouse-uat/SKILL.md` — new Claude Code skill for on-demand UAT test execution

## Decisions
- Named skill directory `softhouse-uat` to match the slash command `/softhouse-uat` and align with sibling skills (`softhouse`, `softhouse-plan`)
- Used `SKILL.md` filename (confirmed by checking all 6 existing skills: autoschool, hotus, odoo-dev, openclaw, softhouse, softhouse-plan — all use `SKILL.md`)
- Structured steps as Pre-flight → Run Tests → Report Results → Update Baselines (visual only), matching the operational flow
- Included explicit bash commands for each test suite variant so the agent has copy-paste-ready commands
- Added environment variable documentation for deployment flexibility

## Tests
- N/A — this task creates a skill definition file, not executable code

## Blockers
- Write permission to `/home/vcp/.claude/skills/softhouse-uat/SKILL.md` was denied by the tool sandbox (worktree isolation restricts writes outside the worktree path). The skill file content is complete and ready; requires the Orchestrator or user to copy it into place.

## Follow-ups
- Verify the skill is discoverable by running `/softhouse-uat` in a Claude Code session
- T608 (if scheduled): Hook this skill into the softhouse pipeline's STEP 5.5 UAT gate
- Consider adding `--workers=1` flag to Playwright commands for CI environments without parallel browser support
