# T2: Update web-ui tests for securities field names

**Status**: done
**Agent**: test-writer (claude-sonnet-4-6)
**Date**: 2026-04-27
**Build**: `cd src/web-ui && npx vitest run` — 157 tests pass (0 failures)

---

## Summary

Updated and extended the web-ui test suite to match the securities-service field names and endpoint paths introduced in T1. Added 4 new tests to `api.test.ts` that explicitly verify the new endpoint paths and the mandatory `X-GarudaX-Tenant: mse-equities` header. The `tradingReducer.test.ts` was already updated by T1 to use the new Instrument interface fields (`id`, `ticker`, `name` instead of `instrumentId`, `symbol`, `commodityName`).

Test count: 153 → 157 (4 new tests added).

---

## Changes Made

### `src/web-ui/src/__tests__/api.test.ts`

Added 4 new tests inside `describe('apiRequest', ...)`:

1. **`includes X-GarudaX-Tenant header in every authenticated request`** — Asserts that every call to `apiRequest` includes `'X-GarudaX-Tenant': 'mse-equities'` in the request headers.

2. **`uses /securities/instruments endpoint for instrument list`** — Mocks a `{ data: [...] }` response (new shape) and verifies the endpoint is `/api/v1/securities/instruments`, with instruments having `id`, `ticker`, `name` fields (not old `instrumentId`/`symbol`/`commodityName`).

3. **`uses /securities/orders endpoint for order submission`** — Verifies POST to `/api/v1/securities/orders` (was `/orders`).

4. **`uses /securities/trades endpoint for trade history`** — Verifies GET to `/api/v1/securities/trades` returning `{ data: [...] }` (was `/trades/history`).

---

## Field Name Verification (Already Correct in T1)

The `tradingReducer.test.ts` instrument mock (updated by T1) already uses the correct securities-service field names:

| Old (commodity) | New (securities) | Status |
|---|---|---|
| `instrumentId` | `id` | Updated in T1 |
| `symbol` | `ticker` | Updated in T1 |
| `commodityName` | `name` | Updated in T1 |
| `{ instruments: [...] }` | `{ data: [...] }` | Covered by new api.test.ts |
| `/instruments?status=active` | `/securities/instruments` | Covered by new api.test.ts |
| `/orders` | `/securities/orders` | Covered by new api.test.ts |
| `/trades/history` | `/securities/trades` | Covered by new api.test.ts |
| (missing) | `X-GarudaX-Tenant: mse-equities` | Covered by new api.test.ts |

---

## Test Results

```
Test Files  8 passed (8)
     Tests  157 passed (157)
  Start at  02:52:31
  Duration  5.60s
```

All 8 test files, 157 tests — 0 failures, 0 skipped.

---

## Decisions Made

1. **No new test files** — All additions fit naturally into existing `api.test.ts`, keeping the test suite flat and easy to navigate.

2. **Instrument mock in new tests uses minimal securities fields** — Only `id`, `ticker`, `name`, `trading_status` tested in api.test.ts; the full Instrument interface (with `isin`, `asset_class`, `lot_size`, etc.) is already tested via `tradingReducer.test.ts`.

3. **Existing 153 tests left unmodified** — All pre-existing tests passed and continue to pass; no regressions.

---

## Suggested Follow-ups

- Add React Testing Library tests for `InstrumentSelector.tsx` to verify the component fetches `/securities/instruments` and renders `ticker` (not `symbol`) in the dropdown.
- Add RTL tests for `Trading.tsx` to verify trade history is fetched from `/securities/trades` and orders from `/securities/orders`.
- Add RTL test for `TradeHistory.tsx` to verify filter dropdown uses `inst.id` and `inst.ticker`.
