# T2 Handoff (Latest) — FIX Gateway Test Suite

**Status:** DONE
**Agent role:** test_writer
**Build:** `go test ./internal/fix/... ./internal/session/... ./internal/broker/... -race -cover` — PASS
**Working path:** `src/fix-gateway`

## Summary

Wrote comprehensive test suites for all four packages in `src/fix-gateway/`. All 73 tests pass with `-race` detector enabled and zero failures.

## Coverage Results

| Package | Coverage | Target | Result |
|---|---|---|---|
| `internal/fix` (parser + mapper) | **95.3%** | 80%+ | PASS |
| `internal/session` | **100.0%** | 70%+ | PASS |
| `internal/broker` (store + handlers) | **96.2%** | — | PASS |

## Files Written

- `src/fix-gateway/internal/fix/parser_test.go` — 13 test functions
- `src/fix-gateway/internal/fix/mapper_test.go` — 18 test functions
- `src/fix-gateway/internal/session/manager_test.go` — 17 test functions
- `src/fix-gateway/internal/broker/store_test.go` — 27 test functions (11 store + 16 HTTP handler)

## Test Inventory

### parser_test.go
- `TestParseMessage_Valid` — SOH-delimited 7-tag message, all tags verified
- `TestParseMessage_Logon` — 35=A, SenderCompID/TargetCompID/EncryptMethod/HeartBtInt
- `TestParseMessage_NewOrderSingle` — 35=D, Symbol/Side/OrdType/Qty/Price/ClOrdID
- `TestParseMessage_EmptyInput` → error
- `TestParseMessage_MalformedTag` — non-numeric tag (XYZ=) → error
- `TestParseMessage_PipeDelimiter` — fallback delimiter path
- `TestBuildMessage` — tags 8, 9, 10 all present
- `TestBuildMessage_RoundTrip` — build then parse, all tags match
- `TestCalculateChecksum` — deterministic sum-mod-256 verification
- `TestCalculateChecksum_KnownValue` — empty→"000", 0x01→"001", 256×0x01→"000"
- `TestGetTag_Helpers` — GetIntTag/GetFloatTag valid/invalid/missing/nil cases (12 assertions)
- `TestParseMessage_OnlyInvalidFields` — no "=" in any field → error
- `TestBuildMessage_SkipsHeaderTrailerTags` — header tags in fields map not double-emitted

### mapper_test.go
- `TestMapNewOrderSingle_LimitBuy` — Side=1 OrdType=2, full field set verified
- `TestMapNewOrderSingle_MarketSell` — Side=2 OrdType=1, Price=0.0
- `TestMapNewOrderSingle_ShortSell` — Side=5, IsShortSell=true
- `TestMapNewOrderSingle_ShortSellAlt` — Side=6 variant
- `TestMapNewOrderSingle_InvalidSide` — Side=9 → error
- `TestMapNewOrderSingle_MissingSymbol` → error
- `TestMapNewOrderSingle_MissingClOrdID` → error
- `TestMapNewOrderSingle_InvalidOrdType` — OrdType=9 → error
- `TestMapNewOrderSingle_ZeroQty` → error
- `TestMapNewOrderSingle_NilMessage` → error
- `TestMapNewOrderSingle_WrongMsgType` — 35=A → error
- `TestMapNewOrderSingle_TimeInForce` — all 5 valid TIF values (DAY/GTC/IOC/FOK/GTD)
- `TestMapNewOrderSingle_StopOrder` — OrdType=3, StopPx captured
- `TestMapNewOrderSingle_StopLimitOrder` — OrdType=4
- `TestMapNewOrderSingle_WithAccount` — Account field propagated
- `TestMapExecutionReport` — all 10 required tags (37/17/150/39/151/14/6/38/35/60), AvgPx 4 decimal places, side mapping
- `TestMapExecutionReport_SellSide` — SELL → FIX "2"
- `TestMapExecutionReport_ShortSellSide` — SHORT_SELL → FIX "5"

### manager_test.go
- `TestSession_CreateAndGet` — all 7 initial fields verified
- `TestSession_GetNonExistent` → nil
- `TestSession_ProcessLogon` — Disconnected → Active
- `TestSession_ProcessLogon_InvalidCompID` → error
- `TestSession_SequenceIncrement` — 3 increments, final OutSeqNum=4
- `TestSession_SequenceIncrement_UnknownSession` → error
- `TestSession_ValidateInSeq` — correct seq increments InSeqNum
- `TestSession_ValidateInSeq_TooLow` — replay → error
- `TestSession_ValidateInSeq_TooHigh` — gap → error
- `TestSession_ValidateInSeq_UnknownSession` → error
- `TestSession_ProcessLogout` — Active → Disconnected
- `TestSession_ProcessLogout_UnknownSession` → error
- `TestSession_ListSessions` — 0 then 3 sessions
- `TestSession_SessionKey` — "SENDER:TARGET:TENANT" format
- `TestSession_StateString` — all 4 states + UNKNOWN(99)
- `TestSession_MarshalState` — before/after logon
- `TestSession_ConcurrentAccess` — 10 read goroutines + 10 increment goroutines; final OutSeqNum=11

### store_test.go (store + HTTP handlers)
Store (11): Create/Get timestamps, duplicate error, GetByID not found, GetByCompID seeded MSE001 + config fields, GetByCompID not found, List growth (1→3), UpdateStatus with UpdatedAt timestamp advance, Update not found, GetByCompID after create, concurrent access (5 goroutines, final count=6).

HTTP Handlers (16): GET /brokers (seeded 1), POST /brokers (with ID / auto-ID / missing fields / invalid JSON / duplicate 409), GET+PUT /brokers/{id} (found / not found / missing segment / invalid JSON), method-not-allowed on both routes, GET /sessions (empty / 2 sessions / method-not-allowed), RegisterRoutes smoke test.

## Decisions Made

1. **Handler tests added to store_test.go** — `handlers.go` is in the same `broker` package. Testing it lifted coverage from 35.6% to 96.2%.
2. **Pipe-delimiter path covered** — `parser.go` supports both SOH and `|`; added `TestParseMessage_PipeDelimiter` to cover that branch.
3. **Race tests for both concurrent types** — `SessionManager` and `InMemoryStore` both use `sync.RWMutex`; concurrent goroutine tests confirm correct lock behavior.

## Blockers Found

None. All types and functions are exported; no external dependencies needed for tests.

## Pre-existing Issue

`internal/server` and `cmd/fix-gateway` both fail with `go: no such tool "covdata"` — this is a pre-existing Go 1.25 toolchain issue unrelated to our changes. Our three packages compile and pass cleanly.

## Suggested Follow-ups

- Investigate the `covdata` missing-tool error in `internal/server`/`cmd/` — may be a build tag or conditional import issue.
- `internal/session/manager.go` has no `DeleteSession` — if session cleanup is needed for graceful shutdown, add it with a test.
- Consider `-fuzz` tests for `ParseMessage` and `CalculateChecksum` to catch edge cases in delimiter and checksum arithmetic.

---

# T2 Handoff (Updated) — Test Writer: InvestigationStore, ReplayStore, BondStore + Handler Tests

**Status:** DONE
**Agent role:** test_writer
**Build:** `go test ./internal/... -race -cover` — PASS (0 failures, 0 race conditions)
**Working path:** `src/securities-service`

---

## Summary

Added InvestigationStore, ReplayStore, and BondStore (types, store implementations, HTTP handlers, and handler tests). 37 new tests across store and server packages. All pass under `-race`. Existing 15 handler test files updated to pass nil for the 3 new store parameters added to `server.New()`.

---

## Previous Content (P4a: LocateStore, RFQStore, GiveUpStore)

Wrote and verified all requested P4a tests covering `LocateStore`, `RFQStore`, `GiveUpStore` (store-level) and three new handler test files (locate, RFQ, give-up). All 25 new tests pass under `-race`.

---

## Files Modified / Created

### Appended to `src/securities-service/internal/store/store_test.go`

| Test | Covers |
|------|--------|
| `TestLocateStore_Create_Approve_Use` | PENDING→APPROVED→USED lifecycle; double-approve/double-use errors; ErrNotFound on all ops |
| `TestLocateStore_ListByFirm` | Filter by borrower firm 10, 20, unknown; unfiltered returns all |
| `TestRFQStore_Create_Respond_Cancel` | OPEN→RESPONDED + OPEN→CANCELLED lifecycles; double-op errors; ErrNotFound; List by status |
| `TestGiveUpStore_Create_Accept_Reject` | PENDING→ACCEPTED + PENDING→REJECTED; reason field; ResolvedAt set; List by fromFirmID |

### Created `src/securities-service/internal/server/handlers_locate_test.go`

| Test | Asserts |
|------|---------|
| `TestRequestLocate_Success` | POST → 201; status=PENDING, id set |
| `TestRequestLocate_MissingInstrumentID` | 422 |
| `TestRequestLocate_ZeroQuantity` | 422 |
| `TestApproveLocate` | POST .../approve → 200, status=APPROVED; double-approve → 409; not-found → 404 |
| `TestShortSellWithLocate` | SHORT_SELL with APPROVED locate → 2xx; locate consumed, second use → 422 |
| `TestShortSellWithoutLocate` | SHORT_SELL without locate_id → 422 LOCATE_REQUIRED |
| `TestListLocates` | GET all; ?firm_id=10 filters correctly |

### Created `src/securities-service/internal/server/handlers_rfq_test.go`

| Test | Asserts |
|------|---------|
| `TestSubmitRFQ` | POST → 201; status=OPEN, id, created_at set |
| `TestSubmitRFQ_MissingInstrumentID` | 422 |
| `TestSubmitRFQ_InvalidSide` | 422 for side not BUY/SELL |
| `TestSubmitRFQ_ZeroQuantity` | 422 |
| `TestRespondToRFQ` | POST .../respond → 200, status=RESPONDED; double → 409; missing quote_id → 400; not-found → 404 |
| `TestCancelRFQ` | POST .../cancel → 200, status=CANCELLED; double → 409; not-found → 404 |
| `TestListRFQ` | unfiltered; ?status=OPEN; ?status=CANCELLED empty |

### Created `src/securities-service/internal/server/handlers_give_up_test.go`

| Test | Asserts |
|------|---------|
| `TestInitiateGiveUp` | POST /trades/{id}/give-up → 201; status=PENDING; trade_id echoed |
| `TestInitiateGiveUp_MissingToFirmID` | 400 |
| `TestInitiateGiveUp_InvalidTradeID` | 400 (non-integer trade ID) |
| `TestAcceptGiveUp` | POST .../accept → 200, status=ACCEPTED, resolved_at set; double → 409; not-found → 404 |
| `TestRejectGiveUp` | POST .../reject → 200, status=REJECTED, reason echoed, resolved_at set; double → 409; not-found → 404 |
| `TestRejectGiveUp_WithoutReason` | 200 even with nil body (reason is optional) |
| `TestListGiveUps` | GET returns all; data array present |

---

## Test Run Results

```
ok  internal/store       2.178s  coverage: 76.0%
ok  internal/server      1.812s  coverage: 46.9%
ok  internal/engine      1.069s  coverage: 84.7%
ok  internal/kafka       1.043s  coverage: 83.0%
ok  internal/middleware  cached  coverage: 100.0%
ok  internal/settlement  1.043s  coverage: 80.0%
```

0 test failures, -race clean. Pre-existing `cmd/` package emits `go: no such tool "covdata"` — unrelated to these changes.

---

## Decisions Made

1. Handler test servers follow the factory pattern in `handlers_surveillance_test.go` / `handlers_audit_test.go` — fresh in-memory stores, matching engine, `TenantMiddleware([]string{testTenant})`, `httptest.NewServer`.
2. Tests are `package server` (internal white-box) — shares `testTenant`, `doJSON`, `assertStatus`, `decodeBody` helpers defined in `handlers_instrument_test.go`.
3. `TestShortSellWithLocate` creates the locate via HTTP then approves it directly via store to keep the test focused on the order submission path (not the approve HTTP path, which has its own test).
4. `TestGiveUpStore_Create_Accept_Reject` exercises `List(firmID)` inline rather than adding a separate `TestGiveUpStore_ListByFirm`.
5. `TestRejectGiveUp_WithoutReason` confirms the handler accepts a nil body (reason is optional per the implementation).

---

## Blockers Found

None. All store interfaces, handler paths, and type field names matched the implementation exactly.

---

## Suggested Follow-ups

- Server handler coverage is at 46.9%. Handlers for instruments, orders, settlements, corporate actions, off-book trades, and tick tables are largely untested. A coverage-improvement task could target 60%+.
- `TestListLocates` currently only exercises borrower-side firm filtering. A follow-up could seed a locate with a non-zero `LenderFirmID` (via `Approve`) and verify `List(lenderFirmID)` returns it.
- Pre-existing `cmd/` `covdata` error should be investigated (Go toolchain path issue).

---

## New Work: Investigation, Replay, Bond (this session)

### New Types Added (`internal/types/types.go`)
- `Investigation` + `InvestigationStatus` (OPEN / CLOSED)
- `ReplaySession` + `ReplayEvent`
- `Bond` + `DayCountConvention` (ACT/360, ACT/365, 30/360)

### New Stores (`internal/store/store.go`)
- `InMemoryInvestigationStore` — Create, Get, List(filters by status), Close(id,findings), AddEvidence(id,ref)
- `InMemoryReplayStore` — CreateSession, GetSession, ListSessions, AddEvent, GetEvents(sorted by Sequence)
- `InMemoryBondStore` — Create, Get, List, UpdateStatus

### Store Tests Appended (`internal/store/store_test.go`)
| Test | Covers |
|------|--------|
| `TestInvestigationStore_Create_Get_List` | CRUD + filter by status + copy-on-read |
| `TestInvestigationStore_Close` | close with findings; double-close error; ErrNotFound; CLOSED filter |
| `TestInvestigationStore_AddEvidence` | multiple evidence refs accumulate; ErrNotFound |
| `TestReplayStore_CreateSession_AddEvents_GetEvents` | out-of-order events returned sorted by Sequence; empty session returns [] |
| `TestBondStore_Create_List` | CRUD + UpdateStatus changes TradingStatus+UpdatedAt + copy-on-read + ErrNotFound |

### New HTTP Handlers
- `handlers_investigation.go` — POST|GET /investigations; GET|POST /investigations/{id}; POST /investigations/{id}/close; POST /investigations/{id}/evidence
- `handlers_replay.go` — POST|GET /replay/sessions; GET /replay/sessions/{id}; GET|POST /replay/sessions/{id}/events
- `handlers_bond.go` — POST|GET /bonds; GET /bonds/{id}; GET /bonds/{id}/accrued-interest with ACT/360, ACT/365, 30/360 logic

### Handler Tests

**handlers_investigation_test.go** (11 tests):
- `TestCreateInvestigation` → 201; `TestCreateInvestigation_MissingFields` → 400; `TestCreateInvestigation_Duplicate` → 409
- `TestListInvestigations_FilterByStatus` — subtests for no filter / OPEN / CLOSED
- `TestCloseInvestigation` → 200; `TestCloseInvestigation_NotFound` → 404; `TestCloseInvestigation_AlreadyClosed` → 4xx
- `TestAddEvidence` → 200 accumulated; `TestAddEvidence_MissingField` → 400; `TestAddEvidence_NotFound` → 404
- `TestInvestigationEndpoints_NotConfigured` → 503

**handlers_replay_test.go** (11 tests):
- `TestCreateReplaySession` → 201; `TestCreateReplaySession_MissingFields` → 400; `TestCreateReplaySession_Duplicate` → 409
- `TestListReplaySessions` — empty/2; `TestGetReplayEvents` — 3 events out-of-order returned sorted
- `TestGetReplayEvents_UnknownSession` → 200, total=0; `TestGetReplayEvents_MissingEventType` → 400
- `TestReplayEndpoints_NotConfigured` → 503

**handlers_bond_test.go** (14 tests):
- `TestCreateBond` → 201; `TestCreateBond_MissingFields` → 400; `TestCreateBond_Duplicate` → 409
- `TestListBonds` — empty/2
- `TestAccruedInterest_ACT360` — 90 days, 5%, 1000 par → 12.50 (90*0.05*1000/360)
- `TestAccruedInterest_ACT365` — 90 days, 5%, 1000 par → 12.33 (90*0.05*1000/365, rounded)
- `TestAccruedInterest_30_360` — Jan1→Apr1 = 3×30 days → 12.50 (same as ACT/360 for whole months)
- `TestAccruedInterest_ViaHTTP` — full HTTP round-trip; `TestAccruedInterest_MissingParams` → 400; `TestAccruedInterest_BondNotFound` → 404
- `TestBondEndpoints_NotConfigured` → 503

### Server Changes
- `server.New()` has 3 new parameters inserted between giveUpStore and dayManager: `investigationStore`, `replayStore`, `bondStore`
- 6 new route prefixes registered
- `cmd/securities-service/main.go` passes the new stores
- 15 existing handler test files updated (nil, nil, nil for the new params)

### Final Test Results
```
go test ./internal/... -race -cover -count=1
ok  internal/engine      84.7%
ok  internal/kafka       83.0%
ok  internal/middleware  100.0%
ok  internal/server      49.1%
ok  internal/settlement  80.0%
ok  internal/store       78.5%
```

### Decisions Made
1. `calcAccruedInterest()` and `roundTo2DP()` are package-level functions (unexported) in handlers_bond.go, directly callable from the test file since tests are `package server`.
2. 30/360 convention uses the standard ISDA formula: clamp d1 and d2 to 30 when both exceed 30 (end-of-month adjustment).
3. GetEvents sorts events in-memory by insertion sort — consistent with the existing single-threaded test patterns.
4. New store params are positioned before `dayManager` so the existing positional argument order changes minimally for the many test sites.

### Suggested Follow-ups
- Expose `PATCH /bonds/{id}/status` — UpdateStatus is in the store but not yet routed.
- Add `GET /investigations/{id}` happy-path handler test.
- Bond and replay endpoints not in OpenAPI spec (`docs/`) yet.

---

## New Work: StrategyStore, CustodyAccountStore, CustodyBalanceStore, CSDTransferStore (this session)

### New Types Added (`internal/types/types.go`)
- `StrategyType`, `StrategyStatus`, `StrategyLeg`, `TradingStrategy`
- `CustodyAccount`, `CustodyBalance`
- `CSDTransferType`, `CSDTransferStatus`, `CSDTransfer`

### New Stores (`internal/store/store.go`)
- `InMemoryStrategyStore` — Create, Get, List(tenantID), Delete; deep-copies Legs slice on Create/Get/List
- `InMemoryCustodyAccountStore` — Create, Get, ListByFirm; copy-on-read
- `InMemoryCustodyBalanceStore` — GetOrUpdate(accountID, instrumentID, deltaQty, avgCost) with composite key `accountID:instrumentID`; upsert with delta quantity
- `InMemoryCSDTransferStore` — Create, Get, Complete(id), Fail(id, reason); state machine enforces PENDING-only transitions

### New HTTP Handlers
- `handlers_strategy.go` — POST|GET /strategies; DELETE /strategies/{id}; validation: name required, ≥2 legs, non-empty instrument_id per leg, positive ratio_qty
- `handlers_csd.go` — POST|GET /csd/accounts; GET|DELETE /csd/accounts/{id}; GET /csd/accounts/{id}/balances; POST|GET /csd/transfers; POST /csd/transfers/{id}/complete; POST /csd/transfers/{id}/fail; DVP requires settlement_amount > 0

### Store Tests Appended (`internal/store/store_test.go`)
| Test | Covers |
|------|--------|
| `TestStrategyStore_Create_Get_List_Delete` | CRUD + tenant filtering + Legs copy semantics + 20-goroutine concurrent safety |
| `TestCustodyAccountStore_Create_ListByFirm` | CRUD + firm filter + all-list + copy semantics + ErrNotFound |
| `TestCustodyBalanceStore_GetUpdate_ListByAccount` | upsert/increment/decrement + multi-account isolation + 50-goroutine concurrent safety (verifies final qty=50) |
| `TestCSDTransferStore_Create_Complete_Fail` | full lifecycle, double-op rejection, ErrNotFound on missing IDs |

### Handler Tests Created

**handlers_strategy_test.go** (12 tests):
- `TestCreateStrategy_Success` → 201; `TestCreateStrategy_AutoID`; `TestCreateStrategy_DefaultType` (CUSTOM); `TestCreateStrategy_TooFewLegs` (0/1/missing legs → 400); `TestCreateStrategy_MissingName` → 400; `TestCreateStrategy_InvalidLeg` → 400; `TestCreateStrategy_Duplicate` → 409
- `TestListStrategies`; `TestListStrategies_FilterByTenant`; `TestDeleteStrategy` (204/200 + 404); `TestDeleteStrategy_NotFound`; `TestStrategyEndpoints_NotConfigured` → 503

**handlers_csd_test.go** (16 tests):
- `TestCreateCustodyAccount` → 201; `TestCreateCustodyAccount_AutoID`; `TestCreateCustodyAccount_MissingFields` → 400; `TestCreateCustodyAccount_Duplicate` → 409
- `TestListAccountsByFirm` (with firm_id filter)
- `TestGetBalances_Empty`; `TestGetBalances_Seeded`; `TestGetBalances_AccountIsolation`
- `TestCreateTransfer_DVP` → 201; `TestCreateTransfer_FOP` → 201; `TestCreateTransfer_MissingFields` → 400; `TestCreateTransfer_Duplicate` → 409
- `TestCompleteTransfer` (200 + 409 double-complete); `TestCompleteTransfer_NotFound` → 404
- `TestFailTransfer` (200 + 409 double-fail + fail_reason echoed); `TestFailTransfer_NotFound` → 404
- `TestCSDEndpoints_NotConfigured` → 503

### Server Changes
- `server.New()` has 4 new parameters inserted between `bondStore` and `dayManager`: `strategyStore`, `custodyAccountStore`, `custodyBalanceStore`, `csdTransferStore`
- 6 new route prefixes registered
- `cmd/securities-service/main.go` passes the new stores
- 18 existing handler test files updated (nil, nil, nil, nil for the new params)

### Final Test Results
```
ok  github.com/garudax-platform/securities-service/internal/store    2.161s  coverage: 80.5%
ok  github.com/garudax-platform/securities-service/internal/server   1.870s  coverage: 50.4%
ok  github.com/garudax-platform/securities-service/internal/engine   (cached) coverage: 84.7%
ok  github.com/garudax-platform/securities-service/internal/kafka    (cached) coverage: 83.0%
ok  github.com/garudax-platform/securities-service/internal/middleware (cached) coverage: 100.0%
ok  github.com/garudax-platform/securities-service/internal/settlement (cached) coverage: 80.0%
```

0 failures, 0 race conditions. Pre-existing `cmd/` package emits `go: no such tool "covdata"` — unrelated.

### Decisions Made
1. `CustodyBalanceStore.GetOrUpdate` uses delta quantity (not absolute) to match how a settlement engine would update positions incrementally.
2. Three Python script passes were needed to update all 18 test files' `New()` call sites due to varying inline/multi-line patterns.
3. `TestCSDTransferStore` asserts `UpdatedAt != ""` after Complete/Fail (not `UpdatedAt == now`) to avoid 1-second RFC3339 resolution false failures.
4. DVP validation enforces `settlement_amount > 0`; FOP does not require settlement amount.

### Suggested Follow-ups
- Handler coverage at 50.4% — CSD and strategy handler tests cover new code but gaps remain in older handlers.
- Add `PATCH /strategies/{id}` to update strategy metadata or status.
- Expose `GET /csd/transfers` list endpoint (currently only Create and individual ops).
- Strategy and CSD endpoints not in OpenAPI spec (`docs/`) yet.
