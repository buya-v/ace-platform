# T802 — Go Tests for Guided Prompts and Intent Disambiguation

## Status: DONE

## Summary

Added 22 new tests to `src/gateway/internal/bot/executor_test.go` covering guided prompt behaviour, list handler disambiguation, full CRUD execution, and the default handler.

All 22 new tests pass. Full suite passes with no regressions.

## Tests Added

### Guided Prompts (incomplete commands → guide, not list)
1. `TestExecutor_GuidedPrompt_CreateNewInstrument` — "create new instrument" → contains "provide" or "example"
2. `TestExecutor_GuidedPrompt_CreateInstrumentNoParams` — "create instrument" → mentions commodity/contract fields
3. `TestExecutor_GuidedPrompt_NewInstrument` — "new instrument" → guided prompt fires
4. `TestExecutor_GuidedPrompt_CreateCommodityNoParams` — "create commodity" → mentions "category" or "unit"
5. `TestExecutor_GuidedPrompt_AddFee` — "add fee" → mentions "fee" and "rule"/"schedule"
6. `TestExecutor_GuidedPrompt_CreateTicket` — "create ticket" → mentions "bug"/"describe"/"title"
7. `TestExecutor_GuidedPrompt_NewReceipt` — "new receipt" → mentions "issue receipt" or "holder"
8. `TestExecutor_GuidedPrompt_RegisterFacility` — "register facility" → mentions "facility"/"register"
9. `TestExecutor_GuidedPrompt_CreateFacility` — "create facility" → mentions "facility"
10. `TestExecutor_GuidedPrompt_NewOrder` — "new order" → mentions "buy"/"sell"
11. `TestExecutor_GuidedPrompt_CreateOrder` — "create order" → mentions "buy"/"sell"

### List Handlers (show/list prefix still works)
12. `TestExecutor_ListHandler_ShowInstruments` — "show instruments" → calls list API, returns instrument data
13. `TestExecutor_ListHandler_ListCommodities` — "list commodities" → calls commodities API
14. `TestExecutor_ListHandler_ShowMarginCalls` — "show margin calls" → calls margin API, no guided prompt

### Full CRUD Commands (execute, not guide)
15. `TestExecutor_FullCRUD_CreateCommodityWithParams` — "create commodity rice grain kg" → calls POST endpoint, returns ✅

### Help
16. `TestExecutor_Help_ShowsAllCategories` — "help" → shows full help with halt/buy/sell/margin/settlement; no API calls; no guided prompt

### Default Handler
17. `TestExecutor_Default_UnknownCommandHasActionableSuggestions` — "xyzzy nonsense" → reply contains "didn't understand" and actionable suggestions (show/help/halt/create)

### Additional Edge Cases
18. `TestExecutor_GuidedPrompt_CreateInstrumentDoesNotCallListAPI` — verifies NO API call occurs for guided prompt
19. `TestExecutor_GuidedPrompt_AddFeeRulePartialParams` — "add fee rule trading" (missing tier+rate) → guided prompt
20. `TestExecutor_GuidedPrompt_CreateInstrumentMentionsExample` — reply includes "create instrument" example syntax
21. `TestExecutor_GuidedPrompt_NewCommodity` — "new commodity" → guided prompt with category/unit mention
22. `TestExecutor_GuidedPrompt_AddReceipt` — "add receipt" → guided prompt with receipt guidance

## Decisions

- Guided prompt tests use a mock server that `t.Errorf`s if any API is called, ensuring guided prompts are purely local (no HTTP round-trips).
- List handler tests use `mockHandler` with `called.Load()` assertions to confirm the correct API endpoint fires.
- Full CRUD test for "create commodity rice grain kg" asserts the POST endpoint is called AND the reply contains ✅ or "created" — confirming regex match fires before the guided prompt branch.
- Default handler test checks for "didn't understand" (executor.go line 903) and at least one of: show/help/halt/create.

## Files Modified

- `/home/vcp/ace-platform/src/gateway/internal/bot/executor_test.go` — appended 22 tests (lines 2230–end)

## Test Run Results

```
ok  github.com/garudax-platform/gateway/internal/bot  0.227s
```

All 22 new tests: PASS. No regressions in existing suite.
